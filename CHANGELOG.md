# Changelog

Versions are the `version` field in `.claude-plugin/plugin.json`. Because that field is set, an installed plugin only picks up changes when it **changes** — pushing to `main` alone ships nothing. CI enforces the bump.

## 3.27.0

*Merge & cleanup*'s close-issues step — the one `merge-pr.sh` cannot do — was miscounted in the sentences that cite it and silent about when it fires, and the two defects sat four paragraphs apart in one section. The count is gone rather than corrected, the timing has a predicate stated once, and `README.md` no longer recommends the command the skill rejects by name. [#144](https://github.com/trinity-ai-labs/orchestration-skills/issues/144), [#138](https://github.com/trinity-ai-labs/orchestration-skills/issues/138).

- **The two fours named different sets, and their agreeing numerically is the whole reason nobody caught it.** The numbered hand-run list's four items end in *close the issues the work resolved*; the helper's four actions are the list's first three plus the mergeability preflight the list has you run by hand **before** its first item. So *"the four steps below … the helper that does all four"* was false about the one item the same section twice says the helper cannot do. Neither count is corrected, because neither was the right one to keep: `bin/merge-pr.sh`'s own header enumerates **seven** steps, so "the helper does four" is a count with no single defensible answer, which is exactly the condition `AGENTS.md`'s second move is for. Both citations are de-ordinalised — *the steps below*, *all of them but the last* — and the relationship the counts were standing in for is now stated instead: the helper does every step below but the last, plus the preflight the list has you run yourself before step 1.

- **The timing predicate amends the sentence that already asserted the timing — it is not a third statement beside two correct ones.** *Merge & cleanup* stated the hand-close twice at two different moments, each pointing at the other; both sentences were individually right and nothing named what reconciled them. The reconciliation now sits in the clause that already said *in the same breath as the merge*, because that is the sentence whose object was unscoped: **which issues you close here, and therefore when any given issue closes, turns on what THIS PR settled.** The issues this PR settled close at this merge; an issue no single increment settles — an umbrella, or an arc whose next cycle is still to run — closes when the arc does. `skills/orchestrate/SKILL.md` §7 and `skills/decompose/SKILL.md`'s *Cross-reference, don't auto-close* each already own a half and are pointed at, not edited; both files are byte-unchanged in this release.

- **The pointer that resolved an arc-level close to a per-merge step now names its referent instead of its position.** *"Close it at close-out with everything else, **which is the step above**"* sent a reader to the per-merge hand-close, whose timing contradicts the sentence making the reference. It reads *close it by hand when the ARC closes rather than at this merge*, and names §7's termination check as what closes it. A positional pointer is the same failure shape as a remote count: it reads exactly as authoritative when it has drifted, and nothing compares it with what it points at.

- **One instance outside the slice's fence, fixed because the fix is what would have broken it.** *The epic branch* → *Mechanics* carried *"scoped to the four steps the helper performs; the FIFTH one is the step an epic changes"* — a remote citation of the very naming this release deletes, one section away. Filing it would have shipped a dangling ordinal this change created, so it is de-ordinalised in place with no other edit to that bullet.

- **`README.md` recommended by name the command `skills/execute/SKILL.md` rejects by name.** L312 said *"close the issue yourself (`gh issue close`)"* while the skill says *"REST rather than `gh issue close`"*, with the rate-limit reason `skills/decompose/SKILL.md`'s *GitHub write mechanics* gives. The README now says REST and carries the reason in one clause. **The timing half of that line gets no new sentence, deliberately:** its closing clause — *"a stray `Closes #<n>` closes that issue the moment that PR merges — too early, if the arc still has cycles to run"* — already names arc-versus-increment as what decides, and a README that condenses this section must not become a second home for a rule the skill now states once. What did change in that sentence is its object: *the issue* became *the issues that PR settled*, which is scoping the imperative correctly rather than restating the rule, and it is in the same clause the REST fix rewrote.

- **Docs: `README.md` updated, `skills/orchestrate/SKILL.md` and `skills/decompose/SKILL.md` deliberately untouched and verified byte-unchanged against the fork point.** Each states the half of the timing rule it owns, so editing either is what would have produced the third statement. `skills/write-issue/SKILL.md`, `skills/review/SKILL.md` and `skills/setup/**` were read for citations of the close-out's step count and carry none. `AGENTS.md` is unchanged — its citation convention covers this case as written, and the ranking picked the move without needing to be widened. `bin/` and `scripts/` are untouched: the helper's behaviour was always correct and this release changes only prose about it.

## 3.26.0

This repo's most-repeated defect finally has a rule instead of a fourth instance. Four releases have each paid for one — 1.8.0 through 3.18.2 shipping a paste-verbatim count that disagreed with itself, 3.22.0 sweeping a closing-check count across three files, 3.23.0 de-ordinalising *Reading a gate result*, 3.24.0 de-ordinalising `Run all six` — and nothing anywhere stated the convention that makes a numbered list safe to append to, so the next insertion was unprevented and the one before it was nearly *edited away* by a breakdown written in good faith. `AGENTS.md` now states it, ranked, beside the rule it belongs with. [#136](https://github.com/trinity-ai-labs/orchestration-skills/issues/136).

- **Ranked, not listed as three equals — and the third move carries its own counter-evidence.** The moves are: an item inside a numbered list cites only what PRECEDES it (`skills/orchestrate/SKILL.md` §3, where item 5 cites *items 1-4*, item 6 cites *items 1-5*, item 7 cites *items 1-5* and *item 6*, so an eighth falsifies none of them); failing that, de-ordinalise a remote citation rather than increment it; and only where a remote count cannot be removed, declare one site authoritative. The ranking is the content. Ordering them as equals would have recommended, in third place and without comment, the exact mechanism this corpus has already watched fail — and a convention that does that is worse than no convention, because it launders a known failure as advice.

- **The instance the issue could not have had, because it was live in the tree while the issue was being written.** 3.19.0's fix for the 1.8.0 → 3.18.2 count *was* declare-one-authoritative: the enumerating paragraph in `skills/execute/SKILL.md` was named the authoritative count, with *"an addition moves the number here AND in every line that cites it."* That enumeration has three citations, not two. The same commit swept two and left `plus the five items above` — **three lines below the paragraph it was rewriting, and visible as a context line inside its own diff hunk.** The mechanism did not decay over releases; it failed in the commit that installed it, at the shortest distance a citation can sit at, under the attention of an author who was at that moment thinking about nothing else. That is why de-ordinalising outranks it: removing the referent removes the work, while declaring an authority only moves the obligation to remember, and the obligation is the part that fails.

- **The count is now stated once, beside the list, and deleted from both remote sites — which overturns a decision 3.19.0 made deliberately.** That release considered dropping the number from the gate-mode bullet and rejected it: *"that bullet is read on its own by a dispatcher picking a gate mode, and 'one of the paste-verbatim items' tells that reader nothing about how many they still owe."* The premise is that the number stays correct, and it has now been falsified twice for this one enumeration. The number's only real service to that reader is a completeness check, which is worthless when wrong and worse than absent when confidently wrong — and the reader assembling the paste set is not reading the gate-mode bullet, they are reading the line directly under the enumeration. `Six items` stays where it is, in the same sentence as the six items, because a count a reader can verify by looking is not a citation and nothing in the convention reaches it.

- **`README.md`'s reconcile gloss reads as a sample, which it always was.** 3.24.0 examined this paragraph and left it, recording that *"its four-item gloss of a six-item list was already illustrative"*. That is a claim about intent: the prose was an em-dash followed by four items joined with *and*, which is the grammar of a completed list, and a reader had no signal that anything was missing. It now names the checklist, points at where it is stated once, and introduces the four with *whose items include*. Two corrections to the issue's accounting, both from reading the paragraph rather than the issue: it is four of **seven**, not four of six, and it is not silent about item 7 — 3.24.0 added a separate sentence to the same paragraph for the scope-drift check. What was genuinely unmentioned is **items 5 and 6**, deferred decisions and derived state. The fix does not complete the list, deliberately: a seven-item enumeration in the README would be a second copy of §3 that drifts, which is the failure this whole release is about, and it would go stale on the eighth item. Marking it a sample survives that.

- **No checker, and the reasoning is stated in `AGENTS.md` so the next person does not re-open it.** The issue asks whether `scripts/check.sh` can reach this and answers probably not; it holds. The citation check catches a stale **path** because a path either resolves in the tree or it does not — the referent is the tree itself. A count has no referent to resolve against, so a checker would have to parse a list out of prose, decide which enumeration a numeral three paragraphs away is talking about, and be right about both. That is a second implementation of the thing it checks, and the check's own comment already draws that boundary for the workspace-path case. The convention says so out loud, because *the gate is green* is exactly the inference that makes an unenforced rule invisible.

- **One more live instance found and filed rather than fixed, per the slice's own fence.** *Merge & cleanup*'s opening reads *"The four steps below … default to the helper that does all four atomically"*, and the numbered list below it has four items whose fourth is closing the issues — which the very next paragraph says the helper cannot do (*"The helper does four steps. Closing the issues the work resolved is a fifth"*). Same class, different section, and a corpus-wide numeral sweep was out of scope for this change. [#144](https://github.com/trinity-ai-labs/orchestration-skills/issues/144).

- **Docs: `README.md` updated, `skills/orchestrate/SKILL.md` deliberately untouched, the rest verified as not-affected by reading.** `skills/orchestrate/SKILL.md` §3 is the convention's worked example and already follows it — the rule was written **from** §3, so §3 changing to match would have inverted the evidence; the diff is checked, not recalled. `skills/decompose/SKILL.md` and `skills/write-issue/SKILL.md` were read for remote citations of a list they do not own and carry none. `skills/review/SKILL.md` and `skills/setup/**` are unaffected: neither enumerates another file's list. `bin/`, `scripts/` and `.agents/` are untouched, and no helper contract, usage line or env var moves.

## 3.25.0

Two gaps found in the same run and unrelated to each other, shipped together because both are prose. *The epic branch* → *Mechanics* never said what an epic does to the issue tracker, and *Worktree creation* never said which copy of `.agents/worktree.json` the helper reads — which is the whole reason a slice that changes that config cannot test itself. [#131](https://github.com/trinity-ai-labs/orchestration-skills/issues/131).

- **The epic branch changes the FIFTH close-out step, and the bullet before it reads as covering the whole close-out.** *"Per-slice close-out needs no change"* is scoped to the four steps `merge-pr.sh` performs; closing the issues the work resolved is the fifth, which *Merge & cleanup* already calls the one that silently goes missing. An epic withdraws that step's only fallback for every slice at once: a slice PR bases on the epic branch rather than on the default branch, so its keyword is inert then and later. In a project whose integration branch simply **is** the default branch the flip happens the moment the branch is cut — outside an epic the keyword fires and the hand-close is a harmless no-op, inside one the hand-close is the entire mechanism. So mid-epic the board is not the arc's progress record; the umbrella body is (`skills/orchestrate/SKILL.md` §7). The new bullet points at *Merge & cleanup* for the mechanics rather than carrying a second copy of them, and it does not defer the hand-close: the fifth step still runs at each slice merge, which is what `skills/decompose/SKILL.md`'s *Cross-reference, don't auto-close* also has the dispatcher doing. *The failure it prevents: a dispatcher reads a board on which nothing has closed all day and either re-dispatches slices that already merged, or closes the arc's issues to catch up — the worse branch, because §7 counts the tracker in its termination check.*

- **A config change cannot be exercised through the helper until it merges, and the two obvious ways round that both fail — for different reasons.** `setup-worktree` resolves the main checkout from the repo's common gitdir, from any cwd including a linked worktree, and reads `<main checkout>/.agents/worktree.json` as a file on disk; `<base>` never enters the lookup and `merge-pr` reads `epicMerge` from the same copy. Reading the config off the branch being cut would change nothing, because at cut time the branch equals its base and the commit adding the key does not exist yet — provisioning happens before the work that changes provisioning. Cutting a **second** worktree off the pushed branch changes nothing either, and for an unrelated reason: the lookup consults no ref, so a fetch moves nothing it reads and the main checkout still holds the old file. A reader told only the first invents the second, so both are stated. The remedy is to apply the effect by hand and say in the hand-back that the config path itself was never exercised; editing the main checkout to make the helper see it early is named and **rejected**, because that file is the shared mutable state the section already warns about. Stated in `/pipeline:execute`'s *Worktree creation* and again, shorter, in `/pipeline:setup`'s Step 5 — the pass that writes the file and so the likeliest producer of a slice that changes it, where it is explicitly not a second direct-commit exception.

- **The same property is why `/pipeline:setup`'s Step 4 works, and saying so is what stops a later reader "fixing" it.** Step 2 writes the config into the main checkout and Step 4 cuts a real worktree to verify it, before any of it is committed — which only works because the helper reads the working tree rather than a ref. That turns the limitation from something that reads as a helper bug into an ordering property with a named beneficiary. Neither *Worktree creation*'s four invariants nor Step 4's numbered list gained an item: both new paragraphs are prose, because neither is a fifth invariant or a seventh verification step.

- **README carried two of the four candidates.** *Per-project config* said which scripts read the config but not from which tree, and having already named the main checkout as where `envFiles` are symlinked **from**, its silence about the config file itself invited exactly the assumption the limitation rejects. The epic-branch paragraph enumerates what an epic changes about the flow — docs land at the end, a structural coordinate is still repointed in its own PR, a marker ref is pushed — and the tracker is one more member of that list, so the enumeration read as complete and was not. The close-out hard rule already states both halves of the keyword rule and needed nothing; the bare-worktree paragraph is about a config that is **absent**, which is a different case from one that changes, and conflating them would have been wrong.

## 3.24.0

`skills/orchestrate/SKILL.md` §4's placement test is arc-relative in every direction — *can the arc land without it?* — and nothing anywhere in the loop asked whether the remaining plan is still the thing the user asked for. The two costs §4 already guards both measure **volume**: non-termination watches whether the plan empties, backlog transfer watches what leaves the run as filed issues. Scope drift is **direction**, so it trips neither — the plan empties on schedule, nothing is over-filed, every cycle closes clean, and the arc terminates having built something else. It slips past §9's *Don't reopen the goal* as well, and that is the mechanism rather than the symptom: no single fold rewrites what the arc is for, because each one is genuinely forced by the tree the fold before it produced, so every link in the chain is valid against the goal as stated. Validity is per-step and drift is cumulative, so a rule that checks steps structurally cannot see it. §3's reconcile checklist gains a seventh item that checks the aggregate instead, and the sections it has to be read beside — §4's placement test, §7's termination, §9's goal rule — each gain the one boundary that keeps them and it readable together. [#123](https://github.com/trinity-ai-labs/orchestration-skills/issues/123).

- **It is a THIRD KIND of check, and that framing is what makes it load-bearing rather than a seventh thing to remember.** §3 already splits itself and item 6 says so out loud — *"items 1-5 all interrogate the PLAN; this one interrogates the TREE"* — and drift is neither: the plan is internally consistent, the tree matches the plan, and what is wrong is that the plan no longer answers the **request**, which is written in neither place. So item 7 states its own contrast in item 6's vocabulary. It also carries the correction the issue asked for: every other item can come back *correct* — including the ones that found real forced work and folded it — which is stronger and more accurate than "come back clean", because the mechanism of this failure is item 3 legitimately firing every cycle and each fold being genuinely forced.

- **Mechanical, per §3's own design principle, or it is a fresh judgement each cycle.** *"Would the filer recognise this?"* is exactly the shape §3's opening failure mode describes — a step re-derived each cycle gets a little shorter each cycle, and the cycle it is skipped produces identical output. So it is two things put on the page and a comparison between them: the arc's goal **in the filer's own words**, quoted from the issue rather than recalled, against what the remaining plan would deliver if every item still in it landed as written. It fires when the second cannot be stated as an instance of the first.

- **A `no` HALTS and reports; it is not a new escalation class, and the alternative was priced before it was rejected.** §6 opens *"Escalate exactly one class of thing: a product or design fork the code and conventions cannot settle"*, and that sentence governs questions asked in place of decisions the loop should be making. A drift-`no` is neither — it presents no fork and is not the loop declining to decide something it could decide — so §7's existing *"A cycle that lands nothing must stop and report"* is the device it belongs beside, and §6 is untouched, its "exactly one class" still literally true. Widening §6 instead would have falsified **five** statements across three files that today all say the same thing, which is this corpus's own recurring defect shape. The loop explicitly does **not** unfold the folded items itself: every one of them was forced when it was folded, and a rule that lets the loop reverse a forced verdict makes the arc stop building.

- **§4 gains the level boundary and nothing else — its two cost bullets and the placement test are untouched.** One paragraph, placed where a reader arrives holding a fresh verdict: that verdict is final for its item, and what item 7 re-examines is the sum. Both misreadings are named because both are silent and they fail in opposite directions — let item 7 reach individual items and every forced fold is re-litigable each cycle until the arc stops building; let the placement test's finality reach the aggregate and item 7 can never fire at all. The substance stays in item 7, per the issue's own warning that filing this beside §4's two guards is what would make it read as more of the same and get tuned out.

- **§7's termination stops reading as two-valued and §9 gains one clause.** Termination was one success (plan empty and close-out green) and one stall (a cycle that lands nothing); a third exit that reads as neither gets resolved into whichever of the two it least resembles, and a loop landing work cleanly every cycle resembles a stall not at all — so the finding goes into the record as an observation while the arc carries on building the wrong thing. §9's *Don't reopen the goal* and item 7 read as opposites and are converses: §9 stops the loop moving the target, item 7 catches the plan drifting from a target nobody moved. That distinction is one clause inside the existing bullet rather than a second paragraph re-arguing item 7.

- **The intro's count is de-ordinalised rather than incremented, and three of the four numerals the breakdown named were already append-stable — the correction is recorded rather than applied.** *"Run all six"* is now *"Run all of them"*, following 3.23.0's de-ordinalising of *Reading a gate result* so an inserted item cannot make it stale. The other three do not move, and moving them as specified would have introduced errors: item 5's *"items 1-4 cannot catch it"* and item 6's *"items 1-5 all interrogate the PLAN"* / *"Items 1-5 all come back clean too"* each name the items **preceding** the item making the claim, so an append leaves all three current, while rewriting them to `1-5` and `1-6` would have had each item cite itself. That is §3's actual convention — every item argues its necessity against what precedes it — which makes every numeral in the list append-stable by construction, and item 7 follows it. A sweep for counts and ordinals citing §3 found no fourth site: `README.md`'s condensation of the checklist carries no count, and `skills/execute/SKILL.md`'s *"Six items"* is the paste-verbatim enumeration, unrelated.

- **Docs: `README.md` was affected in two of the three lines checked, on evidence, and the third survives with an argument.** The change adds user-visible behaviour — the loop can now stop mid-arc while succeeding — and both falsified lines describe the loop's autonomy contract rather than its vocabulary, which is exactly the shape that lands in README invisibly. *"it comes back to you only for a genuine product or design fork…"* is the strongest of the three because *comes back to you* covers any return of control, not just a question, so it now names the halt as a report rather than a question and covers the pre-existing land-nothing stop in the same clause — naming only the new one would have left the line newly misleading in the other direction. The reconcile paragraph gains the check as a sentence, since it is where README describes the reconcile pass and the new item has a user-visible consequence; its four-item gloss of a six-item list was already illustrative and is left as it is. The **follow-up-ownership** bullet's *"You get asked only for a genuine design or product fork…"* survives: its claim is about *asking* and about the disposition of *work an agent surfaces*, and a halt is neither — it hands over no work, and the same reasoning is why §7's existing land-nothing stop never falsified it either. `skills/decompose/SKILL.md` Step 2 is unaffected: its bar governs gaps escalated as questions and findings filed as issues, both of which the halt is explicitly not, and Step 2 runs inside one increment's grounding while this check binds the aggregate. `AGENTS.md` is unaffected — it states this repo's own conventions and carries no restatement of the loop's checklist or escalation bar. No `bin/`, `scripts/` or config file is touched.

## 3.23.0

`skills/execute/SKILL.md` is emphatic that a worktree is only isolated if its dependencies are — *"A worktree is only isolated if its dependencies are. Pay the install."* — and that sentence is right and one noun short. A worktree with its own checkout, its own branch and its own `node_modules` is isolated on the **filesystem** and nowhere else: it still reaches the same database, Redis instance, cache directory, fixed port, queue and bucket as every other worktree on the machine. `.agents/worktree.json` had `envFiles`, `install`, `gate`, `scopedCheck` and `env`, and no key that asked *what shared external resource do the checks touch, and how does each worktree get its own* — so *default to parallelization*, this playbook's opening rule, quietly did not hold for any repo whose gate touches a real service, and nothing said so. A `sharedResources` key now asks it, `/pipeline:setup` asks the maintainer and then falsifies the answer with a concurrent gate, and the dispatcher gains the reading that stops a race being debugged as a defect. [#129](https://github.com/trinity-ai-labs/orchestration-skills/issues/129).

- **The key DECLARES and `setup` VERIFIES — it never provisions, and the plugin never calls a project's isolation code.** The issue left that fork open between declare-only, declare-plus-a-lifecycle-hook, and provision; grounding settles it against the two rules `/pipeline:setup` already lives by. Step 1 is *"Ground the repo. Never guess a command"*, and a shared resource is the one value in this config that cannot be ground out of a lockfile, a script or a CI file — it has to be **asked**. Step 4 is *"Verify it, don't assert it"*, and it can **falsify** the answer empirically. So the key is shaped as a claim a pass can test rather than a promise this plugin has to keep. The **hook** is ruled out by the worked instance rather than by taste: the isolation that actually worked had to sit at the entry point *every* invocation reaches, because this flow's own verify bars routinely tell an implementer to run a targeted test file directly — so anything isolating only what the plugin invokes isolates the wrong set, and placement is necessarily the project's. `skills/setup/SKILL.md` gains **No provisioning** as a hard boundary for the same reason.

- **`envFiles` is why a per-worktree value has nowhere to live, and that is the half nobody knows — so it is stated where `envFiles` is introduced, not only where the new key is.** A tracked config file cannot hold such a value: same file on every branch. A file listed in `envFiles` cannot either, because `envFiles` **symlinks** it — literally the same inode in every worktree. Both obvious homes are disqualified, and the second by a design decision this flow made and had presented as pure upside. It fails by appearing to work: the value resolves in every tree, nothing errors, no check fires, and the trees silently share whatever the last writer put there. What is left is derivation at runtime from the one thing already unique per worktree — its own absolute path — which is what the new key declares.

- **Three states, and the absent one is not the safe one.** The key missing means nobody asked; `[]` means somebody asked and the checks touch nothing outside their own tree; entries mean the question was answered per resource, each carrying `isolatedBy` — the mechanism *and the entry point it sits at* — or an explicit `null` for one that stays shared. Absence and `[]` are the same file to a reader and opposite facts to a dispatcher, so `skills/setup/SKILL.md`'s standing rule that keys with no honest value are left out gains its one carve-out: here, leaving the key out is how the tested answer goes missing.

- **The dispatcher-side rule is the corollary, and it is written to COMPOSE with the failure-set rule already beside it rather than compete.** *Reading a gate result*'s existing bullet widens the read **within** one gate — the failure set spans every package, so never stop at the first summary. The new one widens it **across** gates: several worktrees red at the same moment with infrastructure-shaped errors is one shared resource before it is N code defects. Same instrument, one level out. Keyed on the shape of the errors rather than their count, discriminated by re-gating the branch alone, and answered by narrowing the wave rather than by touching a diff. *Observed twice from opposite directions: three reviewers dispatched during a live gate produced 605 spurious failures and then 151, and the implementer got it right only by calling it environmental instead of "fixing" its branch; and in a repo that had already solved this, commenting out the one line that gave each worktree its own database reproduced it exactly — 116 then 121 failures, deadlocks throughout, every one naming the single shared database.* The cost is the misattribution: the slice that lost the race is the one that looks guilty, so the default response lands a fix in an innocent branch. The section's intro also loses an ordinal on the way past — *"the third bullet"* now names its bullet by content, which is what a corpus that has already shipped a stale count should have written the first time.

- **`skills/decompose/SKILL.md`'s *Sizing* prices it, because that is where parallelism is already priced — and this is the one entry there that can make the right wave width ONE.** Everything else in that list prices parallelism in gate *time*; this prices it in correctness. Two slices that touch no common file still contend for one database the moment their checks run at once, so the wave is sized against the resource rather than the files. Two traps are named with it: a missing key is not a no, and **the drained gate does not cover this** — the machine-wide slot serializes *gates*, so what it never sees is every implementer's `scopedCheck` and targeted test file, which are concurrent by construction, and in a project that declares no queue the slot does not exist at all. Nothing else in that file is touched.

- **The leak is named and deliberately not fixed.** A mechanism deriving a per-worktree name from the worktree's absolute path creates resources that are never dropped and are named after a **path rather than a branch**, so a dead worktree's resource is indistinguishable by name from a live one's; `remove-worktree` tears down the tree and knows nothing the tree created. Nothing owns that cleanup today. Safe teardown needs `git worktree list` as its authority rather than a name, which is a `bin/` change under the frozen helper contract and its own arc — so this states the gap in `execute`, `setup` Step 4's teardown item and the README, and closes none of it. No helper reads the new key: it is read by the skills, and `bin/` and `scripts/` are untouched.

- **Docs: three enumerations of the config exist and all three move, plus the example — swept rather than trusted, and the sweep found no fourth.** `README.md` (its JSON sample, its config table and a new note), `skills/setup/SKILL.md` (Step 1's grounding list, Step 2's table and note, Step 4's verification, the hard boundaries) and `skills/execute/SKILL.md` (*Per-project config*) are the three; `examples/worktree.json` demonstrates the key and still satisfies `scripts/check.sh`'s config-example check, which parses it and runs `setup-worktree.sh`'s own reader against it. `skills/review/SKILL.md` and `skills/decompose/SKILL.md` mention the config file only to name one key each and were verified to enumerate nothing. Two README claims were falsified by the change rather than by its vocabulary and were found by reading for the behaviour: *Why this shape*'s **"Isolated worktrees → parallel tasks never collide; each has its own `node_modules` and branch"**, which is now true only of the filesystem, and *Onboarding a new project*'s account of what `/pipeline:setup` does, which never mentioned a question it cannot ground or a verification that cuts a second worktree. `AGENTS.md` is unaffected: it states this repo's own conventions and carries no copy of the config schema. **This repo's own `.agents/worktree.json` gains `"sharedResources": []`**, which is the whole point of the three-state reading arriving in the same release — a repo that ships "a missing key means nobody asked" and then ships a config where nobody asked is the failure demonstrating itself. `[]` is the honest and checkable answer here: `sh scripts/check.sh` runs shellcheck, `python3`, `pwsh`/PSScriptAnalyzer and `git ls-files` against the tree it stands in, and the only thing it touches outside the worktree is a `mktemp` file with a unique name per call, which is not a resource two runs can contend for. That file sits outside this slice's assigned set and the change is deliberate; nothing reads the key mechanically, so it alters no behaviour.

## 3.22.0

`skills/decompose/SKILL.md` Step 3's closing check tested a slice's fields against each other as three pairs, and none of them tested whether a `Brief`'s *derive X from Y* instruction could actually be carried out from where the slice assigned to it would run. The third pair — any requirement against the vocabulary the target actually has — asks whether the target can *express* what is demanded, settled by reading the target; it says nothing about whether the slice can *reach* what is demanded, which needs a different instrument, resolving an import from the slice's own location. A brief can name a source that is real and correctly cited by Step 1's whole-repo grounding and still sit on the wrong side of a workspace boundary from the slice told to derive from it — existence and reachability are different facts, and only the first was ever checked. A fourth pair now covers the second, and `skills/execute/SKILL.md`'s *A brief that contradicts ITSELF* names the move the incident actually took: an implementer handed an unreachable derivation transcribes the values by hand, the output is correct, the slice goes green, and the brief's false premise is never contradicted anywhere. [#124](https://github.com/trinity-ai-labs/orchestration-skills/issues/124).

- **The count moves in three files, swept rather than trusted against a list of expected locations.** `skills/decompose/SKILL.md`'s lead sentence and `skills/write-issue/SKILL.md`'s `Verify` bullet both stated the count explicitly and both now read "four"; `skills/execute/SKILL.md`'s dispatcher bullet, checked and found to only point at the closing check rather than restate its count, needed no numeral changed. This corpus has shipped exactly this shape of failure once before: `skills/execute/SKILL.md`'s paste-verbatim enumeration gained an item in 1.8.0 and only one of its two counts moved, so a stale count read as authoritative for many releases after.

- **Existence and reachability are now stated as two separate facts, each where it is actually checked.** `skills/decompose/SKILL.md` Step 1's research bullet — the one that finds the real files and modules a slice needs — now says explicitly that it establishes a coordinate's existence in the tree and nothing about whether that coordinate is reachable from where the slice will actually live; that gap is exactly what let this failure read as grounded, and Step 3's fourth pair is where reachability is actually tested.

- **`skills/write-issue/SKILL.md`'s issue-level analogue gains a fourth mapped check to keep its own count honest.** An issue's `Verify` bar is now read against a `Brief`'s `Constraints`, its `Approach`, the target's vocabulary, and — new — whether a named source is reachable from wherever the work will land, so the citation's number and its enumeration stay the same size as each other.

- **Docs: `README.md` is unaffected.** It summarizes `/pipeline:decompose`'s grounding output at the level of owned files, do-not-touch boundaries, waves, conflict maps and model tiers; the closing check's own pair count and the reachability/transcription distinction are grounding-pass mechanics one level below what README condenses, and none of "pairs", "reachable" or "transcription" appears anywhere in it. `AGENTS.md` is unaffected — it states this repo's own conventions and carries no restatement of the closing check.

## 3.21.1

`remove-worktree` resolved a workspace member's worktree at a path that never exists, in **both** shipped languages, and reported success. A repo inside a polyrepo workspace keeps its worktrees at `$WORKTREE_HOME/<workspace>/<leaf>/<repo>`; both ports assembled only the bare `$WORKTREE_HOME/<project>/<leaf>`, found nothing, printed `path does not exist or already removed`, and **exited 0**. `merge-pr`'s step-2 teardown delegated by handing over a **branch name** rather than the absolute path it had already resolved, so the close-out's own workspace awareness was discarded at exactly the step that needed it — and the `.ps1` side's `$LASTEXITCODE` check passed, because the miss was not an error. `--delete-branch` then ran against a branch still checked out in a live worktree, which is the precise failure the documented remove-then-merge ordering exists to prevent. This is shipped code on a user's PATH, so it was a runtime failure on a real machine at the moment of use; polyrepo close-outs have been torn down by hand since. Both halves are fixed, neither substitutes for the other, and the reversal was measured against the fork point in both languages before the fix and after. [#126](https://github.com/trinity-ai-labs/orchestration-skills/issues/126).

- **The layout branch is `setup-worktree`'s answer, not a third one — and it accepts EITHER layout, which is `merge-pr`'s.** The two existing statements of "where does this worktree live" answer slightly different questions: `setup-worktree` is *creating*, so it picks exactly one path; `merge-pr`'s wrong-repo guard is *checking*, so it takes either. A teardown is the second kind, so it prefers the workspace path where the repo sits in a workspace and falls back to the bare one when only that exists — a tree cut before its repo moved into a workspace is at the bare path, and a teardown that could no longer see it would be this same bug pointing the other way. `WORKTREE_DEST` is deliberately **not** read: it is a creation-time input `setup-workspace` sets to place a tree it is about to cut, and at teardown a caller already has a way to name an exact path — pass it as the argument. Reading it would let one stale export silently redirect every later `remove-worktree <leaf>` at a single fixed path regardless of the leaf asked for.

- **`merge-pr` now hands over the path, and the source it hands over is git's registry rather than a fourth assembly of the same string.** It already computed `worktree_holding <head branch>` for the self-teardown guard and then threw it away — so the fix is to keep it, in two forms: the path exactly as git printed it, because that is what the teardown receives and git matches its registry against what it recorded, and the resolved form, because the guards decide sameness by string comparison. Being git's own answer it is right under every layout at once, including anywhere `WORKTREE_DEST` put a tree. The branch name stays as the fallback for the case git has no answer — a detached HEAD, a switched branch, a tree already gone — which is exactly why teaching `remove-worktree` the layout was still required. Blanking that variable when it names the main checkout now does double duty: it is both what stops the self-teardown guard refusing a close-out launched from the one directory that is always safe, and what guarantees the one path this run must never hand to a teardown is never handed to it.

- **The exit-0 conflation is separated by asking git, and idempotence is preserved rather than traded.** *"Nothing is at the path I derived"* and *"the worktree I was asked to remove is already gone"* are different claims that produced identical output and the same exit code — which is exactly why this went unnoticed, since a teardown looking in the wrong place is byte-identical to one with nothing left to do. Git's worktree registry is the authority on which trees a repo has whatever path they sit at, so a **derived** path that resolves to nothing is now checked against it: a registered worktree named for that leaf, at either layout, sitting somewhere else and actually on disk, is a wrong-path failure that stops with the real path named. An **absolute** path was named by the caller, so there is no derivation to have got wrong and nothing is second-guessed. Re-running a close-out whose tree is genuinely gone still exits 0, and a registration left behind by a hand-deleted directory is still just a prune. No caller has to parse a message any more, and the frozen contract needed nothing: no argument, env var, stdout line or exit code was added, renamed or repurposed — 1 already meant "real failure", and this classifies a previously-silent failure as one.

- **The review pass turned up a second, unrelated divergence in the same block, isolated in its own commit.** On the absolute-path branch the `.sh` derived the repo to prune from `git -C "$WT"` — inside the branch that has just established `$WT` is not a directory, so that call could only ever fail. It did, on every absolute-path call: the `.sh` printed `cannot find repo` and skipped the prune, leaving the stale registration behind, while the `.ps1` fell back to `REPO` and pruned it. Same command, same input, two results — reproduced side by side before the fix and after, and the `.sh` now does what the `.ps1` already did. It is exactly the class `AGENTS.md` says the parity check sits too high up to see and review has to catch, which is the second time in this one change that surface-shape parity was green over ports that behaved differently.

- **The parity question is answered yes, and the added assertion demonstrably fires on the fork point.** `scripts/check.sh`'s parity check compares **siblings against each other**, which is structurally blind to an omission they SHARE — two ports that are identically wrong agree perfectly, which is precisely how this shipped green. The form that catches it is therefore per-file rather than comparative, and it stays at the altitude the rest of that check already works at: a token in the comment-stripped source, exactly like the env-var scan. A helper that consumes `WORKTREE_HOME` must read `.agents/workspace.json`. Run against the fork-point `bin/`, it goes red on both `remove-worktree.sh` and `remove-worktree.ps1` and on nothing else. It makes no claim that the branch it finds is *correct* — whether a workspace path is assembled right is semantics, and a checker that judged it would be a second implementation of the thing it checks, which is the widening the issue ruled out. `AGENTS.md`'s two enumerations of what the parity check covers move with it.

- **Docs: `README.md` was affected, on evidence, in two places.** *The mental model* stated the worktree location as one absolute path, `$WORKTREE_HOME/<project>/<branch-leaf>` — the README's only statement of the layout, and the second layout is the whole subject of this fix; it now carries both and says every helper resolves both. The `remove-worktree` row in the helper table claimed the helper safely tears down a worktree, which was false in a polyrepo, so it now says how the tree is found and that a derived path pointing nowhere stops rather than reporting a clean no-op. `AGENTS.md` is updated for the parity check's new assertion in both places it enumerates one. `examples/workspace.json` and `examples/worktree.json` are unaffected — no config key changed. No `SKILL.md` is touched: three have open arcs against them, and none of them documents where a worktree physically lands.

## 3.21.0

A hand-run gate is the only path in this flow that records nothing. A drained ticket produces three artifacts — a streamed log under the queue root, a structured verdict on the ticket in `done/`, and the PR comment — and a gate you run yourself produces none of them, which is why `skills/execute/SKILL.md` had to spend a hard rule on capturing `EXIT=$?` and never piping: the flow hand-rolling, on its least-observed gate, bookkeeping a runner does for free. The two gates that could not be enqueued are now enqueued, by different means. The **close-out gate** needed only a reordering — the epic → integration PR opens as a **draft** first and the gate is enqueued against it — and the rule that appeared to forbid that is restated over the **merge** rather than the open, in both places it was stated, because a draft PR puts nothing on the shared branch. The **mid-arc integration gate** cannot be reordered, so `prNumber`/`prUrl` become optional at enqueue and it goes in as a PR-less ticket. The ceiling is projects that have a queue: a repo that declares none is an opt-out `/pipeline:setup` endorses, and it wants nothing this offers. [#122](https://github.com/trinity-ai-labs/orchestration-skills/issues/122), following [#121](https://github.com/trinity-ai-labs/orchestration-skills/issues/121).

- **The close-out inversion is the whole of that half — no tooling, and the objection answered rather than waved past.** *Gate the integrated whole* said a green gate was the precondition for the epic → integration PR, "so it does not open until the gate is green". The load-bearing half of that is *green before the merge*: GitHub refuses to merge a draft, and this flow already spends the draft flag as the interlock against merging an unread diff, so opening first costs the shared branch nothing and gives the ticket something to attach to. Both statements of it move — the one in *Gate the integrated whole* and the cross-reference in *Docs land at the end*, which had called the ledger's precondition "the same precondition shape" and now says plainly that the ledger's is over the **open** while the gate's is over the **merge**, and why the two differ. What falls out is that the close-out PR stops being an exception: it is a draft carrying a gate comment from the moment it opens, so the note explaining why **Hard rules**' "a non-draft PR that isn't being merged right now is a bug" did not apply to it has nothing left to explain.

- **The mid-arc gate gains a record, not a comment — and the two counts are stated separately because letting one stand for the other is the failure.** *Reading a gate result* listed three paths producing no comment to read. Paths that produce **no durable record at all** go 3 → 1, the survivor being an override-mode slice on a project with no queue. Paths that produce **no PR comment** go 3 → 2, because a PR-less ticket has no PR and never will. So that gate does not leave the section's scope; what changes is what you read — the verdict on its ticket in `done/` rather than a shell's exit status. Read as "everything is commented now", a dispatcher waits on a comment that cannot arrive, and "settled with no comment" is the queue's own signal for a *failed post*, so the wait presents as a delivery problem and the natural remedy retries a post nobody was owed.

- **The spec side states three obligations the optionality creates, none of them inferable.** `skills/setup/references/gate-queue.md` records the absence of the PR fields **at enqueue**, as undeliverable-by-construction, rather than leaving two later readers to guess it from two missing fields: invariant 8's prune has to separate a ticket nobody will report from one whose report failed, and *Reporting* has to keep "settled, no comment" diagnosable — a fourth producer of that state, and the only benign one. Invariant 8 classifies it as **delivered the moment it settles**, so it prunes on the retention window like any green; read as flagged instead, every mid-arc gate becomes a permanent ledger entry and the unbounded growth that invariant exists to bound arrives through the one class of ticket that is working perfectly. Invariant 7 gains the framing that makes both work: this is the state it already builds for, with delivery permanently not applicable rather than pending.

- **Rollout, on the same terms this reference already uses twice.** The queue is scaffolded *into* a project and owned there, so a required argument becoming optional is a contract change reaching runners this repo does not hold. A runner predating it must **reject** a PR-less enqueue and exit non-zero — the treatment invariant 9 already prescribes for an unrecognised `--status`, and for the same reason: the older side makes the gap legible by refusing rather than by doing the adjacent thing. Accepting it silently would spend a full serialized gate and file the result as a delivery failure, which is the one classification that escalates to a human.

- **The seam with 3.20.0's ledger watch is stated where the field is specified, and that subsection is untouched.** The watch scopes on `branch` and prints `prNumber` as its handle; a PR-less ticket has no `prNumber`, so the two compose only because the scoping key was already the right one — by luck until somebody wrote it down. The obligation now sits on the field itself: a consumer scopes on `branch` and treats `prNumber` as absent, with the branch as the handle's fallback. Stating it there rather than in the watch keeps one copy of a rule that binds every reader of a ticket, not just that one.

- **A third gap dissolves rather than being filled: the slot pre-flight nobody had.** Contention is invisible until you have already committed to a gate — a blocked runner announces the holder only on a *failed acquire*, onto its own stderr, in a backgrounded process nobody reads — and the only other way to ask is a drain runner's `--status`, which on a project scaffolded before invariant 9 silently drains. So the careful version of "check who holds the slot before gating by hand" was itself the hazard. With nothing left to hand-run in a queued project, there is no pre-flight to remember and no reason to invoke a drain runner to ask a question. For the same reason "once the fleet is quiet" stops being something anyone polls for: the queue serializes by construction. The enqueue-then-drain instruction reacts to a ticket still sitting unclaimed rather than pre-checking whether a runner is live, because a second drain is idempotent and safe while both instruments for that check are ones this file already records as unreliable.

- **Docs: `README.md` gets two clauses; the plan's not-affected verdict was wrong on both.** *Where the review approval lives* asserted that "the epic tip goes through the integrated close-out check **before the PR is opened**" — the exact ordering this inverts, and a claim the plan's verdict had not looked at, since it scoped the README's gate-queue material to the config table and the *Why this shape* bullet. And the `enqueue`/`drain` row described the queue as something *a PR* joins, which stops being the whole of it the moment a dispatcher's own gate goes in without one — a change in what the keys *mean*, which is the condition the plan named for overturning itself. *Why this shape*'s durable-gate-queue bullet is untouched: its claim is about implementers enqueueing and handing back, which survives intact, and the queue's widened coverage is carried by the two lines above rather than by adding depth the README does not otherwise keep. `AGENTS.md` is unaffected — this repo declares no queue, so its own gating convention is what the change leaves alone. `examples/worktree.json` is unaffected: the `enqueue`/`drain` keys still name the project's own commands. No other `SKILL.md` is touched, by design — two of them have open arcs against them.

## 3.20.0

A dispatcher waiting on the gate had no correct signal to wait on. `skills/execute/SKILL.md`'s *Draining the gate queue* offered a backgrounded drain's completion as the resume signal — but a drain pass loops until the **queue** is empty and the queue is **machine-wide**, so while other sessions keep enqueuing, that exit lands whenever *their* tickets run out, which can be hours after the dispatcher's own ticket settled. The same section's rule against stacking a second drain on a live one makes the other road just as ordinary: your ticket is gated by another session's runner, and then there is no completion event of yours in this session at all. That bullet now says what a drain's exit is not, keeping its `--max N` bound, and a new subsection carries the signal that is actually yours — one persistent `Monitor` over the queue's `done/` ledger, armed once per wave, emitting one line per settlement belonging to that wave. It introduces `Monitor` to this corpus's vocabulary, as a dispatcher-only instrument. The bar it was written against is not a diff but a reading: the Implementer section's foreground ban and the stall-recognition bullet that reads a reported process-wait as a stall both have to still be true afterwards, because the new rule authorizes for one role exactly what the other two forbid for the other. [#121](https://github.com/trinity-ai-labs/orchestration-skills/issues/121).

- **The wave is the unit, not the ticket, and the reason shows up the first time a PR needs work.** A fix agent re-pushes and **re-enqueues**, so a ticket exists that no per-ticket watch was ever armed for — anything keyed to the set of tickets live at arm time is stale the moment the wave moves. The watch keys on the wave's branches, the same set the divergence tick already carries, so every re-enqueue is covered without arming anything new. Two mechanics fall out and are stated because both are silent when wrong: the seen-set is **primed before the loop**, since `done/` is a durable ledger and an unprimed watch replays the entire archive as this wave's news on its first pass, and the dedupe is on the **ticket file** rather than the branch, since a branch that goes red, takes a fix, and re-enqueues settles twice and the second one is the settlement being waited for.

- **It scopes on the enqueue-time fields only, and emits on SETTLED rather than on a verdict.** `skills/setup/references/gate-queue.md` pins the ticket as `{ branch, worktreePath, prNumber, prUrl, mode }`; the verdict facts invariant 7 requires — outcome, exit code, failing tail, gated SHA, delivery — are specified there as *facts the ticket carries*, not as key names, so their spelling belongs to each project's runner. A watch that keyed on verdict field names would be the same portability trap the section's two `--status` bullets already record: correct on the project it was born on, silently wrong on one scaffolded earlier, and indistinguishable from a wave that simply has not settled. So the dispatcher reads the verdict off the ticket and the PR once it is awake.

- **It watches the ledger, not the PR, which is the property invariant 7 was already providing and had never been asked for.** The runner records the verdict on the ticket *before* it attempts to post, so a settled ticket whose comment never landed is a state the design deliberately admits — and the drain section records a PR that sat with no verdict for 37 minutes for exactly that reason. A watch on the PR comment reproduces that hole; a watch on the ledger closes it, because the ledger is where the verdict exists first and the comment is a later delivery of it. `gate-queue.md`'s invariant 7 now names that consumer and the fields it may depend on, having framed the ledger purely as surviving a dying process and a failing network. Invariant 8 is untouched and is what keeps the watch honest at the other end: its `delivered && older than the retention window` predicate already guarantees a just-settled ticket is still present when the waiter wakes.

- **It stays off the divergence tick, and that is a rule rather than a default.** The poll is a local directory listing plus a small JSON read, so a few seconds costs nothing — where the ~10-minute cadence belongs to reading **worktrees**, which is expensive and is answering a different question. Putting a third rider on that tick would be a second helping of the failure the drain rider already names: a dispatcher that comes to read the tick as a list of commands to run stops diffing worktrees, keeping the half that costs nothing to skip and dropping the half whose skipping costs a whole run. Nothing in `skills/orchestrate/SKILL.md` changes — the tick's cadence and purpose stay settled there, and needing an edit in that file would have been the tell that this subsection had drifted into re-deciding them.

- **Silence from the watch is not health, and the teardown is manual for a reason worth stating.** It fires in both directions, because a red settles exactly as a green does and a red is dispatcher feedback wanted at once. What it structurally cannot see is a ticket that never settles — one reclaimed from a dead runner goes back to `queue/` (invariant 2) and produces no event, and neither does one nobody drains — so it is a wake-up, not a liveness check, and the divergence tick stays the instrument that notices a wave that stopped moving. And the queue cannot signal the wave's end, because a red settles too and the wave ends when the dispatcher has merged; `persistent: true` plus a `TaskStop` at close-out is the honest shape, with the cost named: a watch left armed past its wave scopes on branches that no longer settle and goes quiet, which reads identically to a wave that has not settled yet.

- **The dispatcher-only scoping is stated in the subsection's own words, because sitting under `## Dispatcher` is not a scoping.** The asymmetry it rests on is structural rather than a matter of rank: an implementer has a **durable handoff** — push, draft PR, enqueue — which is what makes the wait unnecessary there, and the ticket it enqueued is by design somebody else's to watch; a dispatcher has no handoff to end on and holds the merge decision the settlement feeds. Read together the two rules agree rather than collide — the implementer's turn ends at the hand-back, and the dispatcher's turn is what the hand-back hands to. The stall-recognition bullet is left exactly as it was and still reads correctly, because it is about a wait reported in an *implementer's* hand-back, which a dispatcher's own watch is not.

- **Folded in: the third statement of the full-gate ban was missing the carve-out its two companions carry.** One rule about who runs the gate is stated in three places. *Per-project config* says a project declaring no `enqueue` and no `drain` gates in-line, with override gate mode the **default** there rather than a grant, and the Implementer section's *You do NOT run the full gate* agrees in as many words. The `⛔ Implementers never run the full suite` bullet in *Hard rules (both roles)* did not: its only exception was a slice **explicitly** put in override mode by a brief, "never self-granted", and its wrap-up was unconditionally push → draft PR → **enqueue**. It now carries the same carve-out as a clause, with the other two left authoritative. The failure it prevents is not hypothetical — the plugin's own repo declares neither key, so an implementer here reading the section it goes to for absolutes derives both that it may not gate at all and that its handoff is a command this repo does not define, which is the run *Per-project config* already predicts ending with committed work and no handoff. The closing *Why this shape* paragraph is untouched: it describes the queued shape as rationale rather than instructing anyone.

- **Docs: `README.md` gets one clause, overturning the plan's not-affected verdict on a claim the plan had not looked at.** The two claims that verdict rested on both survive as predicted — the dispatcher bullet's "drains the gate queue, and merges" stays true, and *Why this shape*'s "implementers enqueue and hand back rather than waiting" is reinforced rather than falsified, since the waiting introduced here is explicitly the dispatcher's. But the hard-rules line *"Implementers never run the full gate … they enqueue"* is a **fourth** statement of the rule fixed above, and leaving it was leaving the only ungapped copy in the repo; it now names the queueless default and points at the config table that already carried the fact. Nothing was added there about the watch itself: the README documents no dispatcher polling instrument at all — the divergence tick is absent from it too — so a ledger watch introduced there would be new depth the README deliberately does not carry rather than a stale claim brought in line. `AGENTS.md` is unaffected: it states this repo's own conventions, and the one it carries about gating (`gate` and `scopedCheck` are the same command, there is no queue, run it yourself) is what the *Hard rules* clause now stops contradicting rather than something the clause changes. `examples/worktree.json` is unaffected — the `enqueue`/`drain` keys it demonstrates are unchanged in meaning. No other `SKILL.md` is touched, by design.

## 3.19.2

Check 8 shipped in 3.19.1 extracting `skills/<name>/SKILL.md` and nothing else, and its own block comment named what that left uncovered: `skills/setup/references/gate-queue.md`, a **reference doc cited by path** — the same coordinate, cited the same way, going stale the same way, and outside the check only because the pattern enumerated one filename. The pattern now admits any `.md` under `skills/`, so the check follows the **corpus** instead: a reference doc is covered by the same rule that covers a `SKILL.md`, and the next one added is covered the day it is first cited rather than the day someone remembers to widen a filename list. Everything downstream is untouched — `sort -u`, the `[ -f ]` test, the fail-closed listing of every dangling path with the file and line of every site citing it, and the tracked-files-only scoping that makes it safe to run mid-work. Contributor tooling plus the one README line that describes it; nothing about what the flow does changes, which is what makes it a patch. [#118](https://github.com/trinity-ai-labs/orchestration-skills/issues/118), following [#117](https://github.com/trinity-ai-labs/orchestration-skills/issues/117).

- **The reversal was run twice, because a check that passes before and after proves nothing and the second case is the whole point.** A dangling `skills/<nonexistent>/SKILL.md` in a tracked `.md` reds the gate naming file, line and path, and removing it returns it to green — the 3.19.1 behaviour, still intact. A dangling `skills/setup/references/<nonexistent>.md` was then run against `main` at `05342e4` **first**, where it left the gate green at `EXIT=0`, and against the widened pattern, where it reds identically. That green on the unmodified tree is the only evidence the widening did anything; without it the second case is a check that would have passed either way. Two dangling paths in two different files are still both reported in one run.

- **The residual's count was already stale, in exactly the way the check's own second residual predicts.** The block comment and the 3.19.1 entry both said `gate-queue.md` was cited **4** times. Re-derived here it is **5**, and the history says why: 4 was correct at `bad68a0`, where the check and its comment landed, and the release commit two commits later cited the path once more *in the entry announcing the count* — so the comment went stale inside its own PR, and the entry was wrong the moment it was written, by making itself the fifth. A duplicated count reads exactly as authoritative as a current one, which is the residual that same entry named as mechanically uncheckable. Neither historical entry is edited: a CHANGELOG records what was believed when it shipped, and correcting it in place would delete the evidence. The widened comment carries **no** count at all — the `ok` line prints the live one, from the tree — so there is nothing left in it to drift.

- **Path-*shaped* is the boundary that keeps the check quiet, and widening to bare filenames was considered and rejected.** The pattern stays anchored on the literal `skills/`, so every match names a directory. Matching a bare `README.md` or `AGENTS.md` would have been the natural next widening and is the wrong one: this corpus writes both in running prose constantly — *"the repo's own doc set (`README.md`, `AGENTS.md`/`CLAUDE.md`, …)"* — naming no directory and usually not even this repo, so the check would red on every other project's README mentioned in passing. That is the same noisy-check failure the prose-reference rejection turns on, arriving through the widening instead of through italics, and a gate that fires on correct work is one the next author routes around.

- **The placeholder guard survives the widening, which is load-bearing rather than incidental.** `<` and `>` are outside the character class, so `skills/<slug>/SKILL.md` fails at its first character after the prefix and still does not match. That property is what lets a doc *display* a dangling path with nothing suppressed, and the check's own block comment and the README's *Adding a skill* both carry the form — so a widening that reached it would have red the gate on the check's own documentation. It was verified after the change, not assumed. No per-line opt-out is offered, for the reason 3.19.1 gave: one is indistinguishable from a real stale citation claiming to be deliberate.

- **One false positive the nesting admits is named in the comment rather than engineered around.** The match is unanchored on its left and every sibling repo is called `<something>-skills`, so a `github.com/<org>/<x>-skills/blob/main/<doc>.md` URL would match from its own `skills/` onward and report `skills/blob/main/<doc>.md` as dangling. No tracked doc carries one, and the remedy is the house style anyway — a doc in this repo is cited by repo-relative path, which is the form the check exists to keep alive. A left-boundary guard was considered and rejected: a second grep stage plus a boundary character class whose every member is a judgement call, bought for a case that has never occurred and that names itself in the failure output when it does. Named in the block comment so that red is a diagnosis rather than a mystery.

- **Docs: `README.md` is updated, and 3.19.1's own doc line is what the widening made false.** That release added a paragraph to *Adding a skill* describing the check as covering *"every `skills/<name>/SKILL.md` path cited in a tracked `.md`"* — true when written, false the moment the pattern moved, and invisible to every check here. It now describes a path under `skills/`, `SKILL.md` and reference doc alike, and states the path-*shaped* boundary beside the path-citations-only one that 3.19.1 established and that still holds. The repo-tree diagram below it was checked and is unaffected — no file is added, moved or removed. `AGENTS.md` is unaffected for the reason 3.19.1 recorded: it enumerates the `bin/` parity rule because that one is a *contract between two shipped files*, while check 8 governs prose citations and is described where the gate is described and nowhere else. No `SKILL.md` describes this check — it is contributor tooling, never loaded — so the six skills are untouched.

## 3.19.1

`scripts/check.sh` enforced seven rules and every one of them was a **coordinate** check — a shebang's dialect, a manifest field, a skill's `name` against its directory, a `gh api` flag, both halves of a `bin/` pair. None of them checked that a **cross-skill citation resolves**, while the corpus carries 73 of those in the `skills/<x>/SKILL.md` form across its tracked docs. All 73 resolve today, and nothing would have said so if one stopped. An eighth check now extracts every such path from every tracked `*.md`, tests each against the tree, and fails closed listing every dangling one with the file and line of every site citing it. It needs nothing installed, so it runs in the always-on tier beside the parity check. Contributor tooling plus the one README line that describes it; nothing about what the flow does changes, which is what makes it a patch. [#117](https://github.com/trinity-ai-labs/orchestration-skills/issues/117), following [#114](https://github.com/trinity-ai-labs/orchestration-skills/issues/114).

- **The observed failure was already written down in the corpus, in the rule that came out of it.** `execute`'s warning about naming a section apart from the skill that holds it records a Step 0 line that named `pipeline:orchestrate` — a skill with no *Implementer* section, which opens by forbidding implementation outright. It survived a rename that swapped two skills' names wholesale, read perfectly across seven releases, was caught twice only by dispatchers who happened to be reading the surrounding section, and then passed straight into two live dispatch briefs. The remedy shipped then was **prose** telling authors to bind the reference to its file — the right rule, and it produced exactly the artifact a checker can consume: a path that either resolves in the tree or does not.

- **The residual is two thirds of the observed set, and it is stated in the check's own block comment so it stays countable.** 3.19.0 hit this class twice more and neither is reachable here. A citation by **prose phrase** — `decompose`'s `Verify` field citing *"per `execute`'s own note on backgrounding a banned run"* — has nothing mechanical linking its two ends, which is why that edit had to *add beside* the sentence rather than replace it, under a hand-written brief line and a hand-run grep at review. And a **count duplicated** across two sentences — *"five items … paste all five"* against *"one of the four paste-verbatim items"* — shipped stale from 1.8.0 through 3.18.2, because a stale count reads exactly as authoritative as a current one. Both keep the prose rules already pointed at them, and check 8's comment names them rather than implying the check covers more than it does. A **third** residual is unlike those two and is named in the same comment for the same reason: `skills/setup/references/gate-queue.md` is cited by path 4 times and is a coordinate by every test above — it is outside this check only because [#117](https://github.com/trinity-ai-labs/orchestration-skills/issues/117) scoped the pattern to `SKILL.md`, and widening to it is a character class rather than a new idea. Filed as [#118](https://github.com/trinity-ai-labs/orchestration-skills/issues/118) rather than folded in, so the scope the issue argued for is the scope that shipped.

- **Widening to prose section references was considered and rejected, on a property of this corpus rather than a limit of the pattern.** Italics carry emphasis throughout these files, not only around section names, so any pattern over them is mostly false positives — and a noisy check is one the next author routes around, which costs more than the misses it would have caught. The line drawn is the repo's own, from `execute`'s *Docs land at the end*: **if a checker can tell the reference is stale without reading the sentence, it is a coordinate.** A path names something the tree either has or does not; a prose claim has to be read.

- **73 rather than the 45 the issue names, and the difference is the scope of the scan rather than any change in the corpus.** 45 is the count inside `skills/**` alone; the check scans **every** tracked `*.md`, which adds 19 in `CHANGELOG.md`, 8 in `README.md` and 1 in `AGENTS.md` — 6 distinct paths, all resolving. Narrowing it to `skills/**` was considered and rejected: a citation in the README is read by more people than one buried in a skill, and a dead path there is exactly as dead. The cost is worth naming rather than discovering — a future skill rename makes every historical `CHANGELOG.md` citation of the old path dangle, so the entry recording what shipped then has to be repointed instead of left alone. Taken on the ground that a coordinate a reader can follow is worth more than an archive nobody can.

- **Scoped to git-tracked files, which is what makes it safe to run mid-work.** A worktree routinely carries a gate log, a scratch note, or a stray copy of a doc, and a glob would scan all of them; a gate that goes red on a file the repo does not ship is a gate a contributor learns to stop trusting. `git` is deliberately *not* added to the required-tool list — a bare clone has it by construction, and the check asserts its own set is non-empty at both stages (no tracked `*.md`, or no citation matched) rather than printing the same green as a check that passed, which is the rule every other set-scanning check here already follows.

- **The reversal was run in that order, since a check that passes both before and after proves nothing.** Green on the unmodified tree at 73 citations; a deliberately dangling `skills/<nonexistent>/SKILL.md` citation added to a tracked `.md` turns it red and names the file, the line and the path; removing it returns it to green. A second dangling path added to a different file is listed too, so the check reports the whole set rather than the first — a rename breaks citations in batches, and one-at-a-time reporting would cost a gate run per citation to discover the rest. The same probe placed in an **untracked** `.md` leaves the gate green, which is the tracked-file scoping doing its job.

- **It caught this entry while the entry was being written, which is the one behaviour worth knowing before you hit it.** Check 4 anchors on `gh api` precisely so that prose *quoting* the bad flag to warn against it stays legal; check 8 has no equivalent anchor, because a dead path in a doc is indistinguishable from a dead path being *shown* as an example — the discriminator check 4 has does not exist here. So a doc that needs to display a dangling path writes it in the placeholder form the corpus already uses everywhere else (`skills/<slug>/SKILL.md` in *Adding a skill*, `skills/<name>/SKILL.md` in the check's own comment): angle brackets are not a path component, so the pattern does not match and nothing has to be suppressed. An escape hatch was considered and rejected — a magic comment or a per-line opt-out is a suppression, and the four conditions `execute` places on one cannot be met by prose whose only claim is *"this example is meant to be broken"*, which is exactly what a real stale citation would also claim.

- **Docs: `README.md` is updated; nothing else describes this behaviour.** The user-visible change is what the gate refuses, so the search was for the paragraph that enumerates what the gate enforces rather than for the new vocabulary — [#98](https://github.com/trinity-ai-labs/orchestration-skills/issues/98)'s lesson applied rather than re-learned, since the README describes the gate in plain language and contains none of the new identifiers. *Adding a skill* now names the citation check beside the parity rule, states the path-citations-only boundary, and keeps *"those checks need nothing installed, so they always run"* covering it. The repo-tree diagram below it was checked and is unaffected — no file is added, moved or removed. `AGENTS.md` states the conventions this change obeys (contributor-only `scripts/`, POSIX `sh`, every rule naming the failure it prevents) and enumerates the `bin/` parity rule specifically because that one is a *contract between two shipped files*; check 8 governs prose citations and has no such contract to state, so it is described where the gate is described and nowhere else.

## 3.19.0

Every piece of backgrounding language in the corpus sat inside the no-full-suite ban — three statements in `execute`, one in `review` — so all of it was keyed to a **banned** run and none of it could reach the stall a **permitted** one produces. An implementer that backgrounds its own `scopedCheck` reads that sentence correctly and concludes it is not about them, and the stall shape is identical either way: the turn ends, the handoff never happens, and the dispatcher's only signal is the phrasing of the hand-back. The rule is now stated over the **handoff** — never background a check or command and end your turn on it — added *beside* the ban rather than in place of it. Minor rather than patch, because what the flow does changes: a sixth paste-verbatim item reaches every brief, and the dispatcher's escalation test inverts for one class of hand-back. [#114](https://github.com/trinity-ai-labs/orchestration-skills/issues/114).

- **Observed, and the wait bought nothing at all.** A single-slice `/pipeline:orchestrate` run against `trinity-ai-labs/trinity` (PR #4798). The implementer finished the change, backgrounded `pnpm check` — the project's `scopedCheck`, explicitly permitted, and re-run by the pre-commit hook on `git commit` anyway — and ended its turn with *"I'll pause here and wait for the scoped-check monitor notification before committing"*. The change was complete and needed nothing; it sat uncommitted, with no commit, no push, no PR and no enqueue. The gate runner meanwhile claimed the ticket and spent the claim refusing it — *"the worktree carries 2 uncommitted tracked files, so a gate here would judge a tree no commit holds"* — so the queue already had a detector for the symptom while nothing anywhere had a rule against the cause.

- **The dispatcher tell was not merely silent, it was affirmatively wrong — the one site this gap reached where the playbook produced the wrong action rather than failing to produce the right one.** The escalation conjunct read *"the worktree byte-for-byte unchanged across two consecutive ticks, no live children, AND the agent not reporting a wait"*. The observed hand-back **is** the agent reporting a wait, so the third conjunct was false and the rule instructed a dispatcher not to resume a genuinely stalled implementer. It is narrowed to a wait **on sub-agents it spawned**, with the other kind named as the stall it is and marked *resume*; the infra-stall bullet's closing sentence carried the same conflation and now agrees with it. The behavioural read-through is the check that catches this: the observed hand-back must yield **resume**, and *"waiting on sub-agents I spawned"* must still yield **leave alone**.

- **Scoped to checks and commands, never to backgrounded work in general — the obvious wording would have banned a rule the same section protects.** *"Background nothing whose result you intend to wait on"* forbids exactly the nested sub-agent self-suspension that *Dispatch in the background, then monitor for divergence* defends and tells dispatchers to leave alone. The carve-out is stated in the Implementer paragraph **and** inside the pasted block, because the block is where an implementer reads a rule as absolute and it is the one copy that travels into a brief detached from its argument.

- **One statement of the rule, with the other pointing at it.** `Hard rules` keeps its ban — which is correct about the run it is keyed to — and gains a clause saying why that keying structurally cannot reach a permitted check, plus a pointer naming the Implementer paragraph as the authoritative copy. That is the issue's own `Verify` and this repo's standing rule: two copies drift, and nothing marks which one is stale.

- **`review` carried the same defect at exactly the moment of the observed stall.** The agent was mid-pass, about to commit, with a budget of `scopedCheck` plus one targeted test file — both **permitted**, so §3's *"backgrounding a banned run does not make it allowed"* was unreachable for the only two runs that pass is allowed to make. It gains the handoff-keyed rule beside it, phrased for what this pass actually ends at: its report, never a wait. Shipping the fix in `execute` alone would have left the corpus disagreeing with itself in the one skill an implementer loads at the moment it stalls.

- **The paste-item count was already wrong before this change, in exactly the way the corpus's own duplication rule predicts.** The enumerating paragraph said *"Five items … you paste all five"* and enumerated five; the gate-mode bullet said *"one of the four paste-verbatim items"*. The docs item was added in **1.8.0** and only one of the two numbers moved — it shipped that way from 1.8.0 through 3.18.2, because a stale count reads exactly as authoritative as a current one and nothing compares the two. Both now read **six**, the enumeration is six, and the enumerating paragraph is declared the authoritative count so the next addition moves the number here and everywhere that cites it. **Deleting the second number was considered and rejected:** that bullet is read on its own by a dispatcher picking a gate mode, and *"one of the paste-verbatim items"* tells that reader nothing about how many they still owe — the citation is anchored (*"enumerated above"*) instead, which is where the drift is actually resolved.

- **The contract seam held, and it was the reason the edit adds rather than replaces.** `decompose`'s `Verify` field cites this sentence **by phrase** — *"per `execute`'s own note on backgrounding a banned run, the classic stall shape"* — with nothing mechanical linking the two, so replacing the sentence would have broken the citation with nothing to flag it. All three `banned run` sentences in `execute` survive verbatim; `decompose` is untouched. The one other surviving hit for *backgrounding* is 3.14.2's own entry in this file, which is a record of what shipped then rather than a rule in force now.

- **Docs: `README.md` is not affected, verified by reading rather than by grepping the new vocabulary — [#98](https://github.com/trinity-ai-labs/orchestration-skills/issues/98)'s lesson applied rather than re-learned.** The user-visible behaviour that changed is what a dispatcher pastes into a brief and how it reads a hand-back, so the search was for *that*: the README's role table describes an implementer as coding, greening the scoped check, opening a draft PR, enqueueing and handing back, and describes a dispatcher as dispatching, reviewing, draining and merging — neither enumerates the paste-verbatim items nor states an escalation test, and `background`, `foreground` and `stall` appear nowhere in it. `AGENTS.md` states this repo's own conventions (gate == scopedCheck, no queue, PR-only, the version bump) and carries none of these rules. `orchestrate` points at `execute`'s divergence section rather than restating it, so the narrowed conjunct reaches it for free.

## 3.18.2

*Reading a gate result* is thorough about a verdict a runner **posted** — the comment is the only evidence a gate ran, a PR's flags are not, read every package's result rather than one summary. All of it assumes the comment. But a gate is run **directly** in three sanctioned situations that produce no comment at all, and there the process's exit status is the whole verdict — while nothing anywhere said that a pipeline's exit status is its **last** command's. So `pnpm gate | tail -30` reports `tail`'s 0 whatever the gate did, and `| tail`, `| head` and `| grep` are all the natural reach for a log that runs to hundreds of lines. The rule lands once, beside the `^2` check that sends a dispatcher to run a gate directly; the other two paths point at it. Nothing about what the flow does changes, which is what makes it a patch. [#105](https://github.com/trinity-ai-labs/orchestration-skills/issues/105).

- **Two independent dispatchers reached for the same shortcut within a day, and the one that cost nothing is the reason this is worth a release.** One ran the integration gate on a merged tip as `cd <epic-worktree> && pnpm gate 2>&1 | tail -30` and was told exit 0. The gate had failed at step 5 of 6 — `Failed: trinity#typecheck, trinityailabs.com#typecheck` — and it was caught only because the output happened to be read line by line; the exit status agreed with "green" throughout, and the next step would have been grounding the following wave on a tree that did not compile. The other gated both epic close-outs of an arc **in this repo** with `sh scripts/check.sh 2>&1 | tail -3`. Those were genuinely green, so nothing came of it — and that is the danger rather than a mitigating detail: from a piped status a pass and a failure are byte-identical, so every run that happens to land on green teaches that the shortcut is fine.

- **`| grep` is worse than `| tail`, and the issue's own account of it is corrected here rather than shipped.** [#105](https://github.com/trinity-ai-labs/orchestration-skills/issues/105) reasoned that piping through `grep` to *find* a failure "reports success when there is nothing to find, and reports success when there is." The first half is not what `grep` does — it exits **1** on no match — and the true behaviour is a stronger argument than the one it was standing in for: `gate | grep -i failed` exits 0 exactly when it found a failure and non-zero when the gate was clean, so the status does not merely lose the verdict, it **inverts** it, reporting red as green and green as red. That is the shipped wording, and it is deliberately attached to `grep` rather than folded in with `| tail`: `tail` reads as a shortcut and `grep` reads as diligence, so the invocation most likely to feel careful is the one that is wrong in both directions.

- **Placement was derived, not assumed, and *Gate the integrated whole* wins because two of the three paths already converged there.** The **close-out gate** bullet in *The epic branch* → *Mechanics* already pointed at that section for *whether* the gate is a real signal, so it needed a clause extended rather than a copy planted; *On the gate* and *Merging a shared hotspot* both send a dispatcher to "run one directly" and both mean the same integration gate that section governs; and `orchestrate` §2's Step 5 closes an arc out "per `/pipeline:execute`'s *Gate the integrated whole*", so the loop routes there too. Only **override gate mode** had no route to it at all. Any other home would have cost two new pointers instead of one.

- **It reaches the Implementer section, and the stake there is strictly worse than the dispatcher's.** An override-mode implementer runs the gate itself and posts the result as a comment on its own PR — so its reading of that exit status **is** the comment, and the comment is the one artifact this flow treats as evidence a gate ran. A piped invocation there does not merely mislead the implementer; it manufactures a green verdict for everyone downstream, including the *Reading a gate result* rule that says to trust exactly that comment. Nor is it the rare grant it reads as: a project with **no queue** puts every slice on that path, which is this repo and both sibling skills repos. The section gets the pointer plus what is different there — never a second copy of the argument.

- **The clause added to *Reading a gate result* may be the most valuable line in the change, because that section's framing is what let the gap exist.** It opens as though a posted verdict were the only kind and is thorough enough to look complete, so a dispatcher standing in front of a gate log it produced itself finds nothing there that fits its situation and falls back on whatever the shell said. It now states outright that all of it is about a verdict a runner posted, and hands the direct-run case forward. Its three bullets are untouched — they are correct about the path they were written for, and the defect was the missing boundary rather than anything inside it.

- **`set -o pipefail` is named and rejected in place rather than left as the obvious improvement — and this repo is its own best argument against it.** Every shipped `bin/*.sh` and every CI step here already sets `set -euo pipefail`, which is exactly why the rule cannot lean on it: both observed failures were commands typed at a shell tool, outside any script that had set it. An option that has to be set somewhere else fails silently in precisely the case it was added for, which is the failure being fixed arriving through its own fix. A redirect and `$?` work in every shell and show their own working. Saying so is what stops the next editor simplifying the rule into the version nobody can check.

- **The behaviour search found no second site to correct, and it was run on what the prose claims rather than on the new vocabulary — [#98](https://github.com/trinity-ai-labs/orchestration-skills/issues/98)'s lesson applied rather than re-learned.** Five searches across `skills/`, `README.md`, `AGENTS.md`, `bin/`, `scripts/` and `.github/`: exit-status vocabulary (`exit status|exit code|exits? (non-)?zero|\$\?`), piped invocations (`\| *(tail|head|grep|wc|cut|sed|awk)`), the direct-gate paths (`override gate mode|runs? the (full )?gate itself|in the foreground`), what counts as evidence (`the only evidence|gate comment|is gated iff|attests to a gate`), and how a dispatcher is sent to one (`run one directly|once the fleet is quiet|hand-run|integration gate|close-out gate`). **The gap was an absence, not a false statement** — no doc anywhere recommends or shows a piped gate — so there was nothing to fix elsewhere. Two near-misses are deliberately left alone: `execute`'s stash re-match pipes `git stash list` through `grep -c` and says in the same breath that *"`N` is the decision"*, so the count is the answer and the status is never read; and `decompose` already instructs a reader to *"branch on the exit status rather than chaining this call with `&&`"* for a `gh api` probe, which is the same discipline stated correctly for a different command — evidence the gap was specific to the gate path rather than a corpus-wide blind spot.

## 3.18.1

A verify bar routinely asks an implementer to confirm a negative — *X is unchanged*, *no new Y*, *that grep comes back empty* — and every one of those is a claim about a **difference**, which has two ends. The bar states one. The implementer supplies the other, and the nearest one to hand is its own previous commit, which sits *inside* the change being asserted about, so the claim comes back true regardless of what the change did. The fork-point discipline that answers this already existed in `execute`, twice, and both statements are in **dispatcher** sections: grepping the Implementer section for `merge-base|fork point` returned zero, and `decompose` — which owns the `Verify` field — returned zero too. So the rule sat in the file an implementer loads at Step 0, in a section written for somebody else, about a check the implementer never runs. This closes that hole and adds a pointer; it changes nothing about what the flow does, which is what makes it a patch. [#111](https://github.com/trinity-ai-labs/orchestration-skills/issues/111).

- **Observed on the arc that shipped 3.18.0 earlier the same day.** The wave-1 brief required the frozen helper contract untouched, stdout lines included, and the implementer reported the `merge-pr:` stdout line set *"byte-identical to `a8a24e9`"* — the previous commit on its own branch. True, and carrying no information: the commit it was measured against was itself part of the change under review. Against the fork point that diff adds **seven** `merge-pr:` lines. All seven sit inside the squash branch, so a project that has not opted in really does get byte-identical output and the contract really is intact — but that is the *precise* claim, and it is not the one that was made. It was caught one slice later, by a changelog author diffing against `main` while writing the release entry, which is luck rather than a check: the bar had already passed.

- **`decompose`'s `Verify` field is the authoritative copy, because the rule is about how a bar is authored.** It now requires a bar asserting a negative to name what it is measured against, defaults that to the fork point, and — where the honest claim is narrower than the bar's phrasing, *unchanged for a caller who has not opted in* rather than *unchanged* — requires the bar to say so. That last half is not cosmetic: an implementer that finds the broad claim false has two moves, overturning the bar or quietly narrowing it, and *A brief that contradicts ITSELF* already establishes which of those leaves nothing behind.

- **A sweep is the well-formed exception, and the rule says so rather than leaving a reader to conclude the sweep is now broken.** `orchestrate` §8's bare-string rider greps the old literal across the whole tree and proves zero hits — it names its **domain**, which is checkable with nothing to compare against, and it is untouched. The defect is a bar that names neither a baseline nor a domain. Without that sentence the new rule reads as demanding a baseline for every grep, and the one bar in the corpus that already does this correctly reads as the counter-example.

- **`execute`'s Implementer section gets the same rule from the end that runs the check — a pointer plus the failure, not a second copy of the argument.** Diff against `git merge-base HEAD origin/<base>`, never your own last commit, and never `HEAD` — which is worse than either, since it shows only uncommitted work and goes clean the moment you commit, leaving the claim holding against nothing at all. **It also says outright that this is not the trap the dispatcher's two statements guard.** Theirs is a baseline that *moves*: the integration tip advancing under an open worktree, so a sibling's merged work reads as this branch's phantom deletions. This one is a baseline that is too *close*. Both dispatcher statements are unchanged — they are correct about a different failure, and collapsing the two would cost the reader both.

- **`write-issue`'s `Verify` bullet carried the claim in the phrasing the new vocabulary cannot see, and is corrected here — [#98](https://github.com/trinity-ai-labs/orchestration-skills/issues/98)'s lesson applied rather than re-learned.** Grepping `skills/` for *baseline* or *fork point* finds it nowhere; grepping for the **behaviour** — *unchanged*, *byte-identical*, *comes back empty*, *previous commit* — lands on it immediately: the bullet enumerates what a filed bar contains and lists *"the greps that must come back empty"* among them, with nothing said about what any negative in it is measured against. That is the one bar-authoring instruction left in the corpus not carrying the requirement, and by the same bullet's own words a filed bar is inherited by every slice grounded from that issue — so an unnamed baseline is more durable there than in a slice brief, not less. It takes a one-sentence pointer at `decompose`, in the same form that bullet already uses for the three pairs. There is no fourth site: `README.md` names the verify bar only as a slice-depth field and enumerates no bar contents; `review` anchors its own diff to the fork point, which is that pass's input rather than a bar's negative; `AGENTS.md` states the frozen helper contract the observed failure was about but says nothing about how a negative is verified.

## 3.18.0

`merge-pr` hardcoded `--merge` and `execute` stated the never-squash rule three times unconditionally, so a project had no way to say that one epic should land as one commit. A project may now declare `"epicMerge": "squash"` in `<repo>/.agents/worktree.json` and have an epic branch collapse to a single commit when it merges back into the integration branch it was cut from. The motivating shape is a long-lived release branch sitting thousands of commits ahead of its mainline, where every epic landing as N slice-merges plus a merge commit is what makes the branch unreadable. The default is `merge`, and every project that has never heard of the key behaves exactly as it did. [#99](https://github.com/trinity-ai-labs/orchestration-skills/issues/99).

- **It reaches exactly one merge, and the test for that merge is the definition rather than a proxy for it.** `slice → epic` stays a real merge commit with no opt-out — those merge commits are the epic's review record while the arc is live, and *Gate the integrated whole*'s `^2` check reads their second parent — and single-slice work cuts no epic branch, so it has no buffer to collapse. What separates them is a **relationship, never a branch name**, which matters because *Transient-red window* already forbids keying anything in this flow on a branch-name prefix: an epic branch is the branch an epic's slices PR'd into, and `gh pr list --base <head> --state merged` answers precisely that. A slice branch and a single-slice branch both answer zero, so both stay real merges without needing a rule of their own. Every way any question on that path can fail to produce an answer — gh absent, offline, unauthenticated or rate-limited, a config that will not parse, a value that is not exactly the lowercase `squash` — lands on `merge`, including the config reader, which returns empty where `setup-worktree`'s equivalent exits: there an unreadable config yields a broken worktree wearing every appearance of a good one, here it yields today's behaviour, so the fallback **is** the safe state. **A missed squash is cosmetic; a wrong squash is unrecoverable history.**

- **The near-miss is the sharpest thing in this release, and it was the boundary test on its own — [#107](https://github.com/trinity-ai-labs/orchestration-skills/issues/107).** "Does anything merge into this branch" is the definition of an epic branch and is *also* true of every integration branch, which is the branch every slice of every arc PR'd into — `main` in this repo answers in the dozens. Under `"squash"` a `release/x.y.z → dev` close-out would therefore have collapsed an entire release branch, and the pre-fix helper was made to do exactly that on a scratch repo with **every signal clean**: `mode=squash`, a tip with one parent, both copies of the branch gone, exit 0, and the tree comparison below passing — because nothing is wrong with the tree. The fix is a second condition: **the PR's base must not be the repository's default branch.** That is #99's own standing constraint — the squashes are for an epic buffer collapsing into an integration branch, never for a release branch merging into `main` or `dev` — expressed as something the helper checks rather than as advice someone has to remember.

- **The narrowing has one consequence a reader will otherwise read as a bug: a project whose integration branch simply IS its default branch never squashes, even at the genuine `epic → main` boundary.** This repo is such a project, so `"epicMerge": "squash"` here correctly does nothing. That is the only reading under which the constraint can be **enforced rather than advised**, because nothing distinguishes an integration branch merging into `main` from an epic branch merging into `main` except a prefix, and no prefix is ever the key here. #99 had already told such a project to leave the setting at its default; this makes that automatic instead of something to remember — and #99's remedy was in any case unavailable to the project the feature is actually for, one with both a release branch and `epicMerge: squash`.

- **The squash path deliberately does not pass `--delete-branch`, and that omission is the whole safety story.** The branch has to outlive the merge, because what replaces the guarantee a squash destroys is a comparison against the epic tip captured *before* it: `git diff --quiet <gated epic tip> <squash commit>`. Empty means the integration branch now holds, byte for byte, the tree the close-out gate ran on, and only then is the branch deleted, local and remote. A mismatch — or a comparison that could not be made at all — stops the close-out with **both copies of the branch intact** and everything else already finished, so re-running resumes from exactly that check. Git's "not fully merged" warning is not waved through here, it is answered by a different instrument: a squash commit has no second parent, so the epic branch is never an ancestor of the new tip and that warning fires on **every** squashed close-out, saying nothing. The tree comparison is **stronger** than the `^2` it stands in for, not a weaker substitute — it compares the trees directly instead of inferring coverage from parentage.

- **Which turns the close-out cadence from advisory into enforced, and that is a behavioural change worth its own line.** The gated epic tip and the squash commit have equal trees **only** if the final `merge origin/<integration>` into the epic actually ran before the gate. On the merge path a skipped cadence surfaces as a non-empty `^2` diff a dispatcher is free to glance at and shrug at; on the squash path it surfaces as a failed comparison that **blocks the branch delete**. The cheapest way to hit that failure is to treat the cadence as a nicety, and the merge path is where that habit is formed.

- **The PowerShell port broke twice on the same class, in opposite directions, and it is the `bin/` parity rule's own failure mode.** The first `.ps1` draft **squashed a config the `.sh` merged**: PowerShell's `-eq` and its PSObject property lookup are both case-insensitive, so `"Squash"` and the key `"EpicMerge"` were accepted there and rejected here — one config file, two behaviours, in the destructive direction. It then reappeared through the *new* comparison added for #107, where git refs are case-sensitive and `-ne` calls a base named `main` and a default branch named `Main` the same branch, so the `.ps1` merged where the `.sh` squashed. `scripts/check.sh` is structurally blind to both: it holds the siblings to the same usage line and the same contract env vars, which is surface shape, not semantics — exactly what `AGENTS.md` says the parity rule exists for and exactly what it says the check cannot cover. What caught them was a 13-case × 2-port matrix and then a mutation test that flipped `-cne` back to `-ne` and watched the ports diverge. Both ports now compare the key, the value **and** the branch names case-sensitively.

- **All three unconditional never-squash statements become one conditional rule with exactly one copy of the argument.** `execute` stated it in *Hard rules*, in *Merge & cleanup* step 2, and as *Gate the integrated whole*'s "every merge has a second parent"; `README.md` stated it three more times. Every one of them now points at *The epic branch* → *Mechanics*, which is the single place the two conditions, the tree check and the trade are argued — the other sites carry the **shape** of the exception (an option a project declares, never a judgement anyone makes at merge time, and never something an implementer or a dispatcher decides for one PR) and none of its mechanics. **Never rebase keeps no exception at all.** The one site that needed more than a clause is the hand-run close-out, which now carries the sequence, because it is the one place a step can silently be dropped: `gh pr merge <n> --squash` and **not** `--squash --delete-branch`, capture the epic tip first, compare, and delete only on empty.

- **A config key rather than a `--squash` flag, and that is the design rather than an implementation detail for someone to improve on later.** No new argument, no new env var, no new exit code, both usage lines untouched, and a project that has not opted in gets byte-identical output — the squash path's announcements are additions on lines of their own, which is what the frozen contract permits. The deeper reason is what a flag would *mean*: it would hand an irreversible history decision to whoever runs the close-out, at the moment they run it, which is the one thing declaring it per project up front exists to prevent. Close-out is the worst possible moment for that decision — the work is done, the reviewer wants to be finished, and the choice lands permanently.

- **What the option costs, stated next to the switch rather than left to be discovered.** The arc's individual commits stop reaching the integration branch. They are not destroyed — they live on the epic branch until it is deleted, and on GitHub in each slice's PR and its `refs/pull/*` ref — but after the close-out nothing local holds them, and `git blame` on the integration branch lands on the squash commit for every line the arc touched. On a release branch thousands of commits deep that is frequently the point; it is still a real trade, and it is why `merge` is the default rather than the recommendation.

## 3.17.1

`setup-worktree` reported success for a branch it never created. An epic branch `epic/pricing-and-workspace-cap` had its own worktree; its closing docs slice was cut as `docs/pricing-and-workspace-cap`. The worktree path is derived from the branch **leaf**, so both resolved to one directory: the helper found a worktree already registered there, printed `worktree already exists`, **skipped `git worktree add` entirely**, then symlinked the env files, ran the install, and finished with a `READY:` line and a `HEAD:` line byte-identical in shape to a success. `docs/pricing-and-workspace-cap` was never created and the tree stayed on the epic branch. Both ports now read the branch back off the tree and refuse. [#103](https://github.com/trinity-ai-labs/orchestration-skills/issues/103).

- **Invariant 2 could not have caught it, which is why the check has to live in the helper.** It compares the printed `HEAD:` against `git rev-parse origin/<base>` — and the tree handed back is standing on `<base>` itself, so the two shas **match** and the check passes. The one guard built to catch a wrong tree is blind to this wrong tree precisely because it is the base's own. What was left after it was the implementer's own `git branch --show-current`, one whole dispatch later and only if actually performed; without it the slice writes into the epic's merge-point worktree and its commits land on the epic branch, with no PR and no review. The helper now stops before there is a `HEAD:` line for invariant 2 to be fooled by.

- **Keyed on the observed branch, never on a name pattern — the same rule *Transient-red window* already states for the marker ref.** No branch prefix is read by any helper in `bin/`, so a check keyed on `epic/*` would agree with the branch only for as long as whoever cut it happened to pick that prefix, and would fail silently in both directions. Any two branches sharing a leaf collide whatever they are called; comparing what the tree is *on* against what was *asked for* is the only test that holds for all of them, and it needs no convention to be true.

- **It covers `--existing` too, so both modes carry one guarantee instead of the fresh-fork path carrying its own.** The recovery form attaches to a branch that already exists, and the same comparison confirms the tree handed back is on the branch the caller asked to attach to. Placing the check after the add/skip decision rather than beside either branch of it is what buys that, and is also why it precedes both `READY:` forms rather than being written twice.

- **The message names the leaf VARIABLE, not the last segment of the path, because in workspace mode those are different strings.** A repo inside a `.agents/workspace.json` workspace gets `$WORKTREE_HOME/<workspace>/<leaf>/<project>`, so the path's final segment is the *project* name — naming that would report the wrong string in exactly the multi-repo setup where a collision is hardest to read. The refusal names both branches, the shared leaf, why `git worktree add` was skipped, and `git worktree list` as the command that shows which leaves are already taken.

- **A `READY:` line is not by itself evidence that the branch was created — *Mechanics* now says so, and the ⛔ leaf-uniqueness bullet no longer reads as though remembering were the only defence.** That bullet ended by placing the whole burden on choosing the epic's leaf well at epic-cut time: the hardest moment to know every leaf a slice might later want, and one whose mistake only became detectable much later, in a different command. The advice stays, because a refused command is still friction worth avoiding; what changed is that forgetting now costs one refused command instead of a whole dispatch that looks correct. `README.md` carried the same claim — a colliding branch "is handed the epic's own tree instead of a new one, **with no error**" — and is corrected here rather than ledgered, because this is single-slice work.

- **Judged a bug fix inside the frozen helper contract rather than a change to it, and stated rather than assumed.** *The frozen helper contract* freezes arguments, env vars, stdout lines and exit codes, and this adds a non-zero exit to a case that previously exited 0. No argument, env var or stdout line changes; no exit code is added, renamed or repurposed — `1` already meant "refused", as it does for a missing base or an unknown branch. What moved is which inputs reach it, and the case that now fails was never a legitimate success: `READY:` asserts a worktree standing on the requested branch, and printing it for a branch that does not exist was a violation of the contract rather than an instance of it. The freeze exists to stop the two implementations disagreeing about what an input means; both ports change identically here, and `scripts/check.sh` still holds them to the same usage line and env-var set.

## 3.17.0

`execute`'s *Docs land at the end* tells a slice of an epic to record what it falsified into a ledger and not edit the doc, and one closing docs slice consumes the ledger at the end. That reasoning is about **prose describing an end state** and it is untouched here. What it does not survive is a repo whose gate mechanically validates **path citations**: a wave-2 slice deleted four files, four docs cited those files by path, and the gate went red at **step 1 of 6** — `format`, `check` and `test` never ran, so the slice got no signal about its own work at all. It could not pass its gate without editing a doc the rule forbade it to edit, and no ordering escapes that, because the closing docs slice is last by construction. Neither existing carve-out reaches it: this is a code slice in a multi-slice epic, exactly the case the rule is written for. A third carve-out now splits the rule on a testable line. [#101](https://github.com/trinity-ai-labs/orchestration-skills/issues/101).

- **The split is by what kind of thing the reference is, not by which slice is running — and the test is mechanical.** *If a checker can tell the reference is stale without reading the sentence, the slice that broke it fixes it.* A **structural coordinate** — a file path, or a route literal sitting in the same clause as one — passes that test because it names something the tree either has or does not, which is precisely why a gate can enforce it and a prose claim has to be read. **Prose describing behaviour still defers, ledger and all.** Keying on the slice would not have worked: every slice this fires on is a code slice in the middle of an epic, indistinguishable from the ones the deferral is for.

- **Repointing a coordinate is a rename, not a rewrite, which is the whole reason it is safe to devolve.** The deferral exists because four slices patching four sentences of one chapter produces four *semantic* conflicts, resolved by whoever has the least context. Two slices cannot hold conflicting accounts of which path a file now has, so that failure mode has nothing to attach to here — the conflict the rule defends against is not merely unlikely for coordinates, it is unavailable.

- **The boundary is stated as loudly as the carve-out, because it is what stops the carve-out swallowing the rule: repointing a coordinate is NOT licence to fix the paragraph around it.** The sentence containing the path stays in the ledger. A slice that "while I'm here" rewrites the section reintroduces exactly the semantic conflict the deferral prevents — and does it wearing a sanctioned edit, which is strictly worse than the original failure, since a diff that is *supposed* to touch that doc no longer looks wrong to a reviewer.

- **`decompose` Step 1 gains a second falsification question, and it earns its place by being GREPPABLE where the first is not: does this slice delete or move a file the docs cite by path?** That bullet already warns that a keyword grep cannot find the prose case — user-facing prose contains none of a change's new identifiers by construction — and that warning stands. A path citation is not prose: `grep -rF '<path>' <doc-set>` answers it exactly and completely. The two are complements rather than a restatement, and running the cheap one at grounding time turns a red gate into a line in the slice's `Owns`.

- **`Owns` now carries three dispositions where it carried two, because "in edit scope" and "out of it" stopped partitioning the question.** A doc named on an epic is a ledger entry, a coordinate fix, or both against the same file, and the decomposer writes down which. *The failure that prevents:* a doc listed with neither reads as whichever the implementer already expected, and both wrong readings are silent — read as ledger-only the slice cannot pass a path-citation gate, read as edit-scope it rewrites the paragraph the closing slice was going to.

- **Five sites beyond the two the issue names said the same thing, and all five moved — that is the copies-drift failure this repo cares most about.** The argument lives in exactly one place, `execute` → *Docs land at the end*, and the rest point at it rather than restating it. The dispatcher's **paste-verbatim docs block** and the **Implementer** section each carry enough to be actionable where they are read, because a dispatcher pasting the epic variant unchanged hands an implementer a brief that forbids the coordinate fix, and the Implementer paragraph is the one the implementer actually reads; `decompose`'s `Owns` field, its Step 1 falsification bullet, its `Owns` worked example, and `README.md`'s epic paragraph get a clause each. The "two carve-outs" count was itself a citation in three of those places and is now three everywhere.

- **The docs for this change are edited here rather than ledgered, and the irony is the rule working as written.** This is single-slice work, which is the second carve-out — so `README.md` is corrected in this PR. Derived from the behaviour changed, not from the new vocabulary: `orchestrate` states none of the docs rule and points at `execute` for the epic-branch mechanics; `review` and `setup` never mention it; `write-issue` carries the epic-branch trigger but not the docs deferral; `AGENTS.md` and `gate-queue.md` neither. Checked, unchanged.

## 3.16.1

Two corrections, unrelated to each other and sharing a release only because neither is big enough to deserve its own. Neither changes what the flow does, which is what makes this a patch rather than a minor. [#94](https://github.com/trinity-ai-labs/orchestration-skills/issues/94) is a rule whose instruction was right and whose stated reason was false wherever the integration branch is `main` — this repo and both sibling skills repos; it is corrected in `execute`, and then — as [#98](https://github.com/trinity-ai-labs/orchestration-skills/issues/98) — in the two other skills that carried the same universal in different words. [#97](https://github.com/trinity-ai-labs/orchestration-skills/issues/97) is a third rule binding a dispatcher that writes its own brief, already stated once in `decompose` and pointed at from nowhere a dispatcher reads.

- **`Closes #<n>` is not inert here "always" — the predicate is the PR's base against the repo's DEFAULT branch.** *Merge & cleanup* grounded its hand-close instruction on *"Every PR in this playbook targets an integration or epic branch, so that is every PR, always."* The word doing the damage is `always`: a slice PR based on an epic branch is genuinely inert, and so is one based on an integration branch that is not the default, but a project whose integration branch simply **is** `main` has nothing between a PR and the default branch unless an epic branch is standing there — so every PR that is not a slice of an epic bases on the default branch, and the keywords fire. The GitHub documentation quoted directly above that sentence already stated the real test and is untouched — only the sentence applying it to this flow overreached.

- **Observed three times in one session, and it cost nothing precisely because the instruction was right.** [#93](https://github.com/trinity-ai-labs/orchestration-skills/pull/93) targeted `main`, carried `Closes #89. Closes #86.` written under the skill's own assurance that such keywords were inert here, and both issues were already `CLOSED` when the dispatcher went to close them by hand; [#95](https://github.com/trinity-ai-labs/orchestration-skills/issues/95) went the same way through [#96](https://github.com/trinity-ai-labs/orchestration-skills/pull/96). Closing an already-closed issue by REST is a no-op, so **the hand-close is the one action correct under both readings** — a no-op when GitHub got there first, the whole mechanism when it did not. The release says that outright, because a reader who learns keywords sometimes fire will otherwise conclude the step is sometimes skippable.

- **The half that can actually bite is the inverse, and nothing in the corpus covered it.** Where the base **is** the default branch a closing keyword is live, and single-slice work never cuts an epic branch — so in a repo whose integration branch is `main` such a PR goes straight to the default branch and `Closes #<n>` fires the instant it merges. That is the right moment only if that one PR settles the whole issue, and **an increment is not an arc**: the loop grounds one horizon per cycle, so an arc can run several cycles whose increments are each a single slice, every one of them based on the default branch. `orchestrate` §7 treats the tracker as part of termination, so an issue closed early is a false "done" in exactly the artifact the loop reads back. What makes it worse than the failure it mirrors is the residue: a skipped hand-close leaves an **open** issue that any later sweep of what is outstanding can still find — that is how the nine in the existing failure clause were found — while a keyword that fired early leaves nothing to find at all.

- **A dispatcher writing a brief authors a field set, and the dispatch block now points at the check that binds it.** 3.15.0 put the reconcile-the-fields check in `decompose` Step 3's closer, for the slices `decompose` emits. A dispatcher entered directly — from a user asking it to execute one increment, or writing the brief itself on single-slice work — never opens that skill. The block already carrying merge-surface ordering and the bare-string rider *"even when you were invoked directly and never read that skill"* is exactly the seat for it, and it now names three rules across two skills rather than two in one, its lead sentence corrected on both counts.

- **Observed on this repo's own 3.16.0 arc, where it landed correctly on luck twice over.** That brief fenced `skills/write-issue/SKILL.md` as do-not-touch while its verify bar required `grep -rn "in parallel" skills/` to surface no surviving trigger claim — and that file carried exactly such a claim, so the bar could not come back clean while the fence held. The implementer said as much: the file *"would have survived the verify grep."* The fence happened to carry a carve-out and the implementer happened to exercise it and report; remove either and the brief has the three moves the closing check already enumerates, two of them silent. That is the **fence × verify bar** pair — first of the three — in a brief no `decompose` pass ever emitted.

- **It is a pointer, and it says which end of the problem it is.** No copy of the three pairs travels with it: the block it joins exists because those rules live in one place, and a second copy is what 3.15.0 was careful to avoid. It also distinguishes itself from the Implementer section's *A brief that contradicts ITSELF*, which is the **receiving** end and already points at `decompose` — one says do not emit the contradiction, the other says what to do when it arrives, and a reader meeting both should not read the second as having already handled the first.

- **`README.md` carried the same false universal and is corrected here rather than ledgered, because this is single-slice work.** Its close-out bullet said GitHub *"won't auto-close, since PRs merge into the integration branch, not `main`"* — falsified by this change itself, so not optional. It now states the default-branch predicate in both directions, including the stray-keyword hazard. `AGENTS.md` documents this repo's own conventions and mentions neither closing keywords nor the dispatch block — checked, unchanged.

- **The corrected rule lands in three skills, not one, because two others carried the same universal in different words — [#98](https://github.com/trinity-ai-labs/orchestration-skills/issues/98).** `orchestrate` §7 said *"a PR based on an integration or epic branch has its closing keywords ignored outright"* while already pointing at `execute`, which made it a pointer contradicting its own referent — and §7 is the rule that makes the tracker part of termination, the section the inverse hazard above cites, so the loop's own termination rule was teaching the reading that produces the false "done". `decompose`'s *Cross-reference, don't auto-close* said *"PRs merge into the integration branch, not `main`, so GitHub won't auto-close"*, with no pointer at all. Neither restates the predicate now: §7 says close the issues yourself because the hand-close is correct either way, `decompose` reduces its parenthetical to the pointer form that same bullet already uses one sentence earlier for the backlink rationale, and *Merge & cleanup* stays the only copy of the argument.

- **#94's scoping grep was structurally unable to find either, and that is the part worth keeping.** It concluded from `grep -rn "default branch" skills/` that nothing else made the claim — sound grep, wrong inference: **it searched the vocabulary of the CORRECT predicate, so it was blind to every copy phrased in the false one's.** Both restatements expressed the false universal without the words *default branch* — one said "an integration or epic branch", the other "not `main`". What found them was a search on the **behaviour** instead — closing keywords, auto-close, `Closes #`, "won't close", "ignored", issue-closing at merge — run across `skills/`, `README.md`, `AGENTS.md`, `bin/`, `scripts/` and `.github/`. There is no fourth site. It did surface one thing a scoping grep of either vocabulary would have missed: *Merge & cleanup*'s own lead-in still read *"GitHub closes nothing here on its own"*, three sentences ahead of the correction that contradicts it — the false universal surviving inside the very paragraph that replaced it, where a reader who stops at the opening sentence takes the old claim away and never reaches the fix. It now says the closing turns on where that PR was based, which is the question the rest of the paragraph answers.

## 3.16.0

`execute`'s Rule 2 cut an epic branch for multi-slice work **dispatched in parallel**, and the section immediately below it defended that qualifier as the thing keeping the rule narrow. What the qualifier left behind is an arc that answers both rules "no" and still wants the branch: four slices run one at a time, each individually shippable, cutting nothing — so the integration branch carries the arc half-finished for its whole duration, which is the exact condition an epic branch exists to prevent. Rule 2 is now multi-slice, full stop. [#95](https://github.com/trinity-ai-labs/orchestration-skills/issues/95).

- **Two of Rule 2's four costs really were artifacts of concurrency, and the section now concedes that instead of widening over it.** Base churn needs a sibling whose base can move, and there is none when one slice is live at a time; the integration gate signal needs a `^2` diff that comes back non-empty, and conflict-free sequential merges make it empty every time. Both are stated as zero for a sequentially-dispatched arc, in the same list that claims them. *The failure that concession prevents is a reader's:* someone who works out for themselves that base churn cannot happen sequentially, and finds the section silent about it, concludes the rule was widened past its own argument — and the next edit narrows it back to the qualifier this one removed. **The other three hold whatever the dispatch order is**: an arc that turns out wrong is still N merges to unpick, a seam whose halves land four slices apart is still a seam, and the fifth cost below lands identically either way.

- **The fifth cost is the one that carries the sequential case, and nothing in the corpus named it: in a project where shipped content must move a version, N slices merging into the integration branch is N releases for one arc.** Observed on the arc that shipped 3.15.0 here. `AGENTS.md` requires any PR touching shipped content to move `.claude-plugin/plugin.json`'s `version` and add a matching `CHANGELOG.md` heading, and `.github/workflows/ci.yml`'s `version-bump` job enforces it — on PRs whose base is `main`. That arc's three slices merging into `main` directly would each have needed their own bump, colliding on both files, and would have shipped three versions for one coherent change; converging on an epic branch made it one release, and the epic → `main` PR still faced the bump check in one shot. The `if:` condition that scopes that job was written knowing this — its own comment says the epic → main PR *"is itself a PR into main… so the whole arc's accumulated content is still checked in one shot"* — so the CI already assumed the topology while the rule that produces it was still keyed on fan-out.

- **Rule 1 survives with a changed job, and the change says what that job is where a reader meets the rule rather than two sections further down.** Once every multi-slice arc cuts a branch, Rule 1 stops deciding *whether* to cut and decides *which kind of branch you are holding*: a "yes" is an epic that is knowingly red until its last consumer migrates, which is what opens the `transient-red/<epic-slug>` window, and a "no" is isolation and nothing else. That distinction already has two downstream readers — *Mechanics*' **"Cut under Rule 1's primary trigger? Open the window in the same breath"** and *Transient-red window*'s **"Two things a detector must not key on instead"** — and both break if the rules collapse into one. *The failure this prevents is the tidy-up:* a rule that no longer decides the thing its section is about reads as dead weight, and deleting it takes the only thing that says whether the window opens. Nothing reports that, because a window that never opened and a project that has none are byte-for-byte indistinguishable.

- **The five-slice run the old paragraph cited survives and its meaning inverts.** It was there as proof that a count can never answer Rule 1 — independently shippable at every step, every merge green and coherent — and, by extension, that Rule 2's qualifier was doing real work. Half of that is untouched and still load-bearing: **neither rule is a slice count.** The other half now runs the opposite way. That run is the clearest case *for* Rule 2, not against it: it answered Rule 1 "no" and wanted the branch anyway, because the costs landed on the shared branch however green each intermediate state was. **What still bounds Rule 2 is its subject rather than its width** — one change decomposed into slices. A handful of unrelated one-slice fixes running side by side is branch traffic, not an arc, and cuts nothing; the adjacent *"still not 'the integration branch is busy'"* paragraph rests on exactly that and is unchanged. **And single-slice work still never cuts one**, sentence and reasoning untouched.

- **`decompose` moves with `execute` by giving its four-cost summary up rather than growing a fifth item.** The bullet restated the costs compactly for the decomposer; keeping it in step would have meant maintaining two accountings of the same five things, and this release is the exact moment that copy goes stale. It now names the trigger, points at `execute` → *Two rules reach for one* for the accounting, and says outright that it keeps no second copy — the division of labour it already had (`decompose` recommends, `execute` owns the mechanics and the argument) stated rather than duplicated. It also picks up why the first rule still gets answered on a multi-slice arc the second has already decided.

- **Two docs carried the qualifier and are corrected here rather than ledgered, because this is single-slice work.** `README.md`'s epic-branch paragraph restated the trigger as *"multi-slice work **dispatched in parallel**"* and enumerated the four costs; it now reaches for the branch on width alone, carries the release cost, and marks the two that need concurrency. `write-issue`'s epic-branch path asked "whether the slices will be **dispatched in parallel**" as the second of the two questions an issue must make answerable; it now asks whether the work is multi-slice at all. Both were falsified by the rule change itself, so neither was optional. `orchestrate` points at `execute` for the epic-branch decision and states none of it, and `review` and `setup` never mention the trigger — checked, unchanged.

## 3.15.0

Two defects from one epic, neither of which anything in this flow could have reported. [#89](https://github.com/trinity-ai-labs/orchestration-skills/issues/89) is a worktree whose dependencies fall behind the branch it holds while `git status` stays clean and the tree looks exactly right. [#86](https://github.com/trinity-ai-labs/orchestration-skills/issues/86) is a brief that contradicts itself in a way no gate can read, because no gate reads briefs. Both surfaced the same way — somebody looked at the thing and asked a question nothing was going to ask for them — and both are fixed by moving a rule out to the altitude its own reasoning already reached.

- **The epic worktree is the one tree in the flow that outlives the merges landing in it, which makes it the only one whose dependency tree can fall behind the branch it holds.** Every slice worktree is cut *after* the code it needs exists, so it installs correctly by construction and stays correct for the whole life of the slice. The epic worktree is cut at the start of the epic and then holds a branch that keeps moving under it — which is precisely what it is for. Its git state is kept current by the cadence tick; its `node_modules` was kept current by nothing, and no git-shaped rule in *Mechanics* covers dependencies. The failure is silent in every channel: `git status` clean, the tree holding exactly the branch it is supposed to be holding, nothing to read as wrong.

- **Observed: five files across two apps, and no code defect anywhere.** A slice created a workspace package, registered it in the workspace manifest and updated the lockfile; its PR merged into the epic branch while that tree's install, dating from when the tree was cut, still had no link for it. The integration gate run there came back `TS2307: Cannot find module`. A 1.2s `pnpm install --frozen-lockfile` turned it green. **The cost was never the install; it was arriving at it as a diagnosis** — five files, two apps, one module nobody can find reads as a slice repointing its imports at a path that does not resolve, which invites dispatching a fix agent against code that is already correct, or hunting a package-name typo that is not there. So the fix is two things: the install itself, and a *Mechanics* bullet making a module-resolution failure in that tree a stale install until proven otherwise, asked before anyone reads a line of the diff.

- **The install runs on the tick that already exists to keep this tree current, and it runs unconditionally.** `install` was already a config key, so this is the same command `setup-worktree.sh` ran when the tree was cut, and a project declaring none — this repo is one — has nothing to run, making the step a no-op rather than an error. One thing defers it, and it is a rule the section already states rather than a new exception: an install rewrites a tree's dependencies wholesale, which is a strictly larger mutation than the edits *A worktree with a ticket in flight is FROZEN* already bans, so a tree being gated waits for the verdict and installs on the next tick.

- **The issue's own proposed conditional was the trap, and it is the one that looks cheaper.** Keying the install to the cadence merge's own `ORIG_HEAD..HEAD` diff against the lockfile and workspace manifest misses the exact case that produced the report, because a new package does not arrive through that merge at all — it arrives through a **slice close-out**, whose `merge --ff-only` inside this same worktree sets `ORIG_HEAD` too. A conditional that actually held would have to compare the tree's *installed* state against the branch, which means an install stamp: a new config key plus both `setup-worktree` helpers changed under the frozen contract. Weigh the two failure modes rather than the two runtimes. The stamp's is a missed trigger, which silently reintroduces this bug and looks like nothing at all; the unconditional install's is 1.2 seconds on a tick already dominated by serialized gate runs.

- **`decompose` carried a reconciliation rule for exactly one pair of brief fields while the reasoning under it was general.** It lived inside the `Do NOT touch` bullet, written as a fence against a verify bar, and what it actually rested on holds for every pair: **nothing downstream reads any two fields of a brief against each other, and the decomposer is the only party who ever holds all of them.** So a slice could be internally unsatisfiable and still look finished from every other seat — the implementer meets one field by breaking another and reports the half it met, no gate can read a brief, and a diff afterwards cannot tell a requirement that was dropped from one that was never load-bearing. Every pair but the fenced one shipped unchecked.

- **Observed: a brief demanding a distinction the target had no vocabulary for.** One chapter of a shipped prose skill was required to say, where a rule differs by consumer, which consumer it is for — and, in a different section of the same brief, forbidden the pipeline's own planning vocabulary. No page in that corpus names its readers anywhere, so the words did not exist and satisfying the first requirement meant breaking the second. Neither field was a fence, so the one rule that existed could not reach it, and nothing else in the flow compares two requirements. **It surfaced only because the implementer chose to raise it** — it wrote the requirement situation-shaped instead and flagged the conflict in its hand-back. An implementer satisfying the literal instruction would have introduced reader-class vocabulary into shipped prose, and the style violation would have been the only visible symptom, in a repo where no gate can tell whether prose is correct.

- **Then it recurred one level up, which is why the fix is two skills rather than one.** The same requirement had been written into the issue's own `Verify` bar. The brief was corrected by hand; the issue was not — so a correct PR sat against a bar it deliberately did not meet, and the issue would have closed reading as unmet. The asymmetry is what makes the second copy worth its own rule: a brief is rewritten every cycle and a contradiction in one dies with it, while a filed bar is inherited by every slice grounded from that issue and re-read long after the run that would have caught it is over. `write-issue`'s `Verify` bullet now sends the author back over the body before filing, as the same pairs mapped onto an issue's own sections — the bar against **Constraints**, the bar against what **Approach** asks for, and any requirement in it against the vocabulary the target has.

- **The rule is stated as three enumerated pairs, not as a principle over "the field set".** That altitude was available and is the wrong one: a rule stated that broadly is a check-your-work instruction, and nothing distinguishes a decomposer who ran it from one who merely felt careful, so it costs nothing to claim and nothing to skip. Naming the pairs is what gives it the teeth the fence half already had. They are the fence against the verify bar; a content requirement against a style constraint; and **any requirement against the vocabulary the target actually has**, which is a satisfiability question rather than a style one and is invisible unless someone asks whether the distinction being demanded exists in the medium at all. Grounding is the last point at which the target was read, so the decomposer is the party positioned to ask. **And `Do NOT touch` keeps its two mechanical rules and its observed failure**, staying the worked example the closing check points at rather than restates — so no rule is stated twice.

- **The three doc corrections in this release are the falsification ledger this epic's slices wrote, answered against the final tree.** `install` stopped being a create-time-only key the moment the tick re-ran it, so a dispatcher reads it too. The slice that changed the meaning fixed the one place it owned and recorded the rest: `README.md`'s claim that the helper reads three keys and the skills read the rest, that file's config-table `install` row, and `skills/setup/SKILL.md`'s table row naming `setup-worktree.sh` alone. Each takes the phrasing already landed in `execute`'s *Per-project config* rather than a second wording.

## 3.14.3

`orchestrate` §2 closes with a sentence telling a one-increment arc to run one cycle — *"Ground, dispatch, close out — no umbrella, no rewrite, no reconcile against a plan with nothing left in it."* It enumerates what to **drop**, by name, and says nothing at all about what is **kept**; and its own first verb, "Ground", is ambiguous between *invoke `/pipeline:decompose`* — which is what Step 1, four paragraphs above, defines it as — and *understand the issue yourself*. Read from inside §2 by a dispatcher holding a single-slice issue, the trimmed path looks sanctioned and the ambiguity resolves toward the cheaper reading. What that drops is the only pass that requires the dispatcher's brief to be grounded, so the arc dispatches whatever the orchestrator happened to believe, as settled coordinates. [#87](https://github.com/trinity-ai-labs/orchestration-skills/issues/87).

- **The evidence is a single-slice arc that took that reading, skipped `decompose`, and wrote its own brief.** Into that brief went a claim copied out of a dependency's own generated output — a block stating that the package's version-matched docs may not resolve from a monorepo root — asserted as the local fact. It was false there: a hoisted package manager had put the dependency at the repo root, so the path resolved exactly where the brief said it would not.

- **It was caught on the last line of defence rather than the first.** The implementer checked, found the real layout, and wrote the doc around the verified fact — `execute`'s *Evidence that contradicts your brief is a finding you report* doing precisely its job. Had it complied instead, which is the cheaper and more usual move that rule exists to counteract, a false claim would have shipped into the file agents read to orient themselves, with a green gate on top, since no gate can test prose.

- **The two skills never disagreed; the load-bearing sentence was simply in the one that does not get opened.** `decompose`'s own description already covers this case — the pass applies "when the work is plainly a single slice — grounding and validation stand on their own, and a plan that needs no splitting can still be wrong". That text is unreachable from the point where the decision to skip it is made.

- **§2 now names grounding as the step a single-slice arc does not trim.** The droppable list is unchanged and gains the reason it never stated: umbrella, rewrite and reconcile are bookkeeping that carries state *between* cycles, and one cycle has none to carry. That is what closes the set, and it is also what keeps the amendment from reading as an invitation to put the ceremony back — the trim is now argued for, not merely permitted. "Ground" is bound to Step 1 as written, and the reason grounding stays is stated with it: splitting is the part a single slice does not need, while a one-slice plan can still be wrong in the one way that matters — a false premise in its brief, which nothing downstream re-checks.

- **`decompose`'s `Brief` bullet gains the claim class it did not reach.** *Point at the source, never at your conclusion about it* already covers a decomposer's own inference about this repo's code. A fact lifted from a dependency's documentation or generated output is a different shape: it arrives already written down, by an author describing the general case, while the brief asserts the local one. The bullet now calls such a claim an **input to grounding, not a grounded coordinate** — settle which branch of the vendor's hedge this repo is on before it goes in, or write it as the `Assumes X; flag if wrong` it actually is. It is the hardest of the three to catch, because it reads as the best kind of citation: sourced, quotable, and about somebody else's repo.

- **It extends the existing bullet rather than opening a new one, and nothing is restated in `execute`.** All three are one rule at three sizes — a claim that reads as settled fact and was never verified — and separating them across bullets or skills would leave a reader to work that out alone. Searching `skills/` for this claim class returns nothing else, so this fills a gap rather than adding a second copy of one.

## 3.14.2

`decompose`'s `Verify` bullet closed by telling a decomposer how to **phrase** a whole-package or whole-suite check — route it through the project's cached runner, never a raw `vitest`/`tsc`/`eslint` — and never said **who executes it**. That bar is pasted into the implementer's dispatch brief close to verbatim, where it lands beside `execute`'s *No full-suite or whole-package test runs — by ANY invocation* block and the handoff block saying the runner gates the PR after you enqueue. So the field as written produced a brief that contradicted itself: one statement telling the implementer to run the gate, two telling it never to. It stayed invisible because every part reads correctly on its own — the routing advice is true, the ban is true, the handoff is true — and the sentence that reconciles them is a parenthetical inside a section about build caching, in a different skill, that nothing in the `Verify` field points at. [#84](https://github.com/trinity-ai-labs/orchestration-skills/issues/84).

- **The evidence is one arc, two slices, and only one of them made a sound.** Both briefs carried a verify bar reading *"Run the FULL `pnpm gate` — NOT `--mode docs`"*, which meant the ticket's gate **mode** and reads as a command. Slice 2's implementer flagged the contradiction outright, reasoned that three of the four statements in front of it agreed the runner runs the gate, resolved it as being about the mode, and asked to be corrected if that was wrong. Slice 1's enqueued in default mode, did not run the gate, and never mentioned it — the right outcome with no signal attached. That asymmetry is what makes it worth a release: a decomposer reading slice 1's clean hand-back sees a run that went fine and no evidence the bar was ambiguous, so nothing pushes back on writing the next one the same way.

- **The branch neither implementer took is the expensive one.** An implementer that simply obeys a bar naming the gate runs it in the foreground — the machine saturation the slim machine-wide slot exists to prevent — and, per `execute`'s own note on backgrounding a banned run, that is the classic stall shape: the suite churns, the turn ends, the handoff never happens. None of that reports itself as a misread instruction.

- **The fix names the executor and says what the bar is actually doing.** A whole-package or whole-suite check named in `Verify` is the **runner's** to execute, never the implementer's; and when the bar names the gate it is fixing the ticket's **gate mode** — the full gate, or a project's lighter prose one — which `skills/execute/SKILL.md`'s *Gate mode* already establishes as a property of the ticket, set at enqueue. The field now says to write it that way (*gate in the default mode, not `--mode docs`*) rather than as a command line, and names *Gate mode* so a decomposer has the referent without going hunting through `execute` for it.

- **The single targeted test file was already the sanctioned exception in that same sentence; it is now the distinction the field turns on.** That is the one check an implementer may be told to run directly, so it is the one the bar can state as a command — everything wider belongs to somebody else. Making the two explicitly different is what stops the cached-runner routing rule from implying the implementer is the one routing, which is the inference that produced the defect.

- **The routing rule itself is unchanged.** Anything wider than one test file still goes through the project's cached runner and never a raw binary, because a direct-binary run bypasses the shared cache and goes cold. One clause is added: that is routing for **whoever runs it**, which on a default-mode slice is never the implementer.

- **`execute`'s parenthetical at *Go through the task-runner, never around it* stays exactly as it was, and that is the point rather than an omission.** It is correct where it sits — a routing rule stated in the section about routing — and it says as much in its own words: *"This is a routing rule, not a permission … so 'route it through turbo' never applies to them."* The defect was never that the reconciliation was wrong, only that it was unreachable from the field that needed it. Moving it would have put the same sentence in a second wrong place and left two copies to drift.

- **The field structure was deliberately left alone.** #84 floated that a gate mode reads oddly under a heading called `Verify`, and that relocating it in the slice's field set might be the cleaner fix. That is a bigger change than the finding warrants and it would re-open where every consumer of the breakdown reads the mode from. The gate mode stays in `Verify`; this release settles who executes it and nothing else.

- **Nothing else in the corpus assigns the gate to the wrong party — checked, not assumed.** `orchestrate` §8 already says "the single targeted test file an implementer is allowed to run", and its slice/shape-depth definitions name the verify bar as a field without describing its contents. `write-issue`'s `Verify` is an issue-level acceptance bar — *the gate/check that must pass* — which states a condition rather than assigning a runner, and nothing pastes it into a dispatch brief. `setup` already says of a queue-less project that "only who runs the gate changes". `review` §3 carries its own ban on full-suite runs. `README.md` lists the verify bar as a slice-depth field and no more. All unchanged.

## 3.14.1

`orchestrate` §7 requires the close-out to answer, in writing, whether the arc surfaced a defect or a gap in the pipeline itself — and then said *where* that answer is recorded is the project's business, offering three equal homes: a findings file a run appends to, a comment on the umbrella, or a line in the close-out report. Two sentences earlier the same paragraph says findings "die in the transcript when the session ends". A line in the close-out report **is** the transcript. So §7 sanctioned, as one of three options, exactly the death it exists to stop — and it did it invisibly, because both halves read as reasonable and neither is adjacent to the other.

- **The defect was produced by the arc that shipped 3.14.0, one message after that release closed, by an agent that had just rewritten §4.** [#81](https://github.com/trinity-ai-labs/orchestration-skills/issues/81) → [#82](https://github.com/trinity-ai-labs/orchestration-skills/pull/82) was a single standalone issue: no umbrella, and no findings file in this repo. So of §7's three homes, the two durable ones were both conditional on something that arc did not have, and the one that was always available was the one that evaporates — which is the sharp end of it, since that combination is not an edge case but the ordinary shape of a small arc. The close-out answer went into the terminal and nowhere else, and the dispositioning agent described it as "recorded, not filed", treating *record* as a disposition distinct from *file*. The maintainer caught it by reading the sentence; nothing in the flow would have.

- **The fix collapses record into file — there is no third bucket.** A finding that clears §7's bar becomes an artifact with a number: shipped, or filed and linked exactly as §4 files anything, carrying the reasoning and a recommendation rather than a fork. §7 reuses that machinery by **pointing at it** rather than stating a second, parallel disposition procedure, and it points specifically at §4's *"Filed" means filed* rider — two copies of one rule drift, and nothing marks which one is stale.

- **"None found" stays exactly as cheap as it was:** a sentence in the close-out, no artifact. The asymmetry is the point rather than an inconsistency to tidy up — nothing to file means nothing to file — and it is what keeps this a step with an output instead of a ceremony with a per-arc cost.

- **The bar does not move, and §7 now says so where the filing rule is, not only where the bar is.** The requirement is still an observed failure the finding can name, and the warning against manufacturing a rule per arc is untouched. This release decides only where a finding that has *already* cleared that bar goes: an item that cannot name an observed failure has not become a cheap issue to file, it has not become a finding at all. Stating that beside the new disposition is what stops "it gets filed now" from reading as "filing is the easy option".

- **The trap has a name in the text, because naming it is what makes it catchable.** A finding "recorded" in a report is indistinguishable, on the way out, from one that was filed: it reads as handled, satisfies the letter of "the question was answered in writing", and is gone when the turn ends. That is `execute`'s **a bullet in a report is not a follow-up** arriving one level up — the same shape, one altitude higher — and §7 points at that rule rather than restating it. What the close-out report is *for* is now stated positively too: it is where you say the question was asked and what the answer was, never where the finding itself lives.

- **`skills/execute/SKILL.md`'s pointer into §7 was verified rather than assumed, and holds unchanged.** Its close-out line hands the arc-level question to §7 and says outright that it carries no second copy — *"Read the bar and the 'none found' rule there; this file does not carry a second copy."* Both of those are exactly what this release left alone, and the new filing rule sits in the same section, so a dispatcher following that pointer picks the change up for free and the claim stays true.

## 3.14.0

`orchestrate` §4 dispositioned every reconcile finding with one test — *can the arc land without it?* — routing "forced" to fold and everything else to file, with an explicit prohibition on folding the rest. An arc can land without almost anything; that is what makes it an arc rather than a rewrite. So nearly every finding evaluated to "file", and the result was not a judgement, it was a default. [#81](https://github.com/trinity-ai-labs/orchestration-skills/issues/81) is what that produced over roughly a week of daily use: a steadily growing pile of issues, every one of them individually well-formed and correctly classified under the rule as written, and a user left to do the reasoning behind all of them. This release replaces the question §4 opens with.

- **Being right about every individual item is exactly what hid it.** Each filed issue was a correct application of the test, so no single cycle is where the rule looks wrong — the cost exists only in aggregate, and a loop only ever sees one cycle at a time. That is the same shape as the non-termination failure §4 already guarded against, arriving from the opposite side, and it is why nothing inside a cycle ever pushed back on filing.

- **The guard paragraph named one cost and defended only against that one, which is what made the rule one-directional.** It said absorbing everything means the arc never terminates — true, and worth keeping. Its mirror was nowhere in the section: an arc that absorbs nothing terminates cleanly every time and leaves the work in a queue for a person. (§4's existing "mirror failure" paragraph is about mis-filing something *forced*, a correctness risk to the arc — not about the cumulative cost of correctly-filed adjacent work.) §4 now names both, and says which way to lean when they conflict: **toward settling**, on an asymmetry of visibility rather than importance. An arc that keeps absorbing shows it — the plan never empties and the close-out recedes, with the loop itself watching that happen — while an arc that keeps filing closes clean every cycle and puts the whole cost outside the run. The failure you can see is the one you correct unprompted; the rule is for the one you cannot.

- **The new first question is the one the section never asked: _can I reason out an answer myself, or do I genuinely need the user?_** Three outcomes, and the first is the default and reads as the default — *settle it*, because the answer is usually a few greps away; *ask*, for a product or design decision the code cannot settle, which is the only class that reaches the user as a question and goes with the reasoning done; *file*, when the scope is genuinely an arc in its own right rather than an item. Only what survives that reaches the original test, which now decides **placement** and nothing else: fold as a named slice with its own wave, or track it as its own arc. **It stays judgement with a clear default rather than a prohibition in the other direction** — a rule that made folding automatic would fail identically from the opposite side, which is the failure being fixed, mirrored.

- **The objection a reader will have is that this breaks finiteness, and it is answered explicitly rather than promised away.** Settling is not absorbing. Settling produces a *decision* — an answer written into the plan with what it rests on, costing the cycle a few greps — and §4 says outright that it means answering the question, not writing the code, since §9 still forbids the loop an editor. Folding still produces a *slice*: named, waved, given a worktree, gated, reviewed, merged. That price is unchanged, which is what keeps a folding arc self-limiting, and nothing in the rewrite lets an item join a live slice or promotes anything past the horizon. What the first question adds is the case the old test had no vocabulary for at all — a finding whose entire content was a question the tree already answers, which could previously only be filed.

- **The worked case is the one that made the defect legible.** A planner instructed to emit something its own executor rejects: textbook "adjacent" under the old rule, since the arc shipped fine without it, so it was filed as a design fork for the user to settle. The actual answer was four greps away — the rejected shape had exactly one consumer, and that consumer generalised trivially. Nothing about the filed issue looked wrong, which is the whole problem: a fork is the cheapest thing to write and the most expensive thing to receive, and on the way out it is indistinguishable from genuine escalation.

- **The maintainer's addition — investigate before you disposition — is stated once, in §4, as the gate on all of the above.** *When something looks wrong, establish why it is the way it is before you disposition it. Trace it down to the thing that made it that way. If it has a valid reason and is idiomatic for its context, leave it and record that you checked; if it does not, then fix it, raise it, or file it.* It is the precondition of the new first question — you cannot answer *can I reason it out?* without going and looking — and it prevents a failure that is silent in both directions: "fix" a thing dispositioned on its appearance and you remove something load-bearing that looked odd for a reason; "file" it and you hand over an issue whose answer was one file away. **A thing that looks wrong and a thing that is wrong are indistinguishable until someone goes and looks**, and nothing in the flow forced the looking — §3 produces findings, not explanations, and every disposition reads perfectly well written on top of an unexamined one. §3's hand-off line into §4 now says so.

- **Three sibling skills point at §4 rather than carrying a second copy, and the one in question form was failing the same way.** `decompose` Step 2's escalation bar is correct as written and still fails in practice: in the same run, two questions were escalated that were each a few greps from an answer. Step 2 now binds a question to the bar §4 applies to an issue — **a question handed up is a deferral wearing different clothes** — because both hand a person reasoning the run was standing in the best possible place to do. `review`'s *What NOT to change* already stated the investigate rule for exactly one case, an existing suppression being the previous author's claim; it now generalises by pointing at §4, since the odd-looking guard, the redundant check and the narrower type fail the same way and this pass edits the working tree directly. `write-issue`'s *A follow-up from a live run is a first-class input* described what arrived under the old rule; it now describes what actually arrives — what survived the first question, with the reasoning already done — and its placeability paragraph says the disposition is re-run when the arc is next picked up, rather than only the fold-vs-file half.

- **`orchestrate` §6 gained the half it was missing: this loop has two channels to the user, not one.** Its decide-don't-ask bar policed *questions*, and most of what actually reaches a user from an arc arrives as a filed issue — so a loop that asks nothing all run and hands over nine issues at the close satisfied §6 to the letter while transferring more reasoning than one that asked twice.

- **`skills/execute/SKILL.md`'s follow-up-ownership rule was traced and deliberately left alone.** It looks like the same drifted rule — *a follow-up is yours until it concretely requires the user* — but it is scoped by "for each one you do not land in this PR", an implementer's fence exists for a reason, and filing genuinely is the correct discharge at that role, which has neither the arc's plan nor the authority to place work in it. It also already points at `decompose`'s Step 2 for how to ask, so it inherits that change for free, and it already carried the settle-first instinct in its own words: *an existing pattern, `AGENTS.md`, or a plainly obvious default settles most gaps — adopt it, state the assumption, move on.* `README.md`'s *An agent owns its follow-ups* hard rule was re-read against the rewrite for the same reason and holds unchanged: its subject is the roles whose discharge is filing, and its "not a bullet in a hand-back for you to triage" half is reinforced by this change rather than weakened.

## 3.13.0

Three close-out findings that `orchestrate` §7 produced and nobody landed. §7 mandates asking, at the end of every arc, whether the arc surfaced a defect or gap in the pipeline itself: [#72](https://github.com/trinity-ai-labs/orchestration-skills/issues/72) came out of that question during the 3.9.0 / 3.10.0 arc, [#71](https://github.com/trinity-ai-labs/orchestration-skills/issues/71) was fenced out of the #70 implementer's scope while it was writing invariant 9, and [#75](https://github.com/trinity-ai-labs/orchestration-skills/issues/75) is the 3.11.0 arc's answer to the same question. All three were written up properly and linked to what surfaced them — and then left open, #71 and #72 across three subsequent releases and #75 across two. That is what the question actually produced: the asking worked and the landing did not, and the honest reading is that filing is not shipping — a defect the pipeline finds in itself sits exactly as long as one nobody found. This release lands all three.

- **The line every implementer brief opens with named `pipeline:orchestrate`, the one skill whose first section forbids the work the brief then assigns.** *Dispatch* told the dispatcher to open every brief with "Step 0: invoke the `pipeline:orchestrate` skill and act as the IMPLEMENTER", and the very next sentence said the agent then reads the implementer playbook "from the *Implementer* section itself". `orchestrate` has no *Implementer* section — it opens by saying you are "a loop, not a builder" and closes with a hard boundary titled *No implementation code, ever*. Both reachable outcomes are silent. An agent that honours the skill hands back a decomposition of the slice it was told to build. An agent that honours the brief builds it having loaded none of the playbook — no scoped-check rule, no commit-last ordering, no push-draft-PR handoff, no never-self-merge — precisely because the dispatcher deliberately omitted all of that on the grounds that Step 0 was carrying it. The second is the dangerous one: it is indistinguishable from a correct dispatch.

- **It was correct when it was written, and 3.0.0's rename is what made it false.** Before that release `skills/orchestrate/SKILL.md` *was* the increment playbook and did hold the *Implementer* section, so the string named the right skill. 3.0.0 swapped the two skills' names wholesale — the arc loop took `orchestrate`, the increment took `execute`, and the files moved with them — and this string, being a quoted literal *inside* prose rather than a live reference, kept the old name and became an instruction pointing at the file it had just stopped describing. It then shipped in seven releases reading perfectly.

- **The fix landed in three places, not the one the issue named, and the third is the one a grep misses.** The Step 0 string itself; the sentence four lines later that quotes it back while explaining which five items Step 0 cannot carry; and the *Step 0 skills* bullet, which said "Invoke `orchestrate` (as implementer)" in a bare form that a search for `pipeline:orchestrate` never returns. The five remaining `pipeline:orchestrate` mentions in `skills/execute/SKILL.md` were then checked one at a time and every one genuinely means the arc loop: two in the frontmatter description, the DISPATCHER role-entry bullet, the "one increment is the unit" paragraph, and the Agent-tool permission paragraph that names both commands side by side.

- **And the fix carries a rule, because the wrong name was the symptom and not the cause: a section named apart from the skill that holds it is a claim nothing can check.** The corpus's ordinary form binds the two in one phrase — *`skills/orchestrate/SKILL.md` §5*, *`/pipeline:execute`'s Merging a shared hotspot* — so the reference carries its own referent and cannot disagree with anything. The Step 0 line named a skill to invoke in one sentence and *the* Implementer *section* in the next, leaving the binding implicit, and an implicit binding is one no reader and no check can test. That is precisely why the contradiction stayed invisible while sitting in the most-copied instruction in the file: it only reads as a contradiction to someone who already knows which file that section is in. Stated this way the rule also does not quietly condemn the seven existing skill-and-section pointers elsewhere in the corpus, which are bound in one phrase and are fine as they stand. *The evidence it is worth a rule: the error was caught twice by dispatchers who happened to be reading the surrounding section, and then passed straight through into two live implementer briefs by one who trusted the line — which is what a rule stated that plainly invites.*

- **Half of #71 had already shipped, in an arc that was not looking at it, and nothing said so.** The issue asked `setup` Step 4's verification to exercise both of invariant 9's surfaces. The `--status` half landed in 3.11.0 as part of [#73](https://github.com/trinity-ai-labs/orchestration-skills/issues/73)'s reconcile work — the same conclusion reached from a different direction — and #71 was never touched, so it kept reading as wholly outstanding while it was half done. What was actually left is the half the issue itself flagged as harder to verify cheaply: **a blocked acquire must name who holds the slot.**

- **That half is a read-the-output check, and the reason is not that automating it is hard — it is that there is nothing for an assertion to read.** A runner that waits silently and one that announces the holder exit the same way after the same elapsed time, which is the exact asymmetry invariant 9 exists to break, so a green exit records nothing about it. Step 4 now says: enqueue two tickets rather than one, start a second drain while the first is gating, and read *that* runner's output for the holder, what it is gating, and since when. Two things follow that are worth stating rather than leaving to be rediscovered. This is the one place in the whole flow where a second concurrent drain is the point rather than the mistake `skills/execute/SKILL.md`'s drain rider warns about — safe by construction under invariants 1–8, and the only posture the announcement is legible from at all, since it prints to the waiting runner's own stderr and an observer is by definition another process. And where the project's gate finishes too fast for two drains to overlap, the honest record is that the contention could not be produced: an overlap you never achieved is not a check that came back green.

- **`orchestrate` mandates exactly one artifact that fires later, and never held it to the rule the whole skill exists to enforce.** §2 requires a `ScheduleWakeup` divergence tick roughly every ten minutes between dispatch and merge, and is emphatic that arming it is part of dispatching rather than something you reach for once something looks wrong. What it never said is what the prompt may carry. A wakeup takes a prompt; the natural thing to write into it is the slice's scope and the next action; and both are a snapshot of the plan at arming time that nothing re-derives when it fires. That is §1's stale-coordinate failure inside the one artifact §9's *never ground beyond the horizon* could not reach — a tick prompt is grounding written for a moment strictly later than now, which is what that rule forbids everywhere else. §2 now carries the rider, stated as one test rather than a pair of lists: **handles, never conclusions.** Carry what you look a fact up *with* — the branch, the worktree path, the PR number. Re-derive what you looked *up* — the file list, whether a revision is outstanding, and above all the next action.

- **The issue proposed "identity is safe, plan state is not", and the fork-point SHA is the case that pulls the two apart.** A SHA does not change, so it reads as identity and is the one most likely to be carried; it is a conclusion all the same. `skills/execute/SKILL.md` already has you recompute the fork point once per tick after a fetch, because a branch that merged its base in during conflict resolution has moved its merge-base — and a carried SHA then anchors the diff behind that merge and reports the base's own commits as this agent's edits, which is the phantom-additions false alarm that file already documents, arriving through the tick prompt this time. That is why the rider is a test you apply rather than a list you match against: a list has to be complete to be safe, and this entry is the one a reasonable list leaves out. The rider closes by saying the same test governs any instruction the loop writes now and executes later, a resume prompt for backgrounded work included.

- **Both halves of one tick came back wrong in the same firing, which is what makes this structural rather than a lapse in prompt-writing.** On a single-slice change in the 3.11.0 arc, the tick was armed naming a three-file scope; two more files were folded into the live slice afterwards by a maintainer decision taken after arming; and when it fired, a divergence check run against the prompt's own scope would have flagged two legitimately in-scope files as divergence — the alarm §2 sends you to confirm against the fork point before a `TaskStop`, raised against an agent that had done nothing wrong. The next-action half was worse: "flip ready and merge" had been written expecting the next event to be a final diff, and the implementer had since been sent back for a revision. It recurred on a later arc with no fold-in behind it at all — a tick reading "read the diff, then merge" fired on a PR that arrived carrying a CHANGELOG paragraph describing a guard that had been removed during review, and executing the tick as written would have merged it; it was caught only because the dispatcher read the diff instead of obeying the instruction it had written to itself ten minutes earlier. That second instance is why the rule cannot be reduced to "don't fold work into a live slice": the fact that invalidated the tick arrived in the implementer's own diff. A prompt is written at the moment of most confidence about a slice and fires at a moment the loop has deliberately learned more, so any dispatcher writing a helpful, specific one reproduces this.

- **One thing fixed on the way past, in its own commit so it stays separable — and it turned out to be the same defect from the same commit.** `orchestrate` §9 was headed *What execute does NOT do*. That heading was correct before 3.0.0, when the arc loop lived in `skills/execute/SKILL.md`; the rename moved that file to `skills/orchestrate/SKILL.md` and the heading stayed, exactly as the Step 0 string did. Every other skill's equivalent section names its own skill (`What setup does NOT do`, `What decompose does NOT do`, `What write-issue does NOT do`), the bullets underneath are unambiguously about the loop — one says a rule is "restated here because the loop is where the temptation actually lands" — and #75's new rider cites §9 by number, so a reader following that citation landed on a heading claiming the section describes a different file. Shipping the fix for #72 while leaving it would have been a contradiction inside one release.

## 3.12.1

Two things were **filed as issues during #76 rather than fixed** — correctly, since neither belonged in that arc's scope — and they are landed here instead. [#77](https://github.com/trinity-ai-labs/orchestration-skills/issues/77) is a real hazard in all four PowerShell helpers, confirmed by repro rather than by reading. [#78](https://github.com/trinity-ai-labs/orchestration-skills/issues/78) is a gap in what the close-out section says about itself. Filing them was the right call at the time; leaving them filed would not have been, because the first is a break that only ever appears on somebody else's machine.

- **The four `.ps1` helpers asserted a property of PowerShell in a comment and depended on it in the code, and neither the assertion nor the dependency was written down as a choice.** Each sets `$ErrorActionPreference = 'Stop'` and then hand-checks `$LASTEXITCODE` after every `git` / `gh` call — 27 of them across the four, all reached through probes like `Test-GitSuccess` — under a comment stating that Windows PowerShell 5.1 "does not fold a native command's exit code into `$ErrorActionPreference`." That sentence is true about 5.1 and **silent about 7.x**, which is the half that matters: PowerShell 7.4 added `$PSNativeCommandUseErrorActionPreference`, and where it is `$true` a non-zero native exit under `'Stop'` becomes a terminating error. These helpers use non-zero exits as *questions* throughout — `git show-ref --verify` asking whether a branch exists, `merge --ff-only` asking whether a fast-forward is possible, `gh pr view` on a PR that may not be there — so with it on the probe itself is fatal and the check written to read the answer never runs.

- **The issue's framing was checked before the fix was written, and half of it was wrong.** "The default since 7.4" was filed explicitly as unverified, and it does not hold: the shipped default is `$false` in 7.4 and still `$false` in 7.6, confirmed against `pwsh` 7.6.4 and against the 7.6 `about_Preference_Variables` table. So there is no PowerShell release in which these helpers are broken out of the box, and had the version default been the only route in, the honest answer would have been "not a problem." **The route in is inheritance, not the default.** A preference variable is scoped to the session that set it *and every child scope*, and a script invoked from a session runs in one — so a user whose profile sets it `$true` has it in force inside all four helpers. That is not a hypothetical shape for this repo: `setup-worktree.ps1` and `setup-workspace.ps1` already carry comments handling `Set-StrictMode 2.0` on exactly the same reasoning — "which a user's PowerShell profile can switch on globally, so this script cannot assume it is off."

- **Reproduced directly rather than reasoned about, in both directions.** A script setting `$ErrorActionPreference = 'Stop'` and calling `git show-ref --verify --quiet refs/heads/definitely-not-a-branch` inside a function reaches its `$LASTEXITCODE` check and completes normally when the caller leaves the preference alone; invoked from a session that set it `$true`, the same script dies at the `git` line with `NativeCommandExitException` and exits 1, with the check never reached. Adding `$PSNativeCommandUseErrorActionPreference = $false` at script scope restores the first behaviour under the second condition, and the value is visible as `False` from inside the function — confirming a script-scope preference governs native calls in the functions below it, which is the property the fix depends on and the one worth checking rather than assuming.

- **The fix is a declaration, and the reason it is a declaration is the whole point.** Every helper now sets `$PSNativeCommandUseErrorActionPreference = $false` on the line after `$ErrorActionPreference = 'Stop'`. This is correct on every version including the ones predating the variable, where the assignment is simply an unread variable — verified inert even under `Set-StrictMode -Version Latest`, which constrains *reading* an unassigned variable and never assigning any name. It is also what Microsoft's own documentation recommends for this exact situation: its worked example disables the preference around a native command whose non-zero exit is information, then checks `$LASTEXITCODE` by hand.

- **PSScriptAnalyzer was checked rather than assumed, because the fix has the shape the analyzer flags.** `PSUseDeclaredVarsMoreThanAssignments` reports variables assigned and never read in scope, and a preference variable set for its side effect is exactly that. It does **not** fire: the rule carries a known-preference-variable allowlist that already covers this one. Confirmed by running the analyzer over a file that assigns the preference and never reads it alongside an ordinary variable that does the same — only the ordinary one is reported. So no suppression was needed, which matters because the repo ships exactly one sanctioned suppression and `AGENTS.md` is explicit that it is not precedent.

- **The four comments are corrected, and that correction is the durable half of this change.** They now say what the *script sets* instead of what PowerShell *does*: that the native-command switch is separate from `$ErrorActionPreference`, that 5.1 has no such switch while 7.4 added one a caller's session can turn on, and that pinning it off is what keeps the `$LASTEXITCODE` checks below reachable. A comment asserting today's environment default as a guarantee is the thing that goes stale silently when the default moves — nothing at the code shows it has, and the code that depended on the guarantee keeps reading as correct. `AGENTS.md` gains the general form as a convention: a `.ps1` helper declares the session state it relies on rather than inheriting it, since PSScriptAnalyzer does not model session state and CI exercises one `pwsh` configuration, so a break of this kind reaches the machine whose profile differs and nowhere earlier. **The frozen helper contract does not move** — no argument, environment variable, `READY:`/`HEAD:` line or exit code changes, and the preference is an internal script setting rather than a contract variable, so it is deliberately not added to the parity check's contract env list.

- **The epic → integration PR is the one PR in the flow with no implementer behind it, and *The epic branch*'s close-out described it as "like any other."** It is like any other **at merge time** and unlike any other in **authorship**: nothing is built on the epic branch directly, so there is no draft hand-back to promote — the orchestrator authors the PR and then merges it, and `merge-pr.sh` flips it ready one line above `gh pr merge`, putting the reviewer's signature and the author's hand on the same PR. The close-out bullet now names that, and names what stands in for the missing hand-back: every slice was already reviewed as its own draft PR, and the epic tip has just been through *Gate the integrated whole*, which where the merges produced a tree no per-slice gate has run is the one signal those gates structurally could not give. *The failure this prevents: a reader holding Hard rules' "a non-draft PR that isn't being merged right now is a bug" meets the one PR where the draft never had a hand-back to be, with nothing to say whether this is the rule's exception or a defect — and the two call for opposite moves, undoing a legitimate flip mid-close-out or learning to read the rule as advisory.* The smaller of the two options in the issue was taken deliberately: a required re-read of the full `integration...epic` diff before opening was the alternative, and this repo's bar for a new rule is an observed failure, which that one does not have. `README.md`'s *Where the review approval lives* gains the same one-sentence exception, since that paragraph's whole job is to say who signs a PR and it was answering the question with one case missing.

## 3.12.0

The epic branch lived in the main checkout, and the same file that put it there carried the warning saying why it should not have: the main checkout is **the one piece of shared mutable state** in a flow that is otherwise isolated per worktree. Both statements were in `skills/execute/SKILL.md`, roughly ninety lines apart. This release resolves them in favour of the warning. One rule replaces the special case — **the main checkout holds the integration branch and nothing else, ever; every other branch, slice *or* epic, lives in its own worktree** — and the epic branch stops having mechanics of its own and becomes a slice-shaped thing one level up, cut with the same command, torn down by the same helper. Nothing in an epic's life switches the main checkout or parks a branch in it.

- **The old mechanics capped you at one epic at a time, structurally, while the rules directly above them made an epic branch the DEFAULT.** One checkout has one HEAD, and *Mechanics* said "worktrees are only ever for slice work — never cut one for the epic branch." Rule 2, four paragraphs earlier, says multi-slice work dispatched in parallel *defaults* to an epic branch — which is the shape this flow reaches for most often. So the flow reached for epic branches constantly while the mechanics permitted exactly one to exist. Nothing reported that ceiling, because it is not an error condition: the second epic simply has nowhere to put its branch, and whoever hits it improvises.

- **Parking it in trunk produced a silent wrong verification, which is the failure that actually forced this.** Another session moved the main checkout off the epic branch while a close-out gate ran. The gate itself was fine. The verification that read it then ran against a different release branch entirely and reported an absurd count of schema blocks and a missing schema command. **Nothing errored** — the commands all succeeded, against the wrong tree. It was caught only because the numbers were absurd; with a subtler change the same sequence reports a green "verified" against a tree nobody meant to look at, and the epic closes on it. The two steps that must agree — gate this, then verify this — were separated by a checkout any other session could move in between, which is precisely the hazard the ⚠️ in *Worktree creation* already named and which *Mechanics* then mandated walking into.

- **The tell was that the safe workaround was the skill's own recovery tool.** What people actually reached for was `setup-worktree.sh --existing <epic-branch>`: a worktree on the epic branch — the exact thing the text forbade. When a skill's documented recovery path is the right answer to a problem the skill created, the recovery path was the design. It is now simply `setup-worktree.sh <epic-branch> <integration-branch>`, which creates the branch and its tree in one command and never touches the main checkout. The old three-command switch/push/switch-back ceremony is **deleted rather than relocated**, along with the whole explanation of why the last command was not optional: you cannot forget to step off a branch you never stepped onto. The "push it" half stays, because it is still exactly true — without `origin/<epic-branch>` there is nothing for invariant 2 to compare a slice worktree's `HEAD:` against and no base for `gh pr create --base` to open against.

- **A fifth failure, found while designing the fix and never written down anywhere before: the mandatory mitigation for the epic's one real cost was itself a shared-state hazard.** The every-tick cadence ran `git -C <main-checkout> merge origin/<integration>` while trunk was standing on the epic branch. When that merge conflicts — which is *exactly* the case the tick exists to surface early — it leaves the **shared** checkout wedged mid-merge, conflict markers in the tree and `MERGE_HEAD` set. From that moment every other session's `pull --ff-only` fails, in a checkout none of them touched, for a reason none of them can see from their own worktree. So the thing bounding the epic's deferred conflicts was itself capable of blocking every unrelated session on the box. In a worktree you own, the same conflict is yours alone and blocks nobody — and the cadence now carries the rule that you resolve it before you walk away, rather than leaving it half-merged for the next tick to trip over.

- **Three failure modes the new design INTRODUCES, each written as its own rule, because a design that only lists what it fixes is half a design.** They are: the epic branch's leaf must be unique across the whole epic; the epic worktree is a merge point and a gate target rather than a workspace, so nobody codes in it and it never goes dirty; and a conflicted tick is resolved before you walk away from it. The middle one has teeth it did not have before — a dirty epic worktree now blocks both the tick's fast-forward *and* every slice close-out's sync, and both surface as a failure in somebody else's slice naming a tree that slice never touched.

- **The leaf collision is the one worth reading twice, because invariant 2 is structurally blind to it.** The worktree path is `$WORKTREE_HOME/<project>/<branch-leaf>`, so `epic/checkout-flow` and a slice `feat/checkout-flow` resolve to the same directory. This was impossible while the epic had no worktree and is live from the moment it does. Verified against the helper rather than reasoned about: `setup-worktree.sh` asks only whether a worktree is already **registered at that path**, finds the epic's, prints `worktree already exists: <path>` and **skips `git worktree add` entirely** — so the slice branch is never created. It then symlinks the env files, runs the install, and prints `READY: <path> (branch feat/checkout-flow off epic/checkout-flow)` and a `HEAD:` line, identical in shape to a success. And invariant 2 — compare the printed `HEAD:` against `git rev-parse origin/<base>` — **passes**, because the tree you were handed is standing on `<base>` itself, so the two shas match. The one check built to catch a wrong tree cannot see this wrong tree, because it is the base's own. The next thing that could catch it is the implementer's first action, one whole dispatch later, and only if the implementer performs it.

- **`merge-pr.sh` and `merge-pr.ps1` gain a third sync path, replacing a `die` that would otherwise have fired on every slice PR of every epic.** Step 4 previously chose between two paths — the main checkout is standing on the base, or move the base's ref — and *refused* the third case, because `git branch -f` will not move a branch any worktree is standing on. Under the new layout that third case is no longer an anomaly worth reporting; it is the ordinary shape, since an epic branch now always has a worktree and every slice of that epic PRs into it. So a base checked out in a linked worktree is now fast-forwarded **inside that worktree**, in the same retry loop as the other two, with the same `MERGE_OID` ancestry check and the same local-equals-remote verification deciding success — the message is never proof, the ref-equality check is. `--ff-only` cannot discard a commit, so the new path is inherently as safe as the on-base one.

- **The new path does not inherit the weakness of the path it sits beside.** Today the on-base path swallows its own failure with `|| true` and dies six attempts later with a generic message, while only the `branch -f` path produces a good `diverged` refusal. Rather than propagate that, the divergence check was hoisted so it covers **both** off-base paths — it is the precise, ref-based instrument for one of the two ways a sync can be blocked, and answering it first is what lets the worktree path attribute its own failure to the other cause instead of guessing. A fast-forward that then fails inside the worktree names **which** worktree, states that divergence was already ruled out, and prints git's own message, which names the files. Both real causes were exercised against a live repo: `Your local changes to the following files would be overwritten by merge` for a dirty tree, and `You have not concluded your merge (MERGE_HEAD exists)` for an unfinished one — the second being the wedged state the old trunk-side cadence used to create in a checkout everybody shares. And the worktree holding the base is re-resolved **every round** rather than once before the loop. `git merge` names no branch — it moves whatever the tree it runs in has checked out — so a resolution cached ahead of a loop that sleeps between attempts is a fact that can go stale while being relied on. The obvious remedy is a fourth guard mirroring `assert_checkout_unmoved` onto the linked worktree; re-resolving is both smaller and stronger, because it **removes** the staleness rather than detecting it, and it turns both ways the answer can change into self-healing rather than fatal — a worktree removed mid-run, or switched to another branch, simply stops holding the base, and the sync falls through to moving the ref, which is what is correct for that state. That is why there is no such guard in either sibling to go looking for.

- **The self-healing case is preserved and now has a comment saying so.** If an epic's worktree has been torn down while the branch still exists, nothing is standing on the branch, the lookup answers empty, and the sync falls back to moving the ref exactly as before. That is also the state a completed epic passes through, since the epic → integration PR has the **epic branch as its head branch** — which is the other thing that got simpler: `merge-pr.sh` on that PR tears the epic worktree down and `--delete-branch` removes the branch, exactly as for a slice, so the only thing a close-out now leaves behind by hand is the `transient-red/<epic-slug>` marker.

- **One claim elsewhere in the file was falsified by this change and is corrected rather than left standing.** *Mechanics* said an epic branch's **name** is read by nothing — no helper in `bin/`, no step in the playbook. That is now false in one specific way: `setup-worktree.sh` derives the worktree directory from the branch's leaf. The claim splits. The **prefix** is still read by nothing, which is what the transient-red detector's "no branch-name prefix is ever the key" argument actually rests on, and that argument is unchanged. The **leaf** now carries a collision constraint — not a naming contract, since no leaf means anything; one just has to be unlike the others.

## 3.11.0

3.10.0 gave the queue a way to be **asked**. This release is two changes with one root cause — **information that exists, is correct, and is not where the question is being asked from.** In the dispatcher's case it is a contention announcement that fires perfectly into a process the observer is not reading. In setup's case it is a reference that gains invariants no already-scaffolded project is ever told about ([#73](https://github.com/trinity-ai-labs/orchestration-skills/issues/73)). Neither one errors, which is why both had to be found by hand.

- **Invariant 9's announcement half works — it fired, correctly, and the dispatcher was blind anyway, because process-local output cannot answer a cross-process question.** Verified in a real implementation: the slot takes an `onWait` callback, the runner passes it, and `slot held by pid …` lines appear across four separate drain outputs. The grace period is implemented too. So the tempting read — that the waiting runner was silent — is simply wrong. It announced, at the exact moment the fact was true, onto **its own stderr** — the stderr of a backgrounded process nobody had open. A dispatcher deciding "should I start another drain?" is not the process holding the answer, and nothing carries it across. That is the evidence for a claim the reference already asserts and could previously only argue: the read-only mode is the load-bearing half rather than a convenience, because a per-process announcement cannot serve an observer who is by definition another process — leaving that dispatcher the one move the announcement was added to prevent. The reference now says so as the *reason* the requirement exists rather than as a new invariant.

- **A dispatcher read ~26 minutes of elapsed runner time as a stall, and the process list is structurally incapable of telling that apart from a healthy claim.** `pgrep gate-runner`, or an elapsed clock read off it, answers "is a process there" — never the question being asked, because a runner gating a ticket and a runner holding a claim while blocked on another session's slot are the same live process with the same growing elapsed time. The claim was held behind another session's gate. The natural corrective action for a stall is to start another drain, so the misread does not stop at a wrong belief; it routes straight into the incident 3.10.0 was written about. `skills/execute/SKILL.md`'s drain rider gains one bullet, placed as the successor to the concurrent-drain one — that bullet routes a state question to `--status`, and this one is the edge of what that answer covers: **"is the queue contended?" and "is this gate advancing?" are different questions, and `--status` answers only the first.**

- **And the obvious remedy breaks: `drain --status` on a runner that predates invariant 9 does not error — it runs a full drain.** Observed on a real project: an operator ran `--status`, got `drained 0 ticket(s)`, and caught it only because the output *shape* was wrong. Nothing failed, and nothing would have — an unrecognised flag falls straight through to the ordinary drain pass. The mechanism is the scaffolding model itself: `/pipeline:setup` authors this design **into** a project and the project owns it from there, so a project scaffolded before invariant 9 has a runner that never implemented the mode, and there is nothing in the flag to say which kind you are holding. The effect inverts 3.10.0 on exactly the installs most likely to still be running an old runner — the rule added to stop a dispatcher reaching for an unwanted drain is what fires one, while it is trying not to. So `skills/setup/references/gate-queue.md` invariant 9 gains the obligation on the *other* side of the flag: **a runner that does not implement `--status` must reject the unrecognised flag and exit without claiming anything.** A `--status` that drains is not a degraded answer; it is the precise opposite of the one guarantee the flag was added for, and rejecting unknown flags is what makes the absence of the mode legible instead of silent.

- **`--status` answers ownership; nothing in the queue answers movement — and the counts get read as one number when they are two facts.** `queue: 0` says nothing is starved; `processing: N` across N distinct pid suffixes says every claim is owned. The claim suffix is the ticket-to-process join key, which is the direction `ps` cannot give you. What no queue state records is how far along a claimed ticket's gate is, so for that you look inside that ticket's worktree at the gate's own children — the build and test processes it spawned. Counts plus live children yields a real answer ("step 3 of 6, in `<package>`") where an elapsed clock yields a guess. The queue directory is the portable form of the same read, and portable precisely because every transition in this design is a filesystem rename by construction: `ls` over `<queue-root>/<project>/queue` and `/processing` gives the same counts and the same pid suffixes, claims nothing, and contends for nothing.

- **The skill already applied this instrument to the other actor, and the drain is the one place the lesson was never carried.** The stalled-implementer test in *Dispatch in the background, then monitor for divergence* is the worktree byte-for-byte unchanged across two consecutive ticks with **no live children** — read the work, not the clock. Same instrument, other half of the fleet. That is why this ships as one bullet inside an existing rider rather than as a new section: the rule was already in the file, pointed at implementers, and the runner was simply never held to it.

- **Same root cause one level up: `/pipeline:setup` had no way to be entered by a project that already has a queue, so a scaffolded runner never learns the spec moved** ([#73](https://github.com/trinity-ai-labs/orchestration-skills/issues/73)). Every trigger in the skill's description was an **absence** — no `.agents/worktree.json`, a helper warning "no config", a dispatcher refusing an unconfigured project — and none on the **presence** of a queue scaffolded against an older reference. Step 3's verb was `scaffold` with no branch for "they already exist, compare them", and Step 4's queue check was a happy-path round-trip that never invoked the read-only mode, so it returned green on a runner missing it. A re-run therefore did not merely fail to close the gap; it handed back a successful verification report. Step 3 gains a reconcile branch that **reports a per-invariant delta and rewrites nothing** — two projects' queues *should* be allowed to diverge, and an overwrite cannot tell a deliberate divergence from a stale one, so it destroys both. Step 4 now exercises `drain --status` and confirms it claims nothing, and the description gains its one presence trigger.

- **The delta is read from the code rather than from a version stamp, and the reason is the part that looks over-engineered until you see it: an invariant can be partly already true on the day it ships.** The reference is written from implementations that already exist — it says so in its first paragraph — so an invariant added to it is often mostly a description of what projects already do, with one genuinely new clause inside. Invariant 9 is the worked example: the announcement half was already implemented in a real project before the invariant was written, the `--status` half was new. A stamp comparison answers "current" or "behind" and both are wrong here — bumped on partial adoption it says current, left alone it says behind on an invariant nine-tenths satisfied — and neither names the one missing clause, which is the only part that ever caused drift. It is also exactly what a human re-read misses: a maintainer recognises their own runner in most of invariant 9 and concludes they are current. That closes the version-stamp open question in #73 by dropping it.

## 3.10.0

The gate queue's eight invariants are all about **correctness**, and they hold. Not one of them is about what a drain *says*, and that is the gap this release closes.

- **Three drains stacked in one session, because a wrong model of what a drain is survives contact with one.** Observed: a dispatcher backgrounded `gate:drain` three separate times across a session without ever checking whether one was already running, holding the model that a drain is **PR-scoped** — one per PR — where it is in fact queue-scoped, looping until the queue is empty and then exiting. Nothing any of those drains emitted could contradict that model. The mechanism is that claiming a ticket and acquiring the slot are two separate steps (invariants 1 and 3), so a second runner claims a ticket immediately and *then* blocks on the slot — and from outside, "blocked on another session's slot" and "gating a ticket" are indistinguishable, with both processes exiting at a time bearing no relation to when they started. The corrective action for a queue that looks stuck is to start another drain, which is indistinguishable from the mistake; the dispatcher began checking for a live runner only after it had stacked three. `skills/setup/references/gate-queue.md` gains a ninth invariant, **Contention is announced**: on a failed slot acquire the runner prints who holds it, what that holder is gating and since when, then waits — and a read-only **`--status`** mode reports queue depth, in-flight claims and the slot holder while claiming nothing and gating nothing. Neither records anything new, which is part of the argument that this is not a correctness change: the PID is in the slot, `processing/`'s claim suffix maps it to the ticket that runner is gating — the extraction invariant 2 already performs — and the slot's own timestamp, the one invariant 4's grace period reads, says since when.

- **`--status` is the load-bearing half, because the absence of a way to *ask* is what made the wrong reach the only reach.** With no read-only mode, the only instrument for interrogating a queue is another runner — one that claims tickets and contends for the slot, which is the one thing an observer wanted not to do. A dispatcher that wants to know the state rather than work it therefore has no move but the wrong one. `skills/execute/SKILL.md`'s drain rider now points at it: **one drain per tick**, riding the divergence timer, never a second on top of one already running, and `drain --status` when the question is about state rather than work.

- **A refusal would have been a regression, and it is the obvious fix, so the invariant says why it is wrong rather than leaving it to be re-derived.** Two reasons. It breaks the cross-session case — the queue is machine-wide, so a runner that refuses while the slot is held refuses because *another session* happens to be mid-gate, and that session's ticket then waits on a timer it can neither see nor control; concurrent drains from different dispatchers are a property this flow relies on, and they rely on it precisely here. And a refusal is a decision taken off a PID liveness check, in exactly the window invariant 4 already documents that check as wrong: a wrong refusal **drops a drain**, failing closed on the one operation whose whole job is not dropping things, where a wrong wait costs a sleeping process.

- **The one thing this is not is a race patch, and the release states the evidence rather than implying it.** The slot is correct: claims are atomic renames and the slot holder is handed down per-spawn rather than exported into the session (invariant 5), so three stacked runners could neither double-gate a ticket nor inherit each other's slot — and a real ledger of **399 gated tickets carries zero overlapping gate windows**, meaning two gates have never run concurrently on that box. Nothing was damaged and nothing was at risk. Invariant 9 is about **observability, not correctness**: invariants 1–8 already make concurrent drains safe, and this one exists because safe and legible are different properties. It also states the fact the incident turned on — **more drains never add throughput**: one pass loops until the queue is empty (invariant 6), so a second runner gates tickets the first would have gated at the same wall-clock moment. Concurrency changes *which process* does the work, never *when the work finishes*.

- **`execute`'s concurrent-drain bullet answered a question nobody was asking.** It said a second drain is *safe* — true, and load-bearing for the cross-session case — and got read as saying a second drain is *useful*. The rewrite keeps the safety guarantee and closes the inference by naming both questions and giving them their different answers, and points at invariant 9 for the mechanism rather than restating it.

## 3.9.0

Three changes and one argument: the pipeline mandated **writing** the structure of an umbrella arc and nowhere mandated **looking for** it.

- **An agent that arrives at a sub-issue reads it as a standalone issue, because nothing ever told it to check for a frame.** `decompose` Step 1 mandated reading *deep* — `gh issue view <N> --comments`, because constraints live in comments — and said nothing about reading *up*; `orchestrate` Step 1 had the same hole. Observed: an agent hunting a classification question found a child issue through a keyword search, read it alone, and concluded the decision it contained was unowned — while the umbrella that framed it held the wave structure that answered the question, and had come back in that same agent's own earlier search as a title in a result list it never opened. It spent an hour re-deriving decisions that were already filed. The mechanism is that **a child issue reads as complete**: nothing is missing, nothing errors, and its own body gives no sign it was written inside a frame, so the whole cost is paid in re-derivation that is indistinguishable from original work — to the agent doing it and to anyone reading the result. `decompose` Step 1 now carries the authority and `orchestrate` Step 1 a pointer to it, per the same one-authority-plus-pointer convention the two files already state in their own voice.

- **The check is two steps because the cheap one is not always available, and the fallback returns a candidate set rather than an answer.** `gh api …/issues/<N>/parent` is definitive and free when the umbrella used native sub-issue links, and 404s `No parent issue found` otherwise. The timeline fallback finds a markdown-only umbrella that `/parent` structurally cannot see — and it returns **every** issue and PR that ever cross-referenced the child, with the umbrella among them and in no way distinguished. Measured against this repo's own arc, child #41's timeline yields `37 42 43 45 46 48 49 50`: eight numbers, one umbrella, seven PRs and siblings. Shipping that command without a rule for picking would have handed an agent eight numbers and no way to choose, so the rule carries the disambiguator — **the umbrella is the candidate whose own body lists this issue in its tracked checklist**, an anchored `grep -qE` over `- [ ] #<N>` that isolates exactly #37 from those eight. The pattern uses `([^0-9]|$)` rather than `\b` for BSD/GNU grep portability. That checklist line is the artifact `decompose`'s own *Umbrella + sub-issues* step already mandates writing, which is why both halves of this release belong together: the reader-side check consumes the writer-side artifact.

- **The backlink was a bare bullet in a mechanics appendix, sitting between two neighbours that both carry a failure rider.** In this repo the rider is the marker of a rule that has been paid for, so a bare mechanic reads as formatting and is what gets dropped under pressure — observed as a real umbrella shipping with six children and **zero** backlinks, leaving the arc one-directional and markdown-only, which is precisely the input condition for the failure above. `write-issue` Step 4's *Umbrella linking* is now the authority: the checklist and the backlink are the two directions of one link, and **only the backlink is readable from the child**. `decompose`'s *Cross-reference, don't auto-close* points at it rather than restating the argument.

- **`Link them as native sub-issues where available` read as optional, and the hedge was the part that got read.** Observed `sub_issues` length **0** on two separate umbrellas in the wild and **0** on this repo's own #37. The relationship is a plain REST endpoint, so the hedge described no real limitation; it is now unhedged and *GitHub write mechanics* carries the exact call. **The trap worth writing down is the id**: `sub_issue_id` takes the child's **database id** (`.id`, e.g. `5261102081`), not its issue number (`38`) — different fields on the same object, both plain integers — and passing the number returns a bare `404 Not Found`, which reads as *"this endpoint doesn't exist"* rather than *"wrong id"*, so the natural conclusion is that the API is unavailable and the whole step gets quietly skipped. That is exactly how an umbrella ends up markdown-only, which closes the loop: `/parent` 404s on every child of #37 *because* #37 is markdown-only, forcing an arriving agent onto the eight-candidate fallback. The two rules are one mechanism seen from each end, and the release says so rather than leaving it to be noticed.

- **A second trap on the same call, found by running it: `-F` there is doing type work, not `@file` expansion.** The section's existing rule pairs `-F` with `@file` and keeps genuine literals on `-f` — and an id is a literal, so `-f` is the natural reach. But `-f` is `--raw-field` and sends `"38"` as a string, which the endpoint rejects with `422 Invalid property /sub_issue_id: "38" is not of type integer`; only `-F` types a bare number as a JSON integer. So both flags fail on a wrong id, with two different errors, and neither says *use the other flag*. Written down beside the id trap because the surrounding rule actively points the wrong way here.

## 3.8.0

- **An agent running `/pipeline:orchestrate` read the harness's "do not call the Agent tool unless the user requested it" guard as still in force, and nearly concluded that `/pipeline:decompose` Step 1's grounding was unavailable to it.** The corpus said, per pass, WHICH agents each pass uses; nothing anywhere said the guard was *answered*. So the two rules met inside one run with no stated relation, and the harness default is the one an agent arrives already holding — which makes the failure quiet and total: a dispatcher that reads the guard as standing over the run writes every slice itself, which is exactly the wrong-role failure `execute`'s opening section already names, reached through a rule that was never about this flow. `skills/execute/SKILL.md` → *First: which role are you?* now states the invariant where the role decision is already made: a user who typed `/pipeline:orchestrate` or `/pipeline:execute` has asked for precisely the fan-out that guard exists to prevent an agent doing unasked, because dispatching implementers into worktrees is what the skill IS. The tell is stated as a mechanical one rather than a judgement — a reading that makes the command the user just invoked inoperable is the wrong reading.

- **What the invocation authorizes is bounded, and the bound is a pointer rather than a roster.** It authorizes exactly the sub-agents the pass you are in declares it uses, no more, with each pass the sole authority on its own answer. The canonical statement deliberately carries no per-pass table: every pass already declares its own answer, and a second copy here would drift with nothing to mark which side is stale. Four short riders say only that the authorization applies where sub-agents are actually spawned, and point back for the reasoning — `decompose`'s Step 1 `Explore` bullet and its `No dispatch, no merge.` boundary, and the matching pair in `write-issue`.

- **`review`'s outright ban was deliberately left untouched, and the invariant is worded so it cannot reach into it.** `skills/review/SKILL.md` §1 forbids sub-agents unconditionally, argued from an observed incident: forked reviewers inherited an implementer's brief, read its imperatives as their instructions, and committed, pushed, opened the PRs and enqueued the gate tickets across three consecutive slices before the implementer got its turn back. A rider there would have reframed an absolute correctness rule as an exception to a general permission — weakening the strongest ban in the corpus to accommodate a rule about a different failure. So the canonical statement handles it by wording instead: **a pass that declares none is not an exception to this rule — it is this rule returning its answer**, and where a pass declares none, the invocation authorizes none.

## 3.7.1

- **`merge-pr` tore down the worktree it was running inside, was killed by its own teardown, and left a transcript that read like a success.** The close-out hands the head branch's worktree to `remove-worktree`, which kills every process rooted in that tree *before* removing it — found by `lsof` (open descriptors **and** cwd) and by `pgrep` over the argv. Both of those find the close-out itself whenever it is running out of, or standing in, the tree it is about to delete. Observed during a real release, and reproduced against the unfixed helpers in a sandbox repo with a stubbed `gh`: `merge-pr.sh` invoked by path from inside its own worktree checkout printed `found 1 process(es) to terminate: … merge-pr.sh 7`, SIGTERMed it, and went on to remove the tree — worktree gone, PR still `OPEN`, head branch still there, the ready flag never flipped, and the last lines of the run were the orphaned teardown reporting `done — … removed`, because the child outlives the parent that would have reported the failure. Re-running the main checkout's copy completed the merge.

- **The `cwd` half is reachable from the sanctioned bare invocation, which is why the guard is two signals and not one.** The obvious vector is the argv scan finding a script path inside the doomed tree, and that one only fires when a helper is run *by path*. The scan's other method matches on **cwd**, and reproducing it that way killed the installed copy resolved off `PATH` — `merge-pr.sh <n>` run bare while standing in the PR's own worktree dies exactly as the by-path form does. So both helpers refuse on either signal: the running script's own resolved directory, and the process's working directory, each compared against the tree about to be removed. Neither has a legitimate case behind it — on macOS and Linux the scan kills such a run, and under Git Bash, where neither `lsof` nor `pgrep` exists, the same overlap makes Windows fail the removal on a file the run itself has locked. Both comparisons are string prefixes rather than a process scan, so the guard holds identically on the platform where the scan is degraded.

- **A guard that kills is worse when it is eager, so the match is anchored and the one false positive is excluded by name.** Containment is anchored on the separator, exactly as the kill scan anchors its own prefix, so a run standing in `…/1322-foo-retry` does not refuse a teardown of `…/1322-foo` (verified in both shells). Paths are compared in physical form — `/tmp` **is** a symlink to `/private/tmp` on macOS, and two spellings of one directory compare unequal, which is a guard that silently never fires. And `merge-pr` reads the doomed tree from `git worktree list` rather than re-deriving a layout, then **excludes the main checkout**: the teardown only ever removes a linked worktree, so a head branch that happens to be checked out in the main checkout is not a tree the run will delete, and refusing there would block a close-out launched from the one directory that is always safe.

- **Refusal, not a re-exec from a safe directory.** Re-execing has to guess *which* copy to run — the installed one may be a different version, or absent entirely on the machine of someone whose checkout **is** the plugin repo — and silently running code the caller did not name is a poor trade on the one helper whose whole job is to be the final step. A `cd` alone would not fix the by-path case either, since the script file still sits in the tree being deleted. The refusal exits non-zero with the reason and the recovery on stderr, and the frozen helper contract does not move: no argument, environment variable, `READY:` line or exit code changes.

- **Both shells, exercised rather than mirrored by eye.** `bin/merge-pr.ps1` and `bin/remove-worktree.ps1` carry the same two signals, the same anchoring and the same exit code, and the pair was run through the same three shapes — by path from inside the tree, bare off `PATH` with cwd inside it, and bare off `PATH` from the main checkout — in a sandbox repo with a stubbed `gh`. Both refuse the first two with exit 1 and an untouched worktree and PR, and both complete the third: teardown, merge, and the local base branch synced to the merged tip. On Windows the failure the guard prevents wears a different face — the OS refuses to delete a directory a process is standing in — but the pair is one contract implemented twice, and a command that refuses in one shell while proceeding in the other is exactly the drift the contract exists to prevent.

- **`remove-worktree.sh`'s `die` printed `\n` verbatim.** It was built on `echo`, where bash prints those two characters literally, so its one multi-line message ("not inside a git repo …") arrived as a single line with a `\n` in the middle — while the PowerShell sibling's newline is a real newline, so the two said different things for the same failure. It now uses the `printf '%b'` form `merge-pr.sh` already had, which is what lets the new refusals carry their recovery instructions on their own lines.

## 3.7.0

Two findings from running this pipeline against a real tree for a session, and both are about the same seam: the `Verify` bar is prose, and everything around it is a file list.

- **A `Do NOT touch` can forbid the verify bar the same brief demands, and `decompose` never read the two fields against each other.** Observed: a slice's brief fenced off a directory, correctly — the slice's whole hazard was that it might widen a security boundary defined in there rather than land inside it — while the same brief's verify bar required a test asserting that boundary still covered what the slice produced, and the natural home for that test was the fenced directory's own test file. Neither instruction is wrong alone; the contradiction exists only at that one file, and the decomposer is the only party holding both fields. An implementer meeting it alone has **three moves and two are silent**: skip the test and the bar evaporates with nothing marking the part that was dropped; breach the fence and it does the exact thing the fence existed to prevent; park the assertion elsewhere — the move that looks most like compliance and can be worth nothing, since an assertion placed outside the module it is about is exactly as green as one placed correctly. Nothing errors in any of the three, and a diff read afterwards cannot tell a verify bar that was dropped from one that was never load-bearing. So the `Do NOT touch` field gains the reconcile, stated as two mechanical rules rather than as a judgement: **a fence covers behaviour and a directory glob does not imply the tests under it** unless the fence says so in words, and **whenever a verify bar asserts something about a module the slice does not own, the fence says which way that module's tests go.** The default is what makes the residual case safe — a fence that forgot to mention tests resolves to "not fenced" instead of handing the implementer the three-way choice — and where the assertion genuinely belongs to the fenced module *and* the slice must not own it, that is a seam to name, not a fence to soften. `execute` carries the implementer's half in one paragraph: a brief that contradicts itself is the same class of finding as a brief the code contradicts, and it is a report rather than a call to make, because an implementer may neither widen its own fence nor quietly narrow its own bar. The same paragraph closes the loop the new default opens: an implementer taking a fenced directory's test file under it has to say so in its hand-back, since a diff landing inside a fenced directory is precisely what the dispatcher's divergence tick watches for and the tick cannot read the reasoning off the paths.

- **A verify bar can be satisfied by a test that re-implements the mechanism instead of calling it, and the reversal rule does not catch it.** Observed: a slice was told to prove a boundary held, with the property named explicitly — match the way the real consumer matches, not by string equality or prefix — and wrote a faithful-looking local reimplementation of the consumer's matcher out of a general-purpose library. The real one's semantics differed and it also preprocessed its input in a layer above the matcher, so the test passed on a boundary that was not there; two later slices extended that suite, both green, before one replaced the stand-in with the consumer's own exported functions and the hole appeared immediately. The bar named the **property** and left the **instrument** to the implementer, and a reimplementation is indistinguishable from the real thing until the day their semantics differ — which is precisely the day the test was supposed to fire. So `decompose`'s `Verify` field now names the instrument alongside the property whenever the bar asserts agreement with a consumer the slice does not own — a library, a compiler, a runtime matcher, a separate process — and the instrument is **the entry point the production caller actually reaches**. That phrasing is load-bearing, because it cuts both ways: calling the consumer's own matcher directly, below the preprocessing its callers go through, reports coverage the real pipeline does not provide, so a stand-in can invent a boundary as easily as hide one. A stand-in stays legal where the slice names it as one and justifies it; what is banned is the silent default.

- **The existing "name the reversal" text is extended rather than joined by a sibling, because the producer-side reversal structurally cannot reach this.** Breaking the *producer* reds a test whose *consumer* is a stand-in exactly as it reds a correctly-wired one — it proves the test is attached to the producer and says nothing whatever about the other end, which is the sharp part of the finding and the reason a rule that was already there did not fire. The bar names a **consumer-side** reversal too: break, stub or swap the consumer and confirm the test goes red.

- **A reviewer's correction is a claim to be verified like any other — and this one is not a hypothetical rider.** Observed on the audit of the fix above: the dispatcher doubted the corrected comment, called the consumer's matcher itself to check, and told the implementer the comment was false. It was not — the preprocessing step sat a layer above what the dispatcher had called — and the implementer pushed back with the call path and was right. The same error, in the opposite role, twenty minutes apart, which is the strongest evidence available that "call the real consumer" cannot be left to either party's judgement. `execute`'s PR review loop gains it where the dispatcher is already told to go read the shared dependency itself: read it **at the level production reaches it**, because a spot check that calls a module directly is evidence about that module and not about the pipeline, and a correction sent on that basis carries a reviewer's authority while being the same defect it is reviewing. The implementer's side is one sentence on the paragraph that already tells it to overturn a false brief: answer with the call path, and where the correction is right, say so and fix it.

## 3.6.0

- **The close-out now has to answer, in writing, whether the arc surfaced a defect in the pipeline itself.** Noticing was never the failure. Running this flow at volume against a real tree turns up gaps in it constantly, and none of them are found by going looking — they arrive mid-work, get noted in passing, and die in the transcript when the session ends, because by the time the arc closes the work is done and the finding is somebody's memory. Observed across five sessions: the only findings that ever shipped were the ones noticed while the session still happened to be running. So it is a **step with an output** rather than a rule to remember — a rule fires only if it is recalled at the end of a long session, which is precisely when it is not, while a missing answer is visible. **"None found" is valid and cheap; writing it is the obligation**, which is the same device §3 uses on the reconcile checklist: an arc that surfaced nothing and an arc where nobody asked look identical afterwards, and the second is the common case. The bar stays an observed failure the finding can name — this is not a prompt to invent a rule per arc, and a close-out that manufactures one is worse than a close-out that never asks, because the skills fill with entries nobody reads and the real findings stop being visible among them. Where the answer is recorded is the project's business; the skill requires only that it be asked and answered.

- **It is asked per ARC, and `execute` points at it so a directly-invoked dispatcher is bound too.** Per-increment is the wrong cadence — asked five times in an evening the question becomes noise and gets tuned out, which is worse than not asking at all — so the rule lives in `orchestrate` §7 where termination is owned. But a dispatcher entered directly at `execute` never reads that skill, and its close-out is where the arc actually ends, so the close-out sequence carries a pointer to it, in the same shape as the two dispatch-time rules already pointed at rather than restated.

## 3.5.0

- **`Closes #N` has never closed anything in this flow, and the rule that said so was filed where nobody taking the default path would read it.** The fact was already in `execute` — as step 4 of the *by-hand* close-out sequence, the fallback for when `merge-pr.sh` cannot handle a case. The default path is the helper, whose paragraph said it does all four steps atomically, so an agent doing the ordinary thing never reached the step that says GitHub will not close the issue. Observed: nine issues, every one genuinely complete, every one named with `Closes #<n>` in a PR that merged clean, all nine still open at close-out — found by accident because somebody happened to list what was outstanding. Closing them is now a named fifth step in the helper paragraph itself, with the by-hand sequence pointing at it instead of carrying a second copy. **The mechanism is also corrected**: the docs say keywords in a PR description "are interpreted only when the pull request targets the repository's **default** branch … If the pull request targets **any other branch**, then these keywords are ignored, no links are created, and merging the PR has no effect", so they do not fire *later* either — nothing re-evaluates them when the integration branch merges. The keyword that does fire later is one in a **commit message**, when that commit reaches the default branch. And the close command is REST (`gh api -X PATCH … -f state=closed`) rather than `gh issue close`, for the reason `decompose`'s *GitHub write mechanics* already gives: the high-level `gh issue` writes go through GraphQL and hit rate limits exactly when you are closing a batch. `orchestrate` §7 gains the one-level-up form — an arc is not terminated while the issues it resolved are open.

- **An implementer that contradicts its brief with evidence has done the job, not deviated from it — and `decompose` now writes briefs that can be contradicted.** The `Assumes X (existing pattern in <file>); flag if wrong` convention only ever covered what a dispatcher *labelled* as an assumption; the damage comes from what a brief states as settled fact, which an implementer does not challenge. Observed twice in one run, caught both times only because those briefs happened to send the slice to the source: a brief called a hand-written parameter type known-good and out of scope, where the handler actually parsed the whole body with one discriminated union and the type was narrower than what the route accepts; and a brief grouped three fields as "declared `unknown`, so the builder asserts the narrowing" when one was already precisely typed and its cast was covering a readonly-to-mutable variance mismatch — following it literally would have pushed a cast into two real callers, the exact trap that brief warned about a paragraph earlier. So a `Brief` points at the source (*read the route handler and use the schema it parses the body with*) rather than at the dispatcher's conclusion about it (*use `FooRequest`*), which is unfalsifiable from inside the slice; and `execute` states outright that following the code and reporting the contradiction is the job. Without that said, the incentive runs the other way: contradicting the brief reads as scope creep and costs a paragraph to defend, while implementing the assertion is cheap and comes back green — so the dispatcher's error becomes the implementer's output with a gate's endorsement on top.

- **A partial fix has to leave a residual you can count.** Observed: a body builder returning `Record<string, unknown>` — the whole request body unchecked — was replaced with the real wire type plus three named casts on the fields whose local type was genuinely `unknown`. Strictly better and not finished, and what made it a stopping point rather than a fudge is that the leftover changed *shape*: one blanket escape hatch became three specific assertions that can be grepped, counted and filed. It was filed that night and closed a run later. So the residual is a named cast, a listed exception carrying its reason, or a ledger entry — never a widened type or a general suppression that merely happens to be smaller than the last one. A partial fix with an implicit residual is indistinguishable from a complete one, so nobody ever finishes it, and the next reader cannot tell what was decided from what was tolerated. It sits beside the fix-here-or-file floor in `execute`, cross-referenced to `decompose`'s scaffolding-and-teardown rule as the same instinct at planning scale — a ratchet being a countable residual with a slice already booked to drive it to zero.

## 3.4.0

- **A comment that asserts what other code does is the one defect class nothing in this pipeline can scan for, so rewriting one now means checking it.** Every class the checks drain is a disagreement between two artifacts a tool can compare — a duplicate, an unimported export, a type that disagrees with its schema. Prose asserting a code path is not that, and the only thing that finds it is a person reading the claim and going to look. Observed: a schema module's doc block described a two-step validation the route performed; the route did only step one and never had done step two, and tracing every consumer showed the refined schema was never parsed in production at all — an invariant declared twice, refined twice, and never once executed, with a comment describing its enforcement in working code. It surfaced by accident, from a slice rewriting that block for an unrelated naming fix that checked the claim first. So a comment your change rewrites or relocates is a claim you are re-asserting: open the code it describes before you reword the sentence, and treat a false claim as a **finding** to report and file, never as a wording bug to correct. The tidy rewrite is the worst available outcome — it documents a bug as the design and removes the last thing that could have led anyone to look. This sits beside the existing convention on how a comment is written rather than replacing it: that one governs the wording (present-tense mechanism, no archaeology), this one governs whether the wording is true, and a comment can satisfy the first perfectly while describing a function call that does not exist.

- **A diffstat measures nothing in a repo whose unit of change is a paragraph.** The divergence check already said a diffstat tells you *where* an agent is editing and not whether the approach is right; it now also says lines-changed carries no signal about *magnitude* here, in either direction. Observed on this repo's own PRs: a reviewer read a 29-line diff carrying six rules as suspiciously thin — about five lines per rule — in a file where a single bullet runs to 400 words. The density was right and the arithmetic was measuring line wrapping. The misread runs the other way just as easily, and that direction is the one that merges: a large diffstat reads as thorough and buys a pass on actually reading the diff.

## 3.3.0

- **Named stashes are a real git primitive, so parked work is now a named ref of the agent's own and the shared stack is the fallback.** 3.2.0 policed `refs/stash` — a marker in the message, a re-match by that marker, an explicit ref on every pop — and all of it is a workaround for a stack that is repo-global and addressed by *position*: `stash@{n}` renumbers whenever any worktree anywhere pushes or drops, and git's `WIP on <branch>` subject names no owner. Git already ships the primitive that removes the class rather than policing it. `git stash create` "create[s] a stash entry (which is a regular commit object) and return[s] its object name, **without storing it anywhere in the ref namespace**", and `git stash apply` documents that its argument "may be any commit that looks like a commit created by `stash push` or `stash create`" — so an agent parks with `create` + `git update-ref refs/pipeline-stash/<branch-leaf>/<epoch>` and restores by that name with `git stash apply --index`. Nothing renumbers it, no other tree's `push`, `pop`, `drop` or `clear` can reach it, work that outlives a turn stays attributable to whoever parked it, and one `git for-each-ref` lists every tree's parked work. The order is the safety property: the ref exists before the tree is cleared, because `create` saves and does not clear.

- **The never-blind-pop rule keeps its full weight as the fallback.** A human's stashes live on `refs/stash` and so does anything an older agent left, so the stack never becomes ignorable: every `pop`, `apply` and `drop` against it still carries an explicit ref re-matched one command earlier, and the argument-less `git stash pop` is still banned outright — including, especially, when `git stash list` shows exactly one entry, which is the weakest evidence there is because the list is repo-wide and the gap between the two commands is when a sibling tree pushes.

- **`refs/worktree/*` is the namespace that looks like the better answer and is a silent data-loss trap — the rule names it so nobody "improves" the recipe into it.** It is genuinely per-worktree, and git's own `git worktree` docs say those refs "are not shared", which is exactly why it reads as purpose-built for this. The same property is what breaks it: a gc keeps what is reachable from the refs the *invoking* worktree can see, and a sibling's per-worktree refs are precisely the ones it cannot. Verified on git 2.50.1 — a linked worktree's `refs/worktree/park` is absent from the main checkout's `git rev-list --all` while present in its own, so **`git gc --prune=now` run in the main checkout deletes the object**, leaving the ref resolving to nothing, `git fsck` reporting `invalid sha1 pointer`, and the next repack dying on `fatal: bad object`. A ref in a global namespace is in every tree's root set and survives untouched. Worth writing down because the ref still resolves afterwards, so the symptom presents as a broken `apply` rather than as a collected object and the debugging goes somewhere else entirely.

- **The objection to the global namespace is answered in place: a shared namespace is not a shared stack.** `refs/pipeline-stash/*` is repo-wide exactly as `refs/stash` is, and it was never the directory that was dangerous — it was positional addressing plus a `pop` that takes whatever is on top. A ref named `<branch-leaf>/<epoch>` has neither. The costs are stated too, rather than left to be found: parked work under a custom ref is invisible to `git stash list`/`show`/`drop` and therefore to every IDE stash panel, which read the same `refs/stash` reflog; and it is more durable than a stack entry, because a ref outside `refs/heads`/`refs/remotes`/`refs/notes` gets no reflog and no expiry clock, where a buried stack entry lives on `gc.reflogExpireUnreachable` (30 days). Durable-and-unsweepable is the reason the tick sweeps the namespace: refs nobody owns accumulate in silence.

- **Two caveats stated rather than left to be discovered.** `git stash create` captures tracked changes only and **silently accepts and ignores `-u`** — it returns a commit with no untracked parent where `git stash push -u` produces one, and the docs give `create` no options at all — so anything new that must travel with the change gets staged first. And `git stash pop` refuses a named ref (`error: '<ref>' is not a stash reference`): it is stack-only, so the restore is apply-then-`git update-ref -d`, which also fails safe, since a conflicted apply leaves the ref intact and the work still parked.

- **The dispatcher's tick now sweeps both namespaces.** The sweep grepped `git stash list` for the marker, which would have gone quietly empty the moment agents stopped using the stack — a check still running, still green, and no longer looking at where the work is. It enumerates `refs/pipeline-stash` first, as the authoritative call, and greps the stack second for anything that reached it anyway.

## 3.2.0

- **The blind `git stash pop` kept happening, so the argument-less form is now banned outright rather than left to follow from the reasoning.** The rule was complete — a repo-global stack, a marker per agent, a re-match by marker before every touch — and it was still being broken occasionally, which is a placement problem rather than a content one: `git stash pop` is one of the most automatic commands there is, it *is* correct in a single-checkout repo, so the instinct arrives already justified and never reaches the paragraph that would have stopped it. `execute`'s stash rule now leads with the mechanical form — every `pop`, `apply` and `drop` carries an explicit ref re-matched one command earlier — and names the case that gets rationalized: a `git stash list` showing exactly one entry, which is the weakest evidence on the list, because that list is repo-wide and the gap between your two commands is precisely when a sibling worktree pushes onto the stack. The costs are stated beside it, because they are not comparable: re-matching is one `git stash list | grep`, and a wrong pop takes a human's WIP out of the tree they left it in and drops it from the stack, recoverable only through `git fsck --unreachable` and only until the next gc.

- **The ban is now stated at the two places an agent actually reaches for a stash**, since a rule at the bottom of *Hard rules* is not where anyone is standing when the urge arrives. Holding a whole slice uncommitted for the review pass is one — the temptation is a clean tree for a check, and stashing there is fine; getting it back blind is not. Resolving a merge conflict is the other and the worse one: it is reached at exactly the point where several worktrees are guaranteed to be live and pushing onto the same stack, so the entry on top when you come back is the least likely to be yours of any moment in the run.

## 3.1.0

Eight rules, every one of them found by running this pipeline rather than by reading it — three days on a large TypeScript monorepo, 134 merges, 464 commits, four multi-slice epics. Where a bullet says *Observed*, that is what it means.

- **A slice's scope had two dimensions and needed a third, so `decompose` grew a `Derives` field.** `Owns` and `Do NOT touch` are both file lists, and between them they cover exactly the collisions git can show you. Neither has any concept of a file whose correct contents are a function of the **whole tree** rather than of any one slice's files — a ratchet ledger, a backlog JSON a checker regenerates, a generated type, an unimported-exports manifest. Two slices edit disjoint sources, each is individually correct, and the shared artifact is right on neither branch; it merges clean, because there was never a textual conflict to stop on. Observed: a slice converting an exported `interface` into an inferred type removed an anchor a duplicate-detector scans for, while a sibling slice was simultaneously changing that detector's rules and owned its backlog ledger — both green in isolation, neither merge conflicting, the ledger correct on neither branch. A slice now declares what it derives, may not treat a derived artifact as final, and reports what its change did to it; the **dispatcher re-derives it on the merged tip before gating** (`execute` → *Gate the integrated whole*), which is the one thing per-slice gates structurally could not have covered. `orchestrate` §5's shared *runtime string* is cross-referenced rather than restated — that one is a shared **value** and a question of wave placement, this one is a derived **file** and a question of slice scope.

- **`orchestrate`'s reconcile checklist interrogated the plan five times and the tree not once.** Every item asked whether the plan still held against the merged tree — coordinates, senses, forced work, assumptions, deferrals. A sixth item now asks whether the *tree's own derived state* still holds, re-derived with the project's regenerator rather than read and reasoned about. The failure the other five structurally miss: nothing moved, nothing was renamed, nothing was falsified, nothing was deferred — and the ledger on the merged tip is wrong anyway, in the one file the next increment reads as ground truth, so the error is inherited by every slice grounded after it instead of surfacing.

- **`Owns` accepted a predicate as though it were a scope.** The field admits both a named list (`src/a.ts`, `src/b.ts`) and a predicate (*every caller of `<helper>`*), and a predicate's file set does not exist at decomposition time — it is discovered by the implementer, in its worktree, after dispatch. So there is nothing to check a sibling's named list against, and `Do NOT touch` cannot help, because it would have to name an intersection nobody computed. Observed: one slice got a named list, a sibling got "every caller of `<helper>` — 40 references, find them, they are yours", and both edited the same file; neither diverged, both were exactly in-brief, and the decomposition was wrong. The brief even carried the count, so the query had been run and only its cardinality kept. The rule is that a predicate is not a scope until it has been evaluated: run it while grounding, write the files down, check them against every sibling — or declare the slice exclusive over the predicate's whole domain and give no sibling a file that could fall inside it. It is silent in every channel a dispatcher watches, which is why it is a rule and not a judgement: the divergence tick sees two agents each inside its own brief, and the first sign is two diffs on one file at merge.

- **"The docs a slice falsifies" was the right question asked of one artifact class.** `decompose`'s doc bullet already derived falsified docs from what a reader would newly believe false rather than from a keyword grep — and stopped at docs. A slice also falsifies a guard-proof row, a `package.json` script entry, a pre-commit chain entry, a fixture, a CI job, a ledger entry, and an enforcement artifact left behind is worse than a stale paragraph because it keeps *asserting* something the tree no longer supports. Observed: deleting one temporary check meant removing its script entry, its pre-commit chain entry, its fixture, its guard-proof table row and a paragraph of testing docs — and the chain was declared in four places, so removing the one the first search found would have left three referencing a deleted file. The bullet now carries both halves: the same derive-from-behavior discipline applied to enforcement artifacts, and *find every declaration, not the first*.

- **Inside an epic, docs were written per slice against states nobody would ever see.** Docs describe an end state; an epic's intermediate states are by construction not shipped. So a doc rewritten by slice 1 is rewritten again by slice 3, and the intermediate version was true of nothing anyone saw — while doc files behave as hotspots in a way source files usually do not: one chapter describing a mechanism four slices are each changing a quarter of turns four patched sentences into four conflicts whose resolution is *semantic*, handed to the person least equipped to do it. Simply deferring the docs breaks the forcing function instead, since the implementer is the only party that knows what its change actually falsified. So the two halves split: `decompose` still names the docs each slice falsifies, the slice's **deliverable becomes an entry in the epic's falsification ledger** rather than the edit, one **closing docs slice** consumes the ledger against the final tree, and **an epic cannot close with a non-empty ledger** — the same ratchet shape this flow already uses elsewhere, and what keeps this from decaying into deferred docs. The ledger is the epic's record, not a file in the tree: a file every slice appends to is the exact hotspot the arrangement exists to avoid. Two carve-outs: a docs-only slice writes inline, and single-slice work has no end of an epic, so its docs ship with the change exactly as before.

- **Scaffolding had no name in the skills, so its teardown had no slice.** Build a temporary enforcer — a ratchet file plus the check that reads it — drain it to zero across several slices, then delete the enforcer, its fixture, its ledger and its documentation together. The pattern ran twice in three days and worked both times, but nothing said the teardown was a *planned* slice, so its precondition lived only in the dispatcher's head and had to be fenced off by hand in four separate briefs. `decompose` now plans the two ends together, with the teardown's precondition written down when the build is planned. The failure runs both ways: a teardown that lands early removes the enforcement while the thing it enforced is still incomplete — an empty ledger reads as an invitation — and a teardown never planned at all leaves dead scaffolding that reads as live machinery to everyone who arrives later.

- **The `Verify` bar asked for pass-after and never for fail-before.** It was "what 'done' looks like: the behavior, the tests to add/touch, the acceptance check". Every check that actually caught something across three days was strictly stronger: restore the old matcher and confirm the new fixture goes red, point a test's mock back at the old symbol and confirm it fails loudly, reverse a rename across 87 files and confirm the formatter reproduces the original byte for byte. Any slice that adds a check or fixes a bug now has to prove the new test fails against the pre-change code, with the specific reversal named in the bar rather than a bare "confirm it fails first". A test that passes both before and after proves nothing and is indistinguishable, in a green run, from one that proves everything — so the slice reports success having verified nothing.

- **Model tier was predicted by how hard the work looked.** `opus` was reserved for subtle algorithms, ambiguity, tricky concurrency, security, large cross-cutting changes — a list of things that *look* hard. Observed: the most dangerous slice of the whole run matched none of them and read as the most mechanical work on the board — point four imports at a shared schema. The shared schema and the inline one disagreed, the shared one was the weaker, and the obvious execution would have silently removed strictness from three live HTTP routes. The list stays, with a better predictor beside it: **does the slice touch something declared, or enforced, in more than one place?** If so the work is not the edit, it is adjudicating a disagreement between two sources of truth. Diff size does not predict difficulty; the number of places the truth is stated does.

- **The gate-cost economics were correct and had a blind spot: an epic that gates once on the merged tip.** The sizing guidance reasons carefully about N slices meaning N serialized drained gate runs. When an epic converges on an epic branch and gates as a whole, that cost is largely gone — and the constraint that replaces it is the dispatcher's own capacity to actually read N diffs, which is well below the number of slices the gate would have tolerated. Removing the serialized gate removes the only bound anyone was measuring, and the wave then grows until the reviews are skimmed — in a flow where no gate can tell whether the agent solved the right problem, the diff review is the only thing standing between a green slice and a wrong one.

## 3.0.1

- **`orchestrate` §2 delegated the divergence tick to a pointer, and a pointer is skippable.** The paragraph named the tick, then sent you to `/pipeline:execute` for the interval, the purpose and every mechanic — so a reader who did not follow it still had enough text to feel informed. What that reader reaches for instead is `ScheduleWakeup`'s own tool description, which warns against scheduling wakeups to poll for background work the harness tracks; that warning is about polling **for completion**, but nothing in §2 said so, and the conclusion "this does not apply to my sub-agents" looks sound. Observed: a dispatcher did exactly this, ran an entire session with no tick armed, and wrote a confident critique of the skill listing three defects the unopened section already answered. §2 now states the load-bearing minimum itself — poll every ~10 minutes (≈600s), completion notifications are free and are NOT the mechanism, the tick is for early divergence detection — keeps the pointer for the detail, and names that rationalization so the next reader cannot reuse it. The mechanism did not change; the delegation is what failed.

- **§2's cadence contradicted the mechanic it pointed at.** It read "you owe a tick for every minute in between" — per-minute against `execute`'s ~10 minutes. Two numbers for one timer means a reader either picks one at random or reads the whole instruction as approximate. §2 now carries `execute`'s number.

## 3.0.0

**Breaking: `/pipeline:execute` and `/pipeline:orchestrate` have exchanged meanings.** The arc loop — the leg you type after `/pipeline:write-issue` — is now `/pipeline:orchestrate`. The per-increment worktree playbook it drives each cycle is now `/pipeline:execute`. Anything that invokes `/pipeline:execute` to run an issue end to end must move to `/pipeline:orchestrate`. *The failure this rename creates for anyone who does not follow it, and the reason it is called out first: the old name still resolves. A saved prompt or a downstream repo's `AGENTS.md` that says `/pipeline:execute #123` now reaches the skill that ships ONE increment and stops — no reconcile, no second cycle, no close-out — and it reports success, because shipping one increment is exactly what it was asked to do.*

- **The two coordination skills had each other's names, and both claimed the same trigger word.** `execute` ran the whole arc while `orchestrate` ran one increment, so `execute`'s description had to offer "coordinate/**orchestrate** a body of work end to end" and `orchestrate`'s had to answer "running a whole arc or epic to completion is `/pipeline:execute`'s loop, not this skill's". Two skills competing for one word is a routing hazard, and it was being paid for in every description that had to fence the other one off. The names now state the level instead of the action: you **orchestrate** an arc, and the loop **executes** an increment of it.

- **The public surface is two commands and says so; the rest are passes the loop drives.** `/pipeline:write-issue` → `/pipeline:orchestrate` is what a person types. `decompose`, `execute` and `review` now describe themselves as reached from the loop rather than as places an arc starts — still directly invocable when you want one increment grounded, one increment shipped, or one diff reviewed. *The failure this prevents: a skill that reads as an entry point gets used as one, and an arc entered at the increment pass ships its first wave and stops, with nothing left holding the loop.*

- **The dispatcher role is named `DISPATCHER`, not `orchestrator`.** Every one of the 94 occurrences of the old role word meant the increment coordinator — the role that cuts a worktree per slice, reviews each PR's diff, drains the gate and merges — including the four inside the arc-loop skill, which read "invoke `/pipeline:orchestrate` and act as the **orchestrator**". The vocabulary now stacks with the skills: `orchestrate` hands you the **ORCHESTRATOR** role and the arc; `execute` hands you **DISPATCHER** for an increment or **IMPLEMENTER** for a slice. *The failure this prevents: leaving the word in place would have put "you are the ORCHESTRATOR" inside `/pipeline:execute` while `/pipeline:orchestrate` sat beside it as a live sibling skill — an agent reading its role as the name of another skill has a path straight back into the loop it was dispatched from.*

## 2.3.0

- **`execute`'s step 2 now owns the wait, not just the dispatch.** The five-step loop reads as a sequence, so the interval between dispatching implementers and merging them — where the agents actually live — was invisible in it, and the only instruction that arms a divergence tick lives in `orchestrate`, reached by a pointer, at the moment the orchestrator is busiest. A run could satisfy every step in order with no tick ever scheduled. Arming the tick is now part of dispatching: step 2 is not finished until the timer is set, and a dispatch report that does not name it is a step still open.

- **An alarm gets confirmed against the fork point before anyone reaches for `TaskStop`, whatever channel it arrived on.** `orchestrate`'s fork-point discipline was written for the diff the orchestrator runs on its own tick, but the alarm that reaches it is usually a fragment from elsewhere — a pasted screenshot, a line in a hand-back. A fragment carries no base, and the direction of a change cannot be read off a hunk: a rename back to the old name is byte-identical whether it is divergence or the **revert** of an out-of-scope rename. Only the fork-point state of that symbol separates them, and confirming costs one `git show` against a mis-cut that throws away the run.

- **The gate drain is sized to the fan-out.** Draining on every tick is right for several slices enqueueing at unpredictable times and near-dead weight for one, whose hand-back is a free notification. It stays on the tick to cover an agent dying between `enqueue` and hand-back, with the rule that its presence there never stands in for the divergence check — the half of the tick that costs a whole run to skip.

- **`execute`'s reconcile checklist grew a fifth item: deferred decisions.** A question an increment deliberately left open is neither a stale coordinate nor a falsified assumption, so items 1-4 cannot catch it — nothing stops resolving, nothing gets falsified, and the brief still reads fine. So the deferral renews itself every cycle in silence, and the half-state ships: a field named `accountId` typed `UserId`, worse than either endpoint because a half-done rename reads as a deliberate distinction to everyone who arrives later. Each deferral is re-asked at the new horizon against one test — does the merged tree still hold together with it open?

- **`AGENTS.md`'s hand-back rule named a draft PR as the endpoint and had no way to express an authorized merge.** The draft is still the default hand-back and the interlock against merging an unread diff, but a maintainer saying "ship it" *is* the review — so the flow now carries through to ready, merge, and a synced integration branch rather than stopping at a draft. The failure runs both ways: merging on your own initiative spends the only review artifact a prose repo has, and handing back a draft after being told to ship leaves a bumped version and a green gate that shipped nothing.

## 2.2.1

- **The epic branch's name carries no mechanical meaning; `transient-red/<epic-slug>`'s name is the contract.** Nothing reads an epic branch's name — not a helper in `bin/`, not a step in the playbook — because the branch is identified by what forks from it and PRs into it, so any prefix a team likes is fine. The marker is the opposite: the detector finds the window by looking for that exact ref, so its spelling is exact and not a matter of taste. `orchestrate`'s epic-branch mechanics state the two together, where the branch is cut, because the branch's name is free precisely *because* the marker took the job of being found — and a reader who carries the freedom across to the marker writes one no detector will find.

- **A detector never keys on a branch-name prefix.** `orchestrate`'s transient-red guidance still names the two prefixes that look right, `epic/*` and `integration/*`, and now carries the rule they are instances of: tooling that has to ask "is this an epic branch" answers with a dedicated ref of its own, the way the marker does.

- **`decompose`'s breakdown header takes the real epic branch name, not a `feat/<epic-leaf>` template.** A `feat/…` epic branch is indistinguishable in a PR list from the `feat/<leaf>` slices merging into it, which is the one distinction that header carries; the slot is a neutral placeholder with a one-line note that the only thing worth choosing the name for is that it does not read like a slice.

## 2.2.0

- **The transient-red window was described but never opened, and a relaxation that never fires is indistinguishable from one that was never built.** `orchestrate` told you to put the window on an epic branch and how to read a drained gate's red against it; `decompose` flagged which slices lived in it. Both are judgments made by something that can read prose. A project's **commit-time** tooling cannot — and where a repo relaxes its pre-commit compile check for exactly this window (forgiving diagnostics that all point at files the branch never touched, holding the branch's own files to the full bar), the only thing it can consult from inside a slice worktree is the ref graph. Nothing in the flow ever put the fact there. The gap is silent in both directions: a detector looking for one ref and a flow that pushed another never meet, nothing errors, and every slice pays full strict-path friction for a relaxation the repo believes it has.

- **Opening the window is now a step, and it is one marker ref.** An epic branch cut under Rule 1's primary trigger gets `transient-red/<epic-slug>` cut and pushed beside it, in the same breath as the branch itself; nothing forks from it and nothing merges into it, and it is deleted with the epic branch at close-out, which is what closes the window with nothing to unwind. `decompose`'s existing transient-red flag is now what tells the orchestrator to open it, not merely to expect red. The ref is deliberately the whole contract: it exists ⇔ the window is open.

- **Two near-miss keys are named explicitly, because both look right.** A detector must not key on the **epic branch itself** — Rule 2 cuts one for parallel dispatch alone, on epics where every intermediate state is green, so keying on `epic/*` would silently forgive real sibling breakage on every one of them — and not on `integration/*`, which this playbook reserves for the shared branch and says never means anything else. The separate marker is what keeps "this epic is isolated" and "this epic is knowingly red" from collapsing into one signal.

- **And one check, once per epic: confirm it actually fires before dispatching the first slice.** Both failure directions are silent, so the only evidence is evidence you go and ask for — from inside the first slice's worktree, where the tooling runs, not from the main checkout. The close-out deletion carries the same weight for the same reason: a marker left behind outlives the epic that justified it and quietly relaxes the next unrelated slice's compile check against a window that closed weeks ago.

## 2.1.0

- **`write-issue` grounded `file:line` for the whole arc while the loop that consumes it grounds only the horizon, and one word wearing two meanings is what hid the contradiction.** **Grounding** is how you know a plan is *true* — this function exists, this really is the only call site, this consumer would ripple. **Coordinates** are how an implementer *finds* the work: a `file:line` to-do list. The skill did both under one unqualified rule ("Ground every target before you write it"), and its `Targets` section was defined as "the concrete `file:line` list the work touches, as the to-do list" — so a five-phase issue carried line anchors for all five phases, and phases 2-5 of that list were the exact stale-coordinate class 2.0.0 rebuilt the pipeline to eliminate. Worse, they sat in the one artifact nothing in the loop re-checks: `execute`'s reconcile pass is scoped to the *remaining plan*, not to the issue body, so a coordinate written here survived every cycle that would have caught it anywhere else. Nothing errored. The issue read well, `decompose` grounded the horizon correctly on top of it, and the later phases' coordinates simply went quietly wrong.

- **The grounding stays, and its purpose is now stated: it is how you establish the plan is TRUE, never how you produce a to-do list.** The cheap fix — ground less — reintroduces the failure the skill exists to prevent, so it was refused outright. `write-issue` still spawns read-only research per subsystem, still greps the WHOLE repo and cites the definition line before a load-bearing claim counts, and still cross-checks anything that surprises it. What changed is what it does with the result: **ground with `file:line`; write down the module and the file.** An ungrounded issue does not come out vague, it comes out *confidently wrong* — asserting a consumer that isn't one, a sole call site that is one of six, a function deleted last month — and nothing downstream can tell those claims from correct ones. A stale coordinate can at least be re-checked against the tree, which is precisely what reconcile does; a false claim has nothing to check it against.

- **`Targets` is now `Surface`, and the rename is the substance rather than the decoration.** The old name read as a to-do list because it was defined as one, and a section that reads as a to-do list gets executed as one — item by item, with a phase-4 coordinate opened as an instruction rather than as context. `Surface` states **where the work lands and what it touches**: real modules and files, grouped by area, plus the consumers each change ripples into — a map, not a checklist, with no line numbers, nothing phrased as a sequence to work down, and explicitly *not* the per-slice owned-file list with do-not-touch boundaries, which belongs to `/pipeline:decompose` and gets drawn at the horizon. A path survives the phases in front of it; a line number does not, and `decompose` re-derives the exact anchors every cycle against the tree an implementer will actually open — so writing them into the issue buys nothing and costs accuracy. The KEEP/STRIP list, the frontmatter description, the follow-up section, the hard-boundaries list and the closing argument all moved with it. The issue comes out **shorter and more durable, not vaguer**: `Goal`, `Approach`, `Phases`, `Seams`, `Verify` and `Constraints` are untouched, because they were always the vision-and-shape half and they are what makes an issue reviewable before anyone builds.

- **`execute`'s reconcile item 1 claimed "everything past the horizon is at shape depth and names no coordinates" — false before this change, and *nearly* true after it, which is the more dangerous of the two.** It now says the list is short because everything past the horizon is at shape depth **and** because a `write-issue` body names modules and files rather than a line-level to-do list — then adds the half that keeps the check alive: **short is not empty.** A module path is still a path, item 1 checks "every path, symbol, table, route, or key", and the increment that just merged is the likeliest thing in the world to have moved one. *The failure this prevents: reading the discipline as a guarantee turns the item into a formality, and a formality reports the same clean result whether or not anything moved* — the same shape as the write-it-down-even-when-empty rule two paragraphs below it.

- **`README.md`'s `write-issue` row now says what an issue actually contains.** It had said only "grounds an idea in the real code and files it as a forward-facing issue" — true under the old model and true under the new one, and therefore silent about the only thing that changed. It now names the sections a body carries and puts "write a line-level to-do list" in the never-does column. *The failure this prevents: a doc that reads correct under both the old behavior and the new one is the kind nothing ever flags — it carries none of the new vocabulary, so a grep for the new terms misses it, and it carried none of the old vocabulary either, so a grep for the stale wording misses it too. It just goes on quietly under-describing the artifact a user is about to receive.*

## 2.0.1

- **`docsBranchPrefix` was stale three ways at once and nothing ever errored on it.** The key appeared in neither skill's per-project config guidance, `examples/worktree.json`, `skills/setup/SKILL.md` (the skill that actually writes `.agents/worktree.json`), nor any `bin/` helper — a decomposer reading it out of a project's config got an empty read back and moved on silently, so a reader concluded a per-project docs fast path existed and was merely unconfigured here. What it named, branch-name inference, is exactly what `orchestrate`'s own *Gate mode* forbids (a ticket's gate mode is set at enqueue, never read off the branch), and what it selected, the shared `node_modules` symlink, had already been removed outright for the failure `orchestrate`'s *Gate mode* still records as a war story rather than deleted history. `decompose`'s Title/branch-name bullet and per-project-config list, and `orchestrate`'s per-project-specifics sentence, no longer mention it.
- **Where a reader was pointed at the prefix, they're now pointed at its live successor instead.** A docs-only slice now gets flagged for `orchestrate`'s ticket-scoped `--mode docs` gate — set on the ticket at enqueue, never inferred from a branch name — rather than a fast path that could never have fired.

## 2.0.0

- **The pipeline is two commands now — `/pipeline:write-issue` → `/pipeline:execute` — and that is what makes this a major rather than a 1.19.** The user-facing command surface changed and trigger vocabulary moved between skills: *execute a plan*, *work or complete a GitHub issue*, *handle a batch of tasks*, *ship an arc or an epic*, *coordinate a body of work* now reach `/pipeline:execute`, a skill that did not exist on 1.x. `orchestrate` keeps entry at the level it actually works at — one increment, or a dispatch brief — and `decompose` keeps direct invocation for anyone who wants a single grounded breakdown. Nothing was deleted and every skill still runs when it is named, but a session reaching for the old arc-level phrasing now lands on a different skill than it did yesterday, and the version string is the only thing that can tell an install so.

- **What the new leg replaces: a plan grounded once, up front, is at its most accurate the moment before any of it runs, and decays from there.** A front-loaded decomposition grounds every wave against the tree as it stands when the pass runs, then writes `file:line` coordinates into briefs that later waves execute. A wave-4 brief naming a route is correct when written and wrong when run, because wave 1 renamed that route — and **nothing errors**. The brief still reads well; the implementer opens a tree where the target is not there, finds the nearest plausible thing, and builds against that, because an implementer handed a coordinate does not treat its absence as a stop. The same pass also asserts a completeness it cannot have: work the arc structurally forces but no slice owns stays invisible until an implementer walks into it mid-wave, out of scope, with no owner and no boundary. Grounding the whole arc is indistinguishable from grounding it *better* right up to the moment a wave lands and moves the paths, which is why this is now a rule rather than a judgement call.

- **Two grounding depths, separated by a horizon, and reaching the horizon is the only thing that promotes an item between them.** The **horizon** is the next dispatchable set — every remaining item whose dependencies have already landed, which is usually a wave and sometimes only the genuinely dispatchable part of one. Inside it, **slice depth**: owned files as real paths, do-not-touch boundaries, depends-on, framework skill, model tier, brief, verify bar, grounded against the tree as it stands right now and dispatched in the same cycle. Outside it, **shape depth**: goal, area, what it waits on, one line on why it comes after the thing before it — and no coordinates of any kind. An item at shape depth is *finished*, not half-written. Both failures are silent and they run in opposite directions: grounding early produces the stale coordinate above, and dispatching at shape depth hands an implementer no owned-file list and no boundary, so it invents its own scope and the first anyone hears of it is a PR in a sibling slice's core files — by which point the sibling has already forked. Nothing else promotes an item: not being well understood, not being small, not a user having asked about it.

- **Reconcile is the step a front-loaded decomposition does not have, and it is a fixed four-item checklist because a step re-derived from scratch each cycle gets a little shorter each cycle.** It runs after the increment has **merged**, against the **merged tree** — not the PR diffs and not a summary of them, since the merged tree is the one the next increment forks from and therefore the only tree whose state can falsify anything. In order: coordinate drift (a target that does not resolve is stale by definition, not a maybe); **vocabulary drift checked per sense, not per string**, because one word routinely carries two meanings a rename splits and a find-and-replace rewrites the surviving sense too — producing a change that compiles, passes, and is wrong; revealed forced work, asked against the tree rather than against the plan, since the plan is the thing that failed to mention it; and falsified assumptions, which are plan defects fixed in the plan rather than discovered later by whoever inherits the brief. **The outcome gets written down even when it is empty**, because a cycle that reconciled and found nothing and a cycle where nobody ran the checklist produce identical plans, and the record is the only thing that tells them apart.

- **Fold vs. file, decided by one test asked in the direction of the arc: can it land without this?** Forced work — the arc cannot compile, pass, or ship without it — gets **folded in** as a named slice with its own wave. Adjacent work — real, worth doing, but the arc is correct without it — gets **filed and linked**, never folded. That guard is what keeps a re-decomposing loop finite: re-grounding every cycle makes absorbing new work trivially easy, since the increment is already open and the tree already read, so an arc that absorbs everything it touches never terminates — and no single cycle is where that becomes obviously wrong, because each fold is individually defensible and the close-out simply never arrives. The mirror failure is just as real and looks even more like diligence: filing something that was actually forced ships an arc that does not build, and leaves a tidy linked issue behind as evidence of thoroughness. Asked in the direction of the *item*, every item looks worth doing; asked in the direction of the *arc*, the answer is checkable.

- **Where a folded item goes is decided by merge surface first, with slice cohesion only as a tiebreaker between placements that have the same merge surface.** Ordered, not balanced: when cohesion says *this belongs with the slice that owns that module, which is running right now* and merge surface says *it rewrites a shape forty files reference*, cohesion loses outright. Churn discovered mid-arc goes in a **serial wave** — one slice, nothing else in flight — because parallel slices fork from different bases, so a large-footprint change dispatched alongside them merges textually clean and semantically wrong: git merges by line and has no idea two edits must compose. When the shared shape is a **runtime string** — a query key, a table name, a route, a config key — the bad merge does not even fail to compile; it splits one cache entry into two or collides two into one, and every gate in the wave stays green. And nothing folded ever joins a wave that has already been dispatched, whatever its size, because those slices forked before the folded item existed and their do-not-touch boundaries cannot name it.

- **The decide-don't-ask bar, plus the one property of it that is specific to a loop: it gets applied once per cycle, so setting it slightly too low multiplies.** Wave assignment, fold-vs-file, sizing and re-slicing are the loop's calls to make and report, not questions to put to the user; the single escalating class is a product or design fork the code and conventions cannot settle, asked in plain chat, one question at a time, with a recommendation and why. The split itself is stated once — in `decompose`'s *Validate the plan and fill the gaps* — and pointed at rather than copied, because two statements of one rule drift and nothing then marks which is stale. What the loop adds is the arithmetic: one avoidable question per cycle across eight cycles is eight user turns spent on things the repo already answers, and each one teaches the user that the loop cannot be left to run, which is the entire value it was supposed to deliver.

- **The loop's state is the umbrella body, its history is that issue's comments, and termination needs two halves rather than one.** The body always describes what is *left*, at the depth the horizon assigns, and is rewritten every cycle rather than appended to; one comment per completed increment records what landed and what it invalidated. Neither artifact works alone — with only comments, the current plan has to be reconstructed by reading the whole thread in order and applying each correction in sequence, and two readers reconstruct it differently; with only a rewritten body, an item that disappears leaves no trace of why, so nobody can tell a completed item from a dropped one. Done means **the remaining plan is empty AND the close-out is green**: an empty plan over a red close-out is not done, and a green gate with items still outstanding is not done either. And a cycle that finishes with the remaining plan identical to the one it started with **stops and reports** — a loop whose structure is *re-ground, dispatch, reconcile, repeat* has no natural stopping condition when an increment keeps failing, so it will re-ground the same frontier indefinitely while every individual cycle looks locally like progress.

- **A verify rider that generalises past this arc: any slice renaming an identifier that crosses a string boundary carries a bare-string sweep in its verify bar.** A table, a route, a cache key, a config key, an env var, a feature flag — anything that leaves the type system's reach and re-enters the program as text. The sweep greps the *old literal* across the whole tree, fixtures, snapshots, generated files, docs and config included, and proves either zero hits or that every survivor is deliberate. The failure it prevents is structural rather than careless: a typecheck cannot see a renamed table asserted as a literal in a fixture, and the single targeted test file an implementer is allowed to run will not be the file that asserts it — so **both** checks an implementer actually runs are blind to the thing, the slice reports green, and the break lands. It attaches to a *shape* of slice rather than to one change, which is why it lives in the skill and not in whichever brief trips it this week, and it goes into the verify bar at **grounding** time: added at review it arrives after the implementer has already reported green, costing a round trip to reopen a worktree that was about to be torn down.

- **`orchestrate` and `decompose` were re-scoped rather than rewritten, and the sharper of the two edits is a role-entry rule.** `orchestrate` now says which role you are in is decided by **how you got here, not by how the work looks** — orchestrator from the loop or from a direct ask to coordinate one increment, implementer from a dispatch brief or a direct build instruction. Both misreadings are silent and both produce work that looks like progress: read as an implementer, an orchestrator writes the code it should have been dispatching, one agent in one worktree with no boundaries and nobody but the author reading the diff; read as an orchestrator, an implementer dispatches the slice it was handed, and its sub-agents inherit a brief whose imperatives end in *commit, push, open a PR* — so the PR is open before the agent that owns the worktree has decided what goes in it. `decompose` is now explicitly the loop's per-increment grounding pass: it grounds the horizon and emits the remainder at shape depth on **all three** of its input paths, since reading the horizon as a property of having been called by `/pipeline:execute` would make a directly-invoked decomposition the one that grounds the whole arc — the same stale coordinates, reached by deciding the discipline did not apply here. Its wave map still covers the whole arc, because a dependency ordering names no files and the horizon is *defined* as the next set whose dependencies have landed; without it the next cycle has nothing to move the horizon along by.

- **The rest of the surface had to be made to agree, and that half of the change is the one nothing would have caught.** `README.md`'s *Why this shape* opened with **"Front-loaded planning → `write-issue` and `decompose` do the expensive grounding before any worktree exists"** — the README advertising, as the flow's headline virtue, the exact defect this arc replaces. It is now the just-in-time argument. `write-issue` hands to `/pipeline:execute` and `setup` onboards a repo onto a two-command pipeline; both had named the three-leg one in their **frontmatter descriptions**, which is the text every session reads when deciding whether to invoke a skill at all. That README bullet and `write-issue`'s "`decompose` turns it into orchestration-ready slices" were both found only because Wave 1 slices reported docs they were forbidden to edit, and both sat a few lines outside every line anchor the tracking issue listed — an anchor list reads as exhaustive, so a falsified sentence just outside one survives while every listed line is correctly fixed, and the slice goes green with the file contradicting itself. And `README.md`'s install-verification line claimed the plugin ships **four** skills; it had shipped five since `review` landed, and would now have been wrong by two. A count-shaped sweep catches that class where a sweep for the exact stale wording never can, because the next drift will be spelled a way no targeted grep is looking for.

## 1.18.0

- **Nothing in this plugin had ever said the word `stash`, and the stack it left unmentioned is shared by every worktree in the repo.** `refs/stash` is a **common ref**: the main checkout and every linked worktree push onto and pop off the same stack, and `git stash list` in one tree lists every other tree's entries indistinguishably from its own. A bare `git stash pop` therefore takes whatever is on top regardless of whose it is — it applies those changes into *your* tree and **drops** them from the stack. And this flow's default dispatch shape is several worktrees live at once, so that is not an edge case — it is the ordinary state of a run.

- **What that cost, observed.** In a repo with the main checkout and two slice worktrees all live and all having stashed, a bare `git stash pop` in one slice worktree applied the main checkout's stash into the slice and dropped it. The human's WIP was gone from where they left it and sitting silently in an agent's worktree, and nothing errored — no warning, no conflict, exit 0. Recovery from a dropped stash is `git fsck --unreachable` and only until the next gc, so the window in which the loss is even repairable closes on its own.

- **The fix is a marker plus a re-resolve, and neither half works alone.** Git's auto-generated `WIP on <branch>: <sha> <msg>` subject identifies nobody — two agents on one branch write the same line, and so does one agent resumed after a stall — so agents now stash with `git stash push -m "pipeline-stash/<branch-leaf>/<epoch>: <purpose>"`. The prefix makes every agent stash greppable as a class, the leaf says whose it is, and the epoch separates the two identical-subject cases. But a name is not a handle: `stash@{n}` is renumbered by every push and every drop **anywhere in the repo**, including in trees you cannot see, so an index resolved at the start of a turn can point at a different entry by the end of it. Hence the second half — re-match the marker immediately before every pop, apply or drop, and treat **zero matches or two-plus matches as a STOP-and-report**, never as a reason to fall back to `stash@{0}`. Your entry not being where you left it means something else moved the stack, which is the moment to hand back rather than to guess.

- **Two bans and a preference ride along, because the destructive operations here are the ones with no undo.** `git stash clear` destroys every other worktree's entries and the human's in one command with no confirmation; dropping an entry you did not create is the same act retail rather than wholesale. And a **WIP commit on your own branch** avoids the whole class: it is worktree-local, unstealable, and unwinds with `git reset --mixed HEAD~1` when a plain uncommitted diff is what the review pass needs. Ending a turn with a stash still on the stack is out for a reason worth stating: once that worktree is torn down, the entry is unattributable, and an unattributable stash is one nobody can ever safely pop. The orchestrator gets the matching sweep — `git stash list --format='%gd %gs' | grep -F 'pipeline-stash/'` on the tick it already runs — with the reading that a hit while the fleet is quiet is a defect to chase, not noise.

- **The second rule: an agent owns its follow-ups, and no skill said so.** The work a change creates or reveals — residual cleanup, a doc it made stale, the second call site, the missing test, the rename left half-done — was landing in hand-back prose as "flagged for follow-up: X, Y, Z". That reads as thoroughness and is indistinguishable from it, which is exactly the problem: the items are untracked the moment the turn ends, and nothing in the gate, the PR, or the orchestrator's review can tell they were ever raised. Untracked, a follow-up is not a follow-up; it is a defect with a polite name. Each one now becomes a real issue via `/pipeline:write-issue`, linked to the originating issue or umbrella, decomposed if it is more than one slice, and folded into the orchestration. The economics are one-sided: the agent holding the item has the worktree open, the install paid for, and the change that caused it on screen, and a later session has to rebuild all three before it can start reading.

- **The rule binds the orchestrator too, and forwarding is the same defect one level up.** An orchestrator reading a report that names unfinished work owns the next move — a fix agent into that worktree, a resume to the same implementer, or a filed and linked issue folded into the wave plan. It is worse from there than from a slice, because the orchestrator is the only party in the run holding the decomposition, the sibling slices, and the plan the item has to be placed against; a triage it declines is a triage nobody in the run can do.

- **A follow-up inherits the arc's branch level, which is the case the existing branch rule most often loses.** "Branch from the branch the work converges on" already covers this, but a follow-up *feels* like new work rather than like part of the arc that produced it, so it gets routed at the integration branch by reflex. While an epic branch is live, a follow-up the epic surfaced forks from and PRs into the **epic branch** like any other slice of it. Routing it to the shared branch instead bypasses precisely the isolation the epic branch was cut for, and delivers the epic's leftovers piecemeal while the epic itself is still converging.

- **Two existing paragraphs were bounded rather than rewritten, because read loosely each of them launders the defect.** `orchestrate`'s stuck-hand-back sanctions handing back when you are *blocked*, and its missing-input paragraph sanctions it for an identifier that names a real thing you cannot obtain. Neither sanctions handing back work you simply did not do — but a hand-back reporting a blocker and one reporting an unfinished intention are the same artifact from the orchestrator's side, both prose naming work that did not land. The test is now stated where those paragraphs live and it is mechanical: **if you can still act on it, it is not a blocker** — fix it in its own commit, or file and link it. `review`'s *Flagged, out of scope* bucket got the same treatment from the other end: an item is admissible only when fixing it falls outside the slice boundary the brief drew or is unverifiable from that worktree, and what stays flagged is the **caller's** to file, since that pass reports and neither dispatches, commits, nor pushes.

- **The general form, and it is why both rules landed in `orchestrate`'s hard rules rather than in one role's section: work that changes hands without a name on it is lost silently, and fan-out is what makes that routine.** The two look unrelated — one is a git command, the other a reporting habit — and they fail identically. A stash with no marker is an entry nobody can claim, so the next agent that needs the top of the stack takes it. An item named only in a hand-back is work nobody has been given, so the run ends with it un-owned. Neither produces an error, and both are indistinguishable, in every artifact this flow inspects, from the correct behavior. The fix is the same shape in both: **make the thing carry the name of whoever owns it — a marker on the stash, an issue number on the follow-up — and require that name to be re-established before anyone acts on it.**

## 1.17.0

- **`write-issue` authors the artifact `decompose` consumes, and it did not know either of the two path decisions that artifact has to answer.** `decompose` makes two calls off an issue: the **output path** — post the breakdown as a comment, or convert the issue into an umbrella with one sub-issue per slice — and the **epic-branch path** — whether the slices fork from the integration branch or from an epic branch cut off it, so the shared branch never carries the work half-finished. The second turns on facts that are properties of the plan its author has just designed rather than of the code as it stands: whether the branch is shippable at each phase boundary, and which producer → consumer seams the plan creates. Nothing in `write-issue`'s body structure asked for either, so neither was written down, and the pass that has to decide had nothing to decide from.
- **What that costs is an unasked question read as a no.** An issue that states no shippability fact is indistinguishable from an issue whose every intermediate state is fine, and the shape that then goes wrong is exactly the one an epic branch exists for: a foundational phase every later phase must follow lands on the shared branch and sits there half-migrated — green at every step, and forked from by every other session. Contract seams fail the same way and more quietly, since a seam whose halves land in different phases shares no file with itself and is invisible to a file-based conflict map by construction; the cross-tree ones — code ↔ docs, code ↔ prompt, code ↔ config — have nothing mechanical linking the halves at all, and the author who designed the change is the last person who can still see them without rediscovering them. `write-issue` now asks for both facts by name, and its hard-boundaries list gains the matching prohibition: state the facts, and never write "use an epic branch" into an issue body — a verdict in the body reads as settled downstream though it was reached without the grounding pass that is supposed to reach it.
- **A terminology collision fixed alongside it: an umbrella is not an epic branch.** The two are orthogonal — an umbrella is a tracking shape, how work is written down, assigned and closed; an epic branch is a branch lifecycle, where slices fork from and PR into — and "epic" is the same word for both, including as the label name `write-issue` tells authors to apply. Read as one thing, filing an umbrella looks like it settled the branch question, and the facts that would actually settle it never get written. Step 3 and the Labels bullet now say so, and the umbrella warrant is worded the way `decompose` states its own test rather than paraphrasing it.
- **`decompose` re-ran its own warrant test against a decision the user had already made, and overruled it.** Told to do the work "as an epic", the pass treated that as a hypothesis to check rather than a decision to execute, ran the epic-branch trigger, and wrote "Epic branch: not needed" into a breakdown — and the instruction had to be given twice before it moved. The cost has two halves and the second is the worse one: the user spends turns repeating themselves, and the overruling does not stay in the conversation, because this question is answered in one line inside a document that is then handed to `/pipeline:orchestrate` and executed as written. Every slice would have targeted the shared integration branch while the user had asked for exactly the isolation an epic branch provides, and an unwarranted "not needed" produces a perfectly well-formed plan, so nothing downstream could tell it apart from a correct one. **The general form, now stated in both skills because it is not specific to this decision: a skill's decision procedure exists for when nobody has decided, and never overrides a stated instruction — once the call is made, the skill is read for mechanics, not for a second opinion.**
- **In that same breakdown four documented contract seams sat beside the answer "not needed", and nothing reconciled the two.** A contract seam whose halves land in different waves is one of the two named shapes that fire the trigger, so the document's own evidence contradicted the document's own answer. The map and the answer come out of different steps and get read as independent, which is how a trigger fires on facts the same document already contains and the answer still comes back "no" — a wrong call reached with everything needed to get it right already written down. `decompose` now requires the epic-branch answer to be reconciled against the seam map it just emitted: a seam whose halves land in different waves, or a wave-0 foundational change every consumer must follow, has to be named and dismissed by name before "not needed" is available.
- **The epic-branch trigger gains a second rule, and this reverses a sentence both skills stated emphatically.** They said slice count is not the trigger — a five-slice run whose every slice is independently shippable wants no epic branch at all. That argument is correct and is kept intact, because it answers a **correctness** question: would a partial state on the shared branch be *broken*? Counting slices cannot answer that and never could. What it was never asked is the **operational** question, which is now the second rule: what does running several slices concurrently cost the shared branch, whether or not every intermediate state would ship? Four costs land regardless — every live slice's base moving under every other one, a shared branch routinely carrying merged trees no single slice's gate ever ran, N merges to unpick instead of one to revert, and a contract seam far easier to compare while both halves are still converging somewhere you control — and all four were already documented in `orchestrate` as things to work around. So **multi-slice work dispatched in parallel now defaults to an epic branch**, keyed on your own fan-out across one change rather than on a threshold: unrelated one-slice fixes running side by side are not an epic, "the integration branch is busy" stays rejected as the opposite concern, and single-slice work still never cuts one. The cost section is untouched and the merge-back cadence stays mandatory — more load-bearing now, since this default fires on the shape the flow reaches for most often.
- **The general form behind the first change, worth stating because it is not specific to these two skills: a downstream pass can only decide from facts the upstream artifact was asked to carry.** `decompose` grounds a plan in real code, so anything readable from the tree it will find on its own. What it cannot find is a property of the plan that existed only in its author's head at the moment of writing, and the only place that ever gets recorded is the artifact the author is already filling in. `write-issue` states the test once and points at `/pipeline:decompose` for the rest rather than restating its reasoning — two copies of one rule drift, and nobody can tell which one is stale.

## 1.16.0

- **`orchestrate` told every implementer to run a quality pass this plugin does not own, and that pass changed underneath running epics.** The instruction was a bare `/simplify` — a skill bundled with the CLI, versioned on its own schedule, with no relationship to the plugin's version field. It began fanning its reviewers out as **forks**, and a fork inherits the spawning agent's entire conversation. In this flow that conversation is an implementer's brief, and an implementer's brief is a list of imperatives ending in *commit, push, open a draft PR, enqueue the gate*. A reviewer handed that does not read it as background; it reads it as its instructions.
- **What that cost, observed across three consecutive slices of one epic.** Review agents applied their own findings, ran the repo's formatter at root, committed in logical blocks, pushed, opened the pull requests, and enqueued the gate tickets — each time before the implementer that spawned them got its turn back. Every artifact looked right, because the reviewers were faithfully executing a correct brief; they simply were not the agent whose judgment those artifacts were supposed to represent. Three worktrees were being written to concurrently by agents that each believed they were the implementer, and in one of them a deliberate fixture corruption (a drift-guard proving its own failure mode) raced a root-level formatter run in the same tree.
- **The second-order failure is the expensive one, and it is the argument for fixing this at the plugin boundary rather than with a better prompt.** Once unauthorized writes are in play, agents lose the ability to distinguish authorized work from rogue work — a careful agent and a runaway agent produce identical artifacts: a branch, a PR, a queued ticket. A sibling implementer, correctly alarmed by its own forks, saw a legitimate parallel slice's branch and PR appear mid-run, concluded it was more of the same, and quarantined that slice's gate ticket out of the queue. It cited the orchestrator's own do-not-touch boundary and the issue's follow-up comment as evidence, deliberately declined to close the PR or delete the branch, and wrote a README explaining how to reverse it. Every step of that was good defensive practice, and it was wrong on the one fact it structurally could not see — that the orchestrator had cut that branch itself, on the user's request. Legitimate work sat un-gateable until a human looked.
- **The fix is `/pipeline:review`, shipped here, and the load-bearing property is that it dispatches nothing.** It is one agent reading its own uncommitted diff — reuse, simplification, efficiency, altitude *and* correctness in one ordered pass, since a correctness fix can introduce something to simplify and a simplification can expose a correctness problem. It never spawns, never commits, never pushes, and never runs a full-suite or whole-package test (the drained gate owns that, one PR at a time). Verification is the project's own `scopedCheck` plus at most a single targeted test file, read from `.agents/worktree.json` rather than assumed.
- **Inline rather than findings-only-with-read-only-tools, which was the obvious fix and the wrong one.** The objection is not that a quality pass writes; it is that a *sub-agent* decided what goes in. When the pass runs in the implementer's own context, the agent applying the change is the agent that owns the slice, and the principle is satisfied with no new machinery. A read-only reviewer would also have been weaker than it looks: the natural "read-only" agent types still hold `Bash`, so a findings-only contract is a convention rather than a guarantee, and even a non-fork sub-agent that inherits no conversation can act on a misread prompt. Spawning nothing is the only version of this with no exception to reason your way into.
- **One rule inverted rather than moved, and leaving it un-inverted would have been the silent half.** `orchestrate` had *accommodated* the fan-out: a standing rule telling orchestrators not to mistake a `/simplify` pass for a stall, because it spawns its own reviewers and legitimately takes minutes, so an implementer reporting that it is waiting on review agents is self-suspended and must be left alone. Against an inline pass that signal means the opposite — an implementer waiting on reuse/simplification/efficiency/altitude reviewers did not run this plugin's skill, and something is writing to its worktree on its behalf. The general form of the rule survives for genuine nested waits; the review-specific case is now an alarm that sends the orchestrator to look at the worktree.
- **Naming: review contains simplify, so the skill is `review`.** The broad tier of review already exists in this flow and is deliberately not what this ships — the orchestrator reads each PR's diff and the drained gate runs the full build and suite over the committed result. `/pipeline:review` is the narrow, early tier that runs where neither of those can: inside the implementer's turn, on uncommitted work, before the commit blocks exist. It is not a leg of the write-issue → decompose → orchestrate pipeline; it is the pass an implementer calls on itself mid-slice.
- **The general form, worth stating because it is not specific to this skill: a flow that depends on a tool outside its own version boundary inherits that tool's changes without review.** The plugin pins its own behavior with the `version` field and a CHANGELOG entry per change; a slash-command it merely names does neither. Anything this flow's correctness rests on belongs in the plugin.

## 1.15.0

- **`decompose` has two deliverables and its description named one, so the pass was invoked only when that one was wanted.** The frontmatter description is the entire basis on which an agent decides whether to run the pass, and it described a transformer: plan in, slices out. Three properties compounded. The name and the triggers were splitting words — "DECOMPOSE, break down, slice, or plan-for-parallelism" is three of four. `GROUND` appeared in caps but grammatically subordinate, inside a sentence whose object was "emit independent task slices", so it read as a step *toward* slicing rather than as a deliverable. And Step 2 of the body — *Validate the plan and fill the gaps*, the step that says a breakdown is only as good as the plan under it, because slicing a plan with holes just buries the holes inside slice briefs where an implementer hits them mid-build — had **zero** representation: no occurrence of validate, gap, correct, defect, or wrong anywhere in the description. The general form, worth stating because it is not specific to this skill: **a pass whose name describes one of its two deliverables gets invoked only when that one is wanted.**
- **What that cost, observed.** An orchestrator was handed a well-specified issue — a type refactor narrowing a realtime transport's event vocabulary — that was plainly a single atomic slice. It reasoned exactly as the description invited: decompose is for splitting, this does not split, so skip the pass and hand-verify the issue's line numbers instead. The user pushed back ("isn't it also about grounding?") and the pass was run. Step 1/2 grounding immediately found that the issue's stated approach would ship a **silent production break**: it proposed making a transport gate exhaustive over the set of *published* event variants, while two variants on that transport are authored by the connection's own server object with no publisher behind them. Implemented as written, both would have failed the gate and been dropped at runtime — presence silently stops working, with a green build and a clean-looking type refactor as the last thing that touched it.
- **The hand-verification planned instead would not have caught it, which is what makes this a description bug rather than a skipped-step bug.** It was going to re-confirm line numbers and variant counts — *is the issue's description still accurate* — where grounding asks a different question — *is what this plan proposes actually true*. The two look interchangeable and are not, and the orchestrator's confidence in skipping came from having read those same files earlier in the session, which is precisely the state in which the second question goes unasked. So the fix cannot be "remember to ground"; the pass has to be reachable from the situation that produced the miss.
- **The trigger for the single-slice case is the load-bearing addition — not the mention of validation.** The description now leads with ground/validate/slice, names validating the plan against what the code actually does as a deliverable in its own right (wrong assumptions, unspecified behavior, defects in the plan itself, surfaced before an implementer builds on them), and carries a fourth trigger for work that is plainly one slice: grounding and validation stand on their own, and a plan that needs no splitting can still be wrong. A description that merely *mentioned* validation while every trigger stayed splitting-shaped would have left the observed reasoning intact and changed nothing. The additions are paid for rather than appended, since this text sits in every session's skill listing: the dispatch-loop appositive and the closing "pairs with the orchestrate skill" both restated what the first sentence already says, and are gone.
- **The body is deliberately untouched.** Step 2 is already correct and already says the thing the miss needed said; nothing in the skill's behavior-once-invoked was wrong. The defect was entirely in its advertisement, and editing the body to fix an invocation problem would have left the real cause in place while looking like a fix.

## 1.14.0

- **The integration gate was stated unconditionally, and it is only load-bearing in the case its own reasoning describes.** "N green PRs ≠ a green epic — each branch was gated against its own fork point, blind to the other slices" is true for **parallel** slices forked from a shared base and merged independently. It is not true for a run whose worktrees are each cut from the current tip and merged sequentially: there every branch is `tip + its own changes`, its gate already saw all prior merged work, and a conflict-free merge produces a tree **byte-identical** to the branch tip that was just gated. So the close-out gate re-executes the same tree for the same answer — on a monorepo whose full gate is ~10 minutes, a real cost paid for zero information, every epic. Observed across a thirteen-merge arc: twelve of thirteen merges introduced nothing beyond the already-gated branch tip.
- **It is now the cheap check that distinguishes the two cases, not a habit:** after each merge, `git diff --name-only <merge>^2 <merge>`. Non-empty means the merge produced content no gate has seen, from exactly two causes — a conflict resolution, whose resolved tree is new content by definition, or a sibling branch that merged in between, which this branch was cut before and gated without — and the merged result gets one gate run. Empty means the pre-merge gate already ran precisely this tree. The rule is kept in both directions because the failure runs both ways: an unconditional gate burns the suite on an already-gated tree, and "the last slice was green, so the epic is green" is wrong the moment two slices ran in parallel, which is this flow's default dispatch shape. The general form, worth stating once: a blanket rule that is right in the dangerous case and wasteful otherwise is better expressed as the check that tells them apart.
- **Two things the empty answer does not buy, both of which would be silent if left unsaid.** It proves the tree was *run*, not that the run was *green* — in the transient-red window slices merge on a green-modulo-baseline judgment rather than a green exit, so an epic tip can be byte-identical to a tree nothing ever observed fully green, and skipping requires both halves (nothing added **and** an actual green behind it). And it governs re-running the **suite** only: the merged *code* still gets read by hand wherever 2+ slices touched one structure, because the semantic drift that section describes is invisible to a green gate and equally invisible to an empty diff.
- **The epic branch made this sharper rather than obsolete — and the closing merge is the one that is usually non-empty.** 1.12.0 moved this gate onto the epic branch; what it did not say is that a gate on the epic tip is not a gate on what lands, because the integration branch moved while the epic ran, so `epic → integration` combines two histories nothing has tested together. The fix is ordering, not another gate: merge the integration branch INTO the epic one final time *before* the close-out gate — the same merge the drain tick already runs — and the gate then covers the exact tree the closing merge produces, whose `^2` diff comes back empty. That gives the mandatory per-tick merge a second reason to exist beyond bounded conflicts, and it means the epic close-out is at most one gate, sometimes none, rather than one by ritual.

## 1.13.0

- **`merge-pr` synced the PR's base branch by *switching the main checkout to it*, which is safe for exactly one session and corrupts a branch the moment there are two.** The main checkout is the one piece of global mutable state in a flow that is otherwise isolated per worktree, and multi-session work in one repo is now ordinary. Observed: three sessions in one repo; one ran `merge-pr.sh` for a slice PR based on an epic branch, so the helper switched the checkout off `release/0.4.0` onto `epic/…` and merged on the remote; in the window before the sync finished, another session's work put the checkout back on `release/0.4.0`; the sync then fast-forwarded *whatever was checked out*, retried six times, and exited with its loud "SYNC FAILED; do NOT cut new worktrees" error — correct, and the only reason the damage was caught. By then the local `release/0.4.0` ref pointed at the **epic branch's merge commit**: six commits of unrelated epic work on the release branch locally, while `origin/release/0.4.0` had independently advanced. Nothing was pushed, so no remote damage; the next worktree cut from `release/0.4.0` would have forked off a poisoned base.
- **The base is now synced by ref whenever it is not the checked-out branch — no switch, so no window.** A branch nothing is standing on needs no working tree to move: `git -C <main> fetch origin` then `git -C <main> branch -f <base> origin/<base>`. There is nothing for another session to land in the middle of and nothing to switch back afterwards. The old `pull --ff-only` path survives unchanged for the case where the base **is** the current branch, which needs no switch either because you are already on it. Both paths keep the existing verification that the local branch actually reached the merged tip — the path changed, the guarantee did not — and the ref path is only ever a *forward* move: a local base carrying commits the remote does not is refused outright with the reconcile command, rather than force-moved past them, since that is precisely the state the observed failure leaves behind. A base checked out in a **linked** worktree cannot be moved by ref at all (git refuses to move a branch any worktree is standing on) and is reported by path, because someone is working there — a condition to hand back, not to suppress.
- **And a branch you did not intend to move having moved is now an alarm.** The old error only ever checked that the *intended* branch reached the merged tip; it had no opinion about a branch that moved **instead**, which is the half that actually corrupts and the half that is silent — every other signal after the merge reads as a clean close-out. The helper now records the main checkout's branch and HEAD before it touches anything and verifies both at the end: if the checkout is on a different branch, or a different commit, it says so and exits non-zero, naming the branch, where it was, where it is now, and that another process moved it mid-run. It also re-checks the branch before **every** advance inside the retry loop, not only at the end, because the on-base decision is made once and then used across seconds of teardown, merge and polling — and on that path `git merge --ff-only` names no branch, so it moves whatever is checked out at the moment it runs. Checking once at the start would have left the original bug intact for that path. The result is a stop rather than a silent corruption, and a re-run finishes the sync as it always did.
- **Both shells, verified against a real git topology rather than by eye.** `bin/merge-pr.ps1` gets the same two paths, the same messages and the same exit codes, and `scripts/check.sh` holds the pair to matching usage lines and env-var sets as always. Behavioral equivalence was exercised in **both** implementations across the same five scenarios in a sandbox repo with a stubbed `gh`: base not checked out (checkout untouched, base ref advanced to the merged tip), base checked out (still fast-forwards), base held by a linked worktree (named and refused), a local base that had diverged from the remote (refused, ref untouched), and another process switching the checkout mid-run (alarm, non-zero). The frozen helper contract does not move: no argument, environment variable, `READY:`/`HEAD:` line or exit code changes.
- **The skill's claim that the helper parks the checkout on the base is corrected, and the concurrency hazard is stated where the wrong-repo hazard already lives.** 1.12.0's epic-branch section told you to switch the main checkout back after every slice close-out, and called that "the third path that parks the main checkout on the epic branch" — true then, and superseded now: there is nothing to switch back, so that instruction is gone rather than left as a harmless-looking ritual whose reason has evaporated. The hand-run close-out's sync step carries both forms for the same reason the helper does. The new hazard paragraph sits beside the wrong-repo one, because they are the same shape — a helper resolving shared state from ambient context — and both are stated with the failure that produced them.

## 1.12.0

- **`orchestrate` documented a cost it had no way to avoid, and now has the branch that avoids it.** Its transient-red section already said the quiet part: for a schema-first / all-or-nothing epic a full-green gate is unattainable mid-epic *by design*, and the instruction was to merge slices onto the shared integration branch anyway, on a green-modulo-baseline judgment. So the shared branch was knowingly red for the duration, and every worktree anyone cut from it during that window started from a red base — a cost stated plainly with no alternative offered. The **epic branch** is the alternative: a convergence branch cut from the integration branch that a multi-slice epic's slices fork from and PR into, gated as a whole, then merged back in one ordinary PR. The transient red now lives there, the integration branch never sees a state the epic has not finished converging on, and the "gate the integrated whole before you close" rule moves onto the epic branch too — where a cross-slice break surfaces before the shared branch has seen any of it, instead of after every slice has already merged into it.
- **The new concept is named `epic branch`, and `integration branch` keeps its existing meaning, deliberately.** The tempting alternative was to rename the base to "release branch" and call the new one the integration branch — and it is wrong twice. `integration branch` is this skill's project-*agnostic* term, where `release/x.x.x` is merely Trinity's instance of it, so the rename would bake one project's convention into the generic vocabulary; and it would silently repoint a live term, so that the same phrase names a different branch than it did in every older cached copy and CHANGELOG entry. In a document agents execute literally, that is the most expensive kind of ambiguity in the exact instruction where being wrong costs most ("branch from the integration branch"). The two-level vocabulary is instead stated once, where the skill first introduces the integration branch, so a reader meets both terms together rather than discovering the second one mid-procedure.
- **The trigger is one question, and slice count is explicitly not it.** *Does any intermediate state leave the integration branch in a condition you would not ship?* Primary: the epic is only correct as a whole — a NOT-NULL schema swap, a required interface field, a renamed module, where every state before the last consumer migrates is unshippable. Secondary: a contract seam, whose two halves landing separately means the branch is wrong in between — the primary condition by another route. Two conditions are marked contributing-but-never-sufficient, because each alone would degrade the trigger into a default: **slice count**, which measures how long a partial state sits on the branch rather than whether that state is broken (a real five-slice run — cache invalidation, a UI gate, a query poll, a SQL predicate, a store guard — was independently shippable at every step and left the branch green after every merge; a count trigger would have fired and bought nothing), and **an integration branch busy with other sessions**, which is the opposite concern and near-permanently true in a multi-session shop, so on its own it means "always".
- **The cost is stated, not buried: conflicts are deferred and concentrated, and the mitigation is mandatory.** Nothing about an epic branch makes a collision go away. It moves it — from a slice author resolving a small one against code they just wrote, to whoever lands the epic resolving a bigger one later with less context. The mitigation rides the wakeup tick that already runs the drain and the local fast-forward: merge `integration → epic` on that same cadence, so drift stays bounded and each collision surfaces while its slice author is still live. Merge, never rebase. A skill that sells a technique without its cost produces agents that apply it everywhere, which is the failure this framing prevents.
- **`decompose` finds it; `orchestrate` acts on it — the split already established for contract seams.** `decompose` grounds slices in real code and emits the producer → consumer map, so it is the only pass positioned to see whether two halves must land together, and it now answers the epic-branch question explicitly in every breakdown — recommended and why, or not-needed and why. An unanswered question reads as "no" without anyone having asked it, so the parallelization-plan template carries the line with its "not needed" default filled in. `orchestrate` holds the lifecycle: cut it in the main checkout (`git switch -c <epic> <integration>` — worktrees are only ever for slice work), point each slice's `setup-worktree.sh <slice> <epic>` at it, gate it once the fleet is quiet, then one ordinary PR into the integration branch, and delete it after that merge under the same never-past-git's-"not merged"-warning guard as any slice branch. Cutting it is three commands, not one, and the two easy to drop are the load-bearing ones: **push it**, or `origin/<epic>` does not exist and neither the HEAD-verification invariant nor `gh pr create --base` has anything to resolve; and **switch the main checkout back off it**, or the every-tick reconcile — which takes no branch argument and follows whatever is checked out — silently stops fast-forwarding the integration branch for the epic's whole duration, and the next unrelated worktree forks off a stale HEAD. The per-tick mitigation carries the same two steps for the same two reasons, and `merge-pr.sh` is a third path to the same parked state — it syncs the PR's base by checking it out, so a slice close-out during an epic leaves the main checkout on the epic branch and it has to be switched back. The helper is right to do that and is not changed; the skill just says what to do after it.
- **No helper changes were needed, which was checked rather than assumed.** `merge-pr.sh` reads the base off the PR itself (`baseRefName`) and drives the mergeability preflight, the merge, and the local fast-forward from that value — so a slice PR based on an epic branch closes out identically, fast-forwarding the local epic branch in the main checkout, with no argument or flag to add. The frozen helper contract does not move.
- **Both shapes stay readable, which was the constraint on how this was written rather than an afterthought.** Single-slice work is the common case and must remain followable start to finish without ever meeting an epic branch — if cutting one worktree for one bug fix required understanding epic branches, the change would have made the skill worse. So the concept lives in one self-contained section that opens by telling a single-slice reader to skip it, and the rest is surgical: the branch-from hard rule refined **in place** (epic branch when there is one, integration branch otherwise) rather than left as two rules that appear to contradict, and one bullet on the drain tick. The instructions naming a branch literally were re-pointed at *the PR's own base* rather than at the integration branch by reflex — the two conflict-resolution commands especially, since merging the integration branch straight into an epic's slice drags in commits the epic has not converged on and defeats the isolation the branch exists for. The one place a conditional would have reached an implementer is resolved by the orchestrator instead: the pasted handoff block now carries a `<base-branch>` the orchestrator substitutes, because it already knows which branch that is and an implementer left to work it out is one that can open its PR against the wrong one.

## 1.11.0

- **`merge-pr` ordered two irreversible steps before the only step that can fail, and now preflights the merge before touching anything.** The close-out has to tear the worktree down before merging — git refuses to delete a branch that is checked out in one, so `--delete-branch` needs the tree gone — and GitHub refuses to merge a draft, so the review flag has to be set before the merge too. Both constraints are real and both stay. What they cost was ordering: the worktree teardown and the `ready` flip ran first, and `gh pr merge` ran last, so a PR whose base had moved under it failed at the merge with `GraphQL: Pull Request has merge conflicts` — leaving no worktree to resolve the conflict in and a PR marked ready with no review behind it, which this workflow's own rules call a bug. It now asks GitHub whether the PR is mergeable before anything is touched: `CONFLICTING` stops with a non-zero exit, the worktree intact and the PR still a draft, and a message naming the fix — merge the base IN, in the branch's worktree, never rebase. `mergeable` is computed asynchronously, so `UNKNOWN` is treated as "not yet known" rather than as either verdict: it polls a few times and then falls through to the merge, because a preflight that hard-failed on `UNKNOWN` would be flaky on exactly the PRs that were just pushed.
- **And it restores the draft flag when the merge fails anyway.** `ready` has to precede `merge`, but it must not *survive* a merge that did not happen, or the PR sits wearing the one signal this flow reads as "an orchestrator read this diff and is merging it". The failure path now puts it back — and only when this run is what set it, so a PR that arrived already ready keeps the state it came with rather than being pushed into a draft nobody asked for. This matters most in the `UNKNOWN` fall-through, where the preflight could not rule the conflict out. The failure message names the recovery for the case the teardown already happened.
- **`setup-worktree --existing <branch>` attaches a worktree to a branch that already exists.** Until now no helper could: `setup-worktree` only ever ran `git worktree add -b`, which creates a branch and fails when one is already there — so recovering from the failure above took a raw `git worktree add` outside the helpers, and a tree cut that way gets none of the env symlinks, none of the config's `env`, and no install. `--existing` is the recovery form for a branch that outlived its tree, and everything downstream is unchanged, so a re-attached tree is as self-contained as any other and can face the gate. It takes **no base** — an existing branch's base is whatever it already forked from, so the argument shape is the flag and the branch, nothing else. If only `origin/<branch>` is left (the local branch is deleted on every successful merge) it recreates a local branch tracking the remote; if the branch does not exist at all it stops rather than creating one, because the fallback would fork off whatever HEAD it found. It is a flag and **never** an inference: attaching merely because a branch happens to exist would silently reuse a branch a caller meant to fork fresh off `<base>`, which is the stale-base seeding the worktree rules warn about hardest, and it arrives with a real tree and a real install where only the HEAD is wrong.
- **Both modes now print the worktree's resulting commit on its own line — `HEAD: <sha>`, beside `READY: <path>`.** The worktree flow's second invariant requires verifying that a new tree's HEAD equals the base tip before an agent is dispatched, and states that the helper does not do it for you. The old output invited *inferring* it from the `READY: … off <base>` text instead — which says what was asked for, in exactly the situation the invariant exists for. Printing the sha the tree actually landed on makes the honest check and the lazy read the same act. `READY:` itself is byte-identical, because callers parse it.
- **The frozen helper contract moves, on the record, in exactly two additive ways: a new `--existing` argument on `setup-worktree`, and a new stdout line beside `READY:`.** Nothing else does — no environment variable added, renamed or repurposed, no exit code reused, the `READY:` line untouched, and `merge-pr`'s and `remove-worktree`'s argument shapes unchanged. `AGENTS.md` now spells the contract out as it stands rather than pointing at "whatever is on `main`", since a frozen contract that is only described by reference is one nobody can check a change against. Both changes landed in both shells together, per the parity rule — and the parity check compares surface shape only, so behavioral equivalence was held to by hand and exercised in both: fresh fork, attach to a local branch, attach when only `origin/<branch>` survives, a branch that does not exist, a base passed to `--existing`, and a re-run over a tree that is already there.

## 1.10.0

- **`decompose` mapped conflicts by file, and file-disjointness turns out to predict almost nothing.** Both existing maps key on files — the conflict map for pairs brushing one file, shared hotspots for 3+ slices editing one structure — and a run whose slices were perfectly file-disjoint (zero merge conflicts across every worktree) still produced several genuine cross-slice breaks, every one of them a contract. The gap is the case where two slices touch **no file in common at all** and still break each other, because one produces a shape the other consumes: a return type, a schema field, a tool grant, a prompt variable, a config key. Neither existing map can see it, and the shared-hotspot remedy cannot help — there is no shared structure to hoist. So `decompose` now emits a second, separate map: producer → consumer → the shape between them. Two seams are called out as the ones a file map structurally cannot see, because nothing mechanical connects their halves: a producer and consumer in different languages or different trees (code defining a value that documentation or a prompt describes), and a seam with no compiler between the halves (a config, a prompt template, a generated brief). The producer's gate is green, the consumer's gate is green, and the break appears only when both are on the branch.
- **`orchestrate` gained the half that acts on it, because a map nobody checks is decoration.** A hand-back report is evidence about its own half and hearsay about the other — an implementer can say what it built, never what its counterpart now expects, having never seen the counterpart. So for any pair the decomposition flagged as a contract seam, reading each diff alone is two half-reviews rather than one review; the rule is to diff both halves against each other before merging either, checking the specific thing (the shape the producer now emits against the shape the consumer now reads). Both PRs will be green, because each gate only ever exercised one side. Split across the two skills deliberately: `decompose` is the pass that grounds in real code and is therefore the only one positioned to *find* a seam, while `orchestrate` is the only one holding both diffs at merge time and therefore the only one that can *check* it. In one skill alone it is either a map nobody acts on or a check with nothing to check against.
- **`orchestrate`'s stuck-handback rule covered blockers you can see, and missed the failure that does not feel like being stuck.** The existing paragraph handles a blocker, an unresolvable ambiguity, a check that will not go green — all states an implementer recognizes it is in. The new paragraph covers the inverse: the brief needs an identifier for something real (an id, a key, an owner, a path, a version), the thing does not exist yet or is unreachable from where the slice runs, and nothing is erroring — a value of the right *shape* can be produced and everything downstream accepts it. Types satisfied, scoped check green, tests passing, PR reading clean. That is the failure, not the escape from it: a fabricated identifier records the wrong fact permanently and quietly, with every guardrail confirming it. The rule names the tell (the input identifies a real thing, and you cannot obtain the real one) and the required move (stop, report what was needed and why it was unavailable *where you stand*, and what the orchestrator must decide). Written from the case where an implementer declined to synthesize an owning identifier and reported the gap instead — the alternative would have passed every check while silently attributing work to the wrong thing. Shape-correct and fact-wrong is the hardest defect class to find later, precisely because nothing about it looks broken.
- **`decompose` now treats documentation of a changing model as a sequencing constraint rather than a file question.** A slice that documents a subsystem must land after every slice that changes it, or it documents a shape that no longer exists by the time it merges — and unlike stale code, nothing fails. The exemption is stated with it, because the unqualified rule is also wrong: documentation of a part of the system the epic does not touch should say so in the wave note, otherwise "docs last" strands a correction that is already misleading its readers. Written from a dispatch where file-disjointness was checked, found clean, and two agents ran in parallel anyway — one documenting a model, the other rewriting it.

## 1.9.5

- **`orchestrate`'s no-suppressions rule was an unqualified absolute that this repo's own shipped code correctly violates, and it now distinguishes gaming from a documented exception.** Since `1.9.2`, `bin/setup-worktree.ps1` has carried a one-function `SuppressMessageAttribute` for `PSAvoidUsingInvokeExpression`: a project's `install` config value is a command *line* (`pnpm install --frozen-lockfile`, possibly with a pipe), so it has to be interpreted rather than spawned as an argv — which is what the `.sh` sibling's `eval "$INSTALL_CMD"` does, at the same stated trust level. "Fix the cause instead of suppressing" has no meaning against it: the flagged construct *is* the intended behavior, deleting the suppression turns CI red, and spawning an argv instead breaks every project whose install command carries a flag. So the rule as written left an implementer two bad options — delete a load-bearing suppression to comply, or conclude the rule is violated in the repo that ships it and stop trusting it — and that is the failure the carve-out names: a rule with an undocumented exception does not get followed carefully, it gets ignored wholesale. The fix is precision, not exemption.
- **The default stays absolute; a suppression you ADD is permitted only when all four conditions hold.** Narrowest scope the tool allows — never file-wide, and never a settings-file exclusion that silences the rule repo-wide, which is indistinguishable from deleting the check. A written justification in place through the tool's own mechanism (`Justification=`, `// eslint-disable-next-line <rule> -- <reason>`), never a bare disable comment. That justification saying why the flagged construct is correct *there*, not that the check is noisy. And a call-out in the hand-back report, so it is reviewed at merge rather than reconstructed later. The second condition is the load-bearing one: a justification-bearing suppression states a claim a reviewer can see and reject in the diff, while a bare disable asserts nothing and is invisible to review — which is what gaming looks like. Every prohibition the rule already named is unchanged and explicitly still gaming: bare disables, widening to `any`, deleting the flagged assertion, shaving comments to duck a max-lines cap, nudging one line under a threshold. None of them can satisfy the four conditions, because for none of them is there a correct-construct claim to write down.
- **The conditions test a suppression you introduce, never one you inherited — stated explicitly, because the unscoped reading recreates the exact failure the carve-out exists to prevent.** A first draft of this rule said only "you call it out in your hand-back report," with nothing scoping it to suppressions the slice adds; an implementer opening a file that already carried a correctly-justified one would read condition four as failing, and the rule declares that failing any one condition makes it gaming — so the compliant move became deleting load-bearing code and turning CI red. An existing suppression is the previous author's claim, already reviewed on the PR that added it: if you think it fails a condition, report it and leave it. The reviewer's copy carries the same scoping, so a pre-existing suppression a diff merely left alone is not that PR's to re-litigate.
- **The carve-out is written out once, and the second copy points at it.** The rule appears twice by design — once in the Implementer section, once in Hard rules — and both previously restated the prohibition in full. A four-condition exception written out twice drifts, and the next editor then has no way to tell which copy is authoritative, so only the Implementer paragraph defines it and says so; the Hard rules bullet keeps a compressed checklist for the reviewer holding a diff, names where the definition lives, and adds the half only a reviewer needs — bounce a suppression that meets the first three conditions but never appeared in the hand-back report, since one nobody flagged is one nobody reviewed.
- **`AGENTS.md` now states this repo's own position, because two places already asserted it did.** Both `CHANGELOG.md`'s `1.9.2` entry and a comment in `scripts/check.sh` refer to "the guardrail-gaming `AGENTS.md` forbids" — a rule `AGENTS.md` did not actually carry. It carries it now, together with the shipped suppression it makes an exception of, so a contributor working in `bin/` gets the answer locally instead of inferring it from a skill that describes other projects' workflow. The bullet **points at** `skills/orchestrate/SKILL.md` for the four conditions rather than paraphrasing them, per the rule `AGENTS.md`'s own preamble already states — a first draft did paraphrase, and had already drifted to three conditions by merging the two that a reader most needs kept apart. What it states locally is what only this repo can: which suppression exists, that it is load-bearing and not to be deleted for compliance, that it is not precedent, and that a repo-wide PSScriptAnalyzer settings-file exclusion is never the answer. The failure runs both ways: read the suppression as permission and you add an unjustified one beside it; read the rule as unqualified and you delete the load-bearing one and turn CI red.

## 1.9.4

- **1.9.3's Troubleshooting "Cause 1" named the wrong cause, stated it as confirmed, and is retracted.** It diagnosed a global `insteadOf` rewrite silently turning the clone's HTTPS into SSH — but that rewrite was never observed on the reporting user's machine; it was inferred from the error text alone and written up as "Confirmed on a real Windows install" without ever being checked against their actual git config. The disconfirming evidence was available and not sought: the same machine, same user, same git config had installed another marketplace's plugins minutes earlier with no trouble, because that marketplace declares explicit `https://…git` URLs via a `url` source rather than the `github` shorthand. Same host, different protocol, because of the source type each marketplace chose — nothing was wrong with the user's configuration. The real cause, per Claude Code's own documentation: the GitHub `owner/repo` shorthand source clones over SSH by default, and this marketplace's three plugin entries used exactly that shorthand for fully public repos that need no credentials at all, so Claude Code chose SSH for a request that had no reason to need it, and a reader with no `github.com` entry in `known_hosts` — normal for anyone who has never pushed over SSH from that machine — hit strict host-key checking. The fix was in our own marketplace manifest the whole time: `trinity-ai-labs/claude-plugins` now declares all three plugins via an explicit `url` source, so a current install clones over HTTPS and this no longer occurs. Cause 1 is rewritten to the real mechanism, keeps the literal error text a reader matches against, states the marketplace fix, and gives the `CLAUDE_CODE_PLUGIN_PREFER_HTTPS=1` escape hatch for anyone still pinned to an older entry or hitting the same shape from another marketplace. The `insteadOf` diagnosis and its `git config` fix commands are deleted outright rather than kept as a "possible other cause" — a fabricated cause presented alongside a real one sends a reader to edit global git config for a problem they don't have. Causes 2 and 3 (corporate TLS interception, dubious ownership) were unaffected and are unchanged. The README also gains a short note for plugin authors: prefer an explicit `url` source over the `github` shorthand for public plugins, since the shorthand's default SSH clone silently demands credentials a public repo's users have no reason to hold.

## 1.9.3

- **The README now has a Troubleshooting section for the three git errors that can block a Windows install, keyed by error text.** A `source: github` plugin clones over anonymous HTTPS and needs no credentials at all, so every install-time git failure traces back to the reader's own git config, never to this repo or the marketplace — both already clone clean, and every repo involved is public. The confirmed case is a global `insteadOf` rewrite that silently turns the clone's HTTPS URL into SSH, so a request that needed nothing suddenly needs an SSH key and a trusted host key it doesn't have; the fix scopes the rewrite to `pushInsteadOf` so pushes keep using SSH while clones and fetches go back to HTTPS. The section also covers corporate TLS interception (trust the CA bundle via `http.sslCAInfo`, never disable verification with `http.sslVerify=false`, which would leave every later fetch silently interceptable) and dubious-ownership (`safe.directory`). All three present as the same undifferentiated "git error," so the section is organized to match error text to cause rather than read top to bottom.
- **Five stale mentions of which CI job lints `bin/*.ps1` are corrected: `CHANGELOG.md` (1.9.2, fixed separately before this release), both `skip` branches of `scripts/check.sh`'s PSScriptAnalyzer step, `README.md`'s "Adding a skill" section, and `AGENTS.md`.** All five said PSScriptAnalyzer runs (or, in one branch, installs) in "the windows-latest CI job," but `windows-latest` doesn't run `scripts/check.sh` at all — it has no shellcheck, and a check that cannot run must not report ok, so that job only ever executes the helpers in both shells. The analyzer actually runs in the `check` job on `ubuntu-latest`, which ships `pwsh` and the module preinstalled; every mention now names that job. The claim was written once and copied outward before the CI restructuring it described changed underneath all five — two of the five weren't even in different files, they were adjacent branches of the same `if`.

## 1.9.2

- **The four `bin/` helpers now ship in PowerShell as well as bash, so a Windows session without Git for Windows has them at all.** Claude Code hands a native Windows install with no Git for Windows the **PowerShell tool**, where bash does not exist — so `setup-worktree.sh` there was never a script that failed, it was a command that did not exist, and the error read as a broken or missing plugin rather than as the wrong extension. `setup-worktree.ps1`, `setup-workspace.ps1`, `remove-worktree.ps1`, and `merge-pr.ps1` implement the same frozen contract: identical arguments, identical `WORKTREE_HOME` / `REPO` / `WORKSPACE` / `WORKTREE_DEST` / `MERGE_PR_FORCE` variables, the same `READY: <path>` line on stdout, the same exit codes. The PowerShell tool is rolling out progressively *alongside* the Bash tool rather than replacing it, so a Windows user can land in either shell — which is why both copies ship rather than one replacing the other. This path also has one advantage the bash side cannot have: `ConvertFrom-Json` is built in, so reading a project's config needs neither python3 nor node, where `setup-worktree.sh` has to shell out to one of them and fails outright on a machine with neither.
- **`WORKTREE_HOME` defaults to `%LOCALAPPDATA%\wt` on Windows in both shells, not just in bash.** `~/.worktrees/<workspace>/<leaf>/<repo>/node_modules/.pnpm/…` routinely runs past MAX_PATH's 260 characters, which is why the bash side moved; the PowerShell side has to move with it, and the reason is not cosmetic. `merge-pr` reads that location to decide whether an already-merged PR has anything left to close out, so if the two shells disagreed about where a worktree lives, a tree cut in one would be invisible to the other and the close-out would refuse with a "wrong repo" error against a perfectly good worktree. An explicit `WORKTREE_HOME` still wins on both sides.
- **`scripts/check.sh` now enforces the parity mechanically, because prose does not stop two hand-maintained copies of the same logic from drifting — it only makes the drift someone's fault afterwards.** `AGENTS.md` states the rule; this makes it a check that fails closed. Every `bin/<name>.sh` must have a `bin/<name>.ps1` sibling and vice versa, the two must agree on their runtime usage line and on the **contract** environment variables they consume, and every `.ps1` must be printable ASCII terminated by LF. The env-var comparison is scoped to the frozen contract surface (`WORKTREE_HOME`, `REPO`, `WORKSPACE`, `WORKTREE_DEST`, `MERGE_PR_FORCE`) rather than to every variable each side happens to touch: the bash helpers need MSYS path-translation plumbing that has no PowerShell counterpart at all, so demanding identical sets would go red on a correct implementation and the only way to green it would be to invent a variable nobody reads — exactly the guardrail-gaming `AGENTS.md` forbids. The usage line is read from **code, not comments**, so a header that paraphrases the invocation cannot shadow the runtime line the comparison exists to check. The `.ps1` scan set carries its own non-empty assertion, and so does the env-var extraction — two empty sets compare equal, so a pattern that quietly stopped matching would otherwise report perfect agreement.
- **The ASCII rule is enforced at the byte level, not by decoding the file.** Windows PowerShell 5.1 reads a file with no byte-order mark using the system ANSI codepage, so one non-ASCII byte — an em-dash in a comment being the likely one, since these helpers' comments are full of them — is decoded as mojibake and surfaces as a parse error nowhere near the character that caused it. Shipping a BOM to dodge that is worse than staying ASCII, so the check reports the offending byte and its line directly.
- **PSScriptAnalyzer reports `SKIP`, never `ok`, when `pwsh` is not installed.** It is the one step here that needs an optional tool, and `pwsh` is deliberately absent from the gate's required-tool list: this repo is zero-dependency by design, and a gate that needs an install is a gate nobody can run before pushing. The parity checks need no `pwsh` and always run; the analyzer is left to CI. A check that could not run must not print green, because that is a claim CI will later contradict — and the contradiction arrives after the merge.
- **CI gained a `windows-latest` job, because a Windows claim no runner exercises is a claim nobody checks.** Both existing jobs run on `ubuntu-latest`, so until now nothing on Windows was ever executed. The new job runs the real scenario end to end — init a throwaway repo, cut a worktree, assert its HEAD is the base tip and that the `READY:` line printed, tear it down, assert it is gone — in **both** shells a Windows user can land in: Git Bash and Windows PowerShell 5.1 (not `pwsh`, because 5.1 is what ships in-box and is therefore the compatibility target). It does not run `scripts/check.sh`: this branch's first CI dispatch found `windows-latest` has no `shellcheck`, and the script's own required-tool check correctly refused to print green there rather than silently skip it — a check that cannot run must not report ok, and this is that rule catching the exact gap it exists for. The job was restructured on the spot to give each runner only the job it can do: the "PSScriptAnalyzer must not SKIP" assertion moved to the `check` job on `ubuntu-latest`, which ships both `pwsh` and PSScriptAnalyzer preinstalled, so it lints `bin/*.ps1` there with no install step to be defeated by a transient PSGallery failure. `windows-latest`'s remaining and entire purpose is the one thing no other runner can do: actually execute the helpers on Windows, in both shells.
- **The docs that stopped being true came along.** The README's prerequisite line said "macOS or Linux… on Windows, run under WSL" and is now the real support matrix; "four executables" is eight; the repo tree grew the four `.ps1` files plus `AGENTS.md` and `.gitattributes`, which landed last release without being listed. `orchestrate` and `setup` both told an agent to invoke the helpers bare as `.sh`, which is exactly the instruction that fails on a PowerShell-tool session — both now carry the shell-selection rule and name the failure it prevents.
## 1.9.1

- **The four bash helpers now agree with themselves about what a path is.** Under Git Bash on Windows, `git` prints Windows-form paths (`C:/Users/…`) while anything derived from `$HOME` is MSYS-form (`/c/Users/…`). The filesystem accepts both, so a mismatch is never an error — only a string comparison can see one, which is why every failure in this release was silent rather than loud. Each helper now carries a `norm_path()` (`cygpath -u` where it exists, the identity function everywhere else) and routes every value that is later compared, grepped, or prefix-matched through it at the point of production. It is duplicated per script on purpose: `bin/` ships on a user's PATH under a parity rule that pairs every `bin/<name>.sh` with a `bin/<name>.ps1`, so a shared file would become a fifth helper owing a PowerShell sibling with nothing to do — PowerShell has no MSYS form to convert.
- **`setup-worktree.sh`'s idempotency guard failed OPEN on Windows.** It asked `git worktree list | grep -qF "$WT"`, comparing git's Windows-form output against an MSYS-form path, so the match never fired: a re-run fell straight through to `git worktree add`, which then died with "already exists" — the exact error the guard was written to prevent. It now reads `git worktree list --porcelain` and compares normalized paths for **exact equality**, which also closes a second, quieter bug the substring match had on every platform: a leaf that is a prefix of another registered worktree (`pfx` against `pfx-retry`) matched it, so setting up `pfx` reported the wrong worktree as already existing and created nothing.
- **`setup-workspace.sh` hung forever when started from a Windows-form path.** Its walk up to `.agents/workspace.json` looped `while [ "$DIR" != "/" ]`, and `dirname` starting from `C:/Users/…` bottoms out at `C:/`, which is never `/` — an outright hang with no output at all, the worst failure shape in the set. It now loops until `dirname` reaches a fixed point, which terminates from any root spelling.
- **`remove-worktree.sh` resolved the wrong target for an absolute Windows path.** A `C:/…` path does not begin with a slash, so the `== /*` test filed it as a *branch leaf* and the script looked for `$WORKTREE_HOME/<project>/C:/…` — a path that never exists, so the run reported "already removed" and exited 0 having removed nothing. All three sites now accept `[A-Za-z]:[\\/]` as absolute too, and the kill-scope prefix is normalized alongside them.
- **`remove-worktree.sh` now finds and kills processes on Windows.** `lsof` and `pgrep` do not exist in Git Bash and both call sites were `command -v`-guarded, so the scan found exactly zero processes, printed "no running processes found", and the removal then failed on files Windows still had locked. There is now a third scan using `Get-CimInstance Win32_Process` for the command-line match and `taskkill /PID <n> /T /F` to terminate. **Windows PIDs and MSYS PIDs are unrelated numbers from unrelated counters**, so the two are kept in separate arrays and each only ever reaches its own killer; the scan also excludes this shell and every one of its ancestors, because `/T` kills a process *tree* and the script's own argv contains the worktree path whenever the caller passed one. What has no cheap Windows equivalent — `lsof +D`'s open-handle enumeration, which needs Sysinternals `handle.exe` — is now printed as an explicit SKIPPED notice, so an undetected lock holder reads as "we did not look" rather than as "nothing was there".
- **`setup-workspace.sh` no longer fails outright on a node-only machine.** It read its manifest with `python3` and nothing else, while `setup-worktree.sh` — reading the same kind of file for the same flow — had had a node fallback all along. It has one now.
- **Both JSON readers now pick an interpreter by RUNNING it, not by `command -v`.** On Windows `command -v python3` succeeds against the Microsoft Store's `python3.exe` App Execution Alias: a stub that opens the Store, prints nothing, and exits. A `command -v`-only probe therefore selects an "interpreter" that emits an empty config, which the caller `eval`s into silence — a bare worktree, or a workspace reported as having no members, with no error anywhere. Each candidate (`node`, `python3`, `python`, `py -3`) is now validated by executing it and checking what came back, and `setup-worktree.sh` additionally captures its reader's output *before* `eval`ing it, so a reader that failed is a hard stop instead of an empty assignment.
- **`setup-worktree.sh` says when an env file was copied instead of symlinked.** Git Bash's `ln -sf` copies unless Windows Developer Mode is on — an unprivileged process cannot create a symlink otherwise — and `ln` exits 0 either way. A copy is a point-in-time snapshot, so later edits to the main checkout's env file stop reaching the worktree, and that surfaces much later as a test reading a stale value with nothing pointing back at setup. It now checks the result and names the files, with the fix.
- **`WORKTREE_HOME` defaults to `%LOCALAPPDATA%/wt` on Windows.** `~/.worktrees/<workspace>/<leaf>/<repo>/node_modules/.pnpm/<pkg>@<version>/…` routinely runs past Windows' 260-character `MAX_PATH`, and the install then fails naming some deeply nested file rather than the length that actually broke it. The default is identical in all four helpers, which matters because `merge-pr.sh`'s wrong-repo guard decides "is there anything to close out here?" from whether a worktree exists under it — a helper that disagreed would look at an empty directory and refuse to close out a perfectly normal merged PR. An explicitly set `WORKTREE_HOME` still wins everywhere, unchanged. `setup-worktree.sh` also warns when `git config core.longpaths` is unset, since that is the same ceiling from git's side.
- **The docs stop stating `~/.worktrees` and `~/.zshenv` as unconditional facts.** Both are now qualified per platform. `~/.zshenv` in particular has **no Windows equivalent** — PowerShell's `$PROFILE` is interactive-only and is therefore the exact same trap `~/.zshrc` is — so the docs name the real answer: a persisted user environment variable, which every later process inherits regardless of shell.

## 1.9.0

- **`.gitattributes` pins `bin/*.sh` and `bin/*.ps1` to LF.** Git for Windows defaults to `core.autocrlf=true`, so without this file a Windows clone checks the shell helpers out with CRLF line endings, and every one of them dies at `#!/usr/bin/env bash\r` with "bad interpreter" — before any other Windows problem is even reachable. Pinning the line ending at the repo makes the checkout correct regardless of the cloning machine's `autocrlf` setting, rather than depending on every contributor's local git config agreeing.
- **`bin/merge-pr.sh`'s close-out step 1 was calling a path that does not exist, on every platform, not just Windows.** It invoked `"$WORKTREE_HOME/remove-worktree.sh"`, which resolves to `~/.worktrees/remove-worktree.sh` — but the helper ships in `bin/`, next to `merge-pr.sh` itself, never under `$WORKTREE_HOME`. It now resolves the sibling the way `setup-workspace.sh` already does, via `HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` computed at the top of the script, so the call no longer depends on `$WORKTREE_HOME` pointing anywhere in particular.
- **The same script's wrong-repo guard only recognized one worktree layout.** It checked for a worktree at `$WORKTREE_HOME/$PROJECT/$LEAF`, which is where a worktree lands outside a workspace — but a workspace member lands at `$WORKTREE_HOME/<workspace>/<slug>/<project>` instead (`setup-worktree.sh`'s own layout rule). The guard now checks both layouts, so closing out a merged PR for a workspace member no longer misreads a perfectly normal worktree as evidence of the wrong repo.
- **`AGENTS.md` now exists, and carries this repo's conventions instead of `.agents/worktree.json`'s `briefConventions` field.** That field had grown into a full rulebook packed into one JSON string — hard to read, hard to diff, and exactly the kind of convention the `setup` skill itself says belongs in `AGENTS.md` with a pointer left behind, not copied inline. Nothing was dropped in the move; every sentence carried over verbatim. `AGENTS.md` also gains two new sections this release starts needing: **the frozen helper contract** — the CLI arguments, env vars, the `READY: <path>` stdout line, and exit codes that this repo's bash helpers expose are pinned as of this version, because a PowerShell port of the same helpers is about to be built against exactly this contract, and an unannounced change on the bash side would silently break parity with a language nobody would think to re-check; and **the `bin/` parity rule** — every `bin/<name>.sh` will need a `bin/<name>.ps1` sibling with a matching usage line and env-var set, kept ASCII-only because PowerShell 5.1 misreads UTF-8 without a BOM and these helpers' comments are full of em-dashes, since a helper shipped in only one shell is simply unavailable to users on the other.

## 1.8.2

- **`setup`'s bootstrap exception claimed something false, and this repo was the counter-example.** It read *"the repo can't cut a worktree until this file exists — the flow can't bootstrap itself"*, which licensed a direct push to the integration branch for any repo lacking a config. But `setup-worktree.sh` cuts a worktree with or without one — it warns on stderr and makes a **bare** one — and bare is only fatal where the project **has an install step**, because then the worktree has no `node_modules` and every check inside it fails for reasons that read as code bugs rather than as a missing config. For a zero-dependency repo a bare worktree is fully functional and the config lands through a normal PR. The exception now says exactly that, and names the failure it prevents; the old wording made a legitimate PR look like a rule violation, which is the direction that costs a review record.
- **The repo that ships `setup` finally has the file `setup` tells everyone to write.** `orchestration-skills` had no `.agents/worktree.json`, so an orchestrator working here had to improvise the gate and the conventions — a guessed gate is one that passes while testing nothing. It declares what is actually true rather than the full key set: zero dependencies means no `install` and no `envFiles`, and a check that runs in under a second means `gate` and `scopedCheck` are the same command with no queue. Both readings are ones `orchestrate` already handles explicitly, so the repo lands in a mode the skill understands instead of one it has to infer.
- **There is now one gate, `scripts/check.sh`, and CI runs that rather than a second copy.** Every check lived inline in the workflow, so a contributor had nothing to run before pushing and CI was the first thing that ever evaluated a change. Two copies of a check drift, and the one that stops firing prints the same green as the one that passed — which is also why every check here asserts its file set is non-empty before scanning, and why the script reports *every* failure before exiting rather than stopping at the first. The version-bump guard deliberately stayed inline: it diffs against a PR base and reads the manifest as it was at that commit, so a local copy would either not work or lie about what a merge would ship.
- **`.agents/` and `scripts/` are exempt from the version-bump guard.** Both are contributor tooling — Claude Code never loads either and neither reaches a user's install — so a PR touching only them ships nothing, and demanding a bump for it invites a bump that ships an unchanged plugin. `market-skills` already exempted both; the two repos disagreeing was the inconsistency to remove. The exempt list stays deliberately inverted — it names what does NOT ship, so a new directory requires a bump by default — and now says why each entry earns its place.
- **Two statements about this repo went stale and are corrected.** `setup`'s tier table listed it under "no flow at all", and `orchestrate` used it as the example of a repo that edits `main` directly — both written before 1.8.1 made all three skills repos PR-only, and both now contradicted by the config that just landed. An agent reading the tier prose would have concluded it could push straight to `main` here, on the authority of the same skill that forbids it a hundred lines later.

## 1.8.1

- **The `orchestration-skills`-repo exemption from worktree/PR ceremony is withdrawn — this repo, `market-skills`, and `framework-skills` are all PR-only now, docs and CHANGELOG included.** The exemption let an editor push straight to `main` on the reasoning that this is "tooling"; but a plugin install is pinned to `.claude-plugin/plugin.json`'s `version` exactly like the other two repos, so a change here reaches users exactly the way theirs do — there was never a real difference to justify the special case. And a repo whose product is prose has no gate that can tell whether a rule is *correct*; the diff is the only review artifact there is, so a direct push spent that record to save a worktree. The rule now lives as its own hard-rules bullet instead of a clause in the `setup-worktree.sh` parenthetical, where an agent skimming for worktree mechanics was never going to look for a repo-wide policy.

## 1.8.0

- **Docs are now a slice's responsibility, stated in three places because the miss is silent.** Nothing in a gate fails when a doc stops being true: the slice goes green, the PR merges, and the miss surfaces later as a user acting on something that changed underneath them. That silence is the whole problem — an implementer that never considered the docs and one that considered them and found nothing produce identical output, so there was no way to tell them apart at review. `decompose` now names the docs each slice falsifies in its `Owns` list, `orchestrate` carries the docs rule as a fifth paste-verbatim brief item, and the implementer's hand-back reports a per-doc verdict rather than a bare "docs reviewed".
- **The rule that makes it work: search by the behavior, never by the vocabulary the change introduces.** This is the failure that motivated the release, and it defeats the obvious version of the check. Prose written for a user describes a feature in plain language and contains none of the identifiers a change adds, by construction — so grepping the docs for your own new terms returns nothing and reads as "no docs affected", with the search itself supplying the false confidence. The observed shape: a README paragraph enumerating the levers a user controls, which a change added a member to, described entirely in words the change never used. Both skills now say to ask what a reader currently believes and which of those beliefs the slice makes false, then to READ the candidate docs rather than grep them.
- **Scoping a doc out is a decision that has to be recorded, because an implementer will not revisit it.** A decomposer works from the plan, not from the diff, so "this doc is unaffected" is a claim made before the code exists — and once it reaches a brief as a do-not-touch, it is settled for everyone downstream. Both skills now require the negative case to be written down as an explicit not-affected-because, which is what lets an implementer overturn it when the diff disagrees. `orchestrate`'s review loop names the orchestrator's own version of this as the failure mode to watch for: a stale doc most often ships not because the implementer skipped it, but because the brief told it not to look.

## 1.7.1

- **`decompose` prescribed the `gh api` flag that publishes the path instead of the body.** Its three write examples used `-f body=@<file>`, and `-f` is `--raw-field` — it sends the value verbatim, so following the skill posted the literal string `@/tmp/…/decomp-36.md` to GitHub. Only `-F` expands a leading `@`. The failure is silent in the worst direction: `gh` exits 0 and prints a comment URL, so the agent reports success and only a human opening the issue ever sees it. One run put four unusable comments across three issues before anyone noticed. `write-issue` documented the trap correctly from its first commit; `decompose` — the skill that posts the largest bodies — never got the same treatment.
- **The warning travels with the flag, not just the correction.** A corrected flag with no rationale reads as arbitrary style and gets reverted by the next editor, so the fix carries the reason and the **verify-after** step: refetch the body and confirm it's markdown. The refetch is the only thing that catches this at the time it happens, since nothing in the exit code will.
- **CI now fails a skill that prescribes `-f` with an `@file` value.** This is a docs bug in a repo whose product is docs — nothing mechanical stopped it reappearing in the next skill written, and the author cannot see it at author time. The grep is anchored on `gh api` so that prose *quoting* the wrong flag to warn against it stays legal: a guard that can't tell a prescription from a warning would flag `write-issue`'s own warning line and force the next author to delete the warning to get green.

- **The gate queue lives under the user's home, not the OS temp dir.** 1.5.0 made a verdict survive a failed post by writing it to the ticket before reporting it, then left that ticket in a directory the system is licensed to reap — so the one artifact added to keep a verdict recoverable was handed to a sweeper with no idea anyone was still waiting to read it. The root is now `~/.gate-queue/<project>/`, alongside the `~/.worktrees/<project>/` convention, and its placement is stated as generic rather than something a project chooses.
- **The slot stays in the OS temp dir, and the reference says why.** Otherwise the first reader to move the queue moves the lock too, for symmetry, and believes they have been consistent. The slot is a mutex, not data: it carries no verdict, its holder-PID is liveness-checked so an abandoned one is stolen rather than trusted, and it is held only for the minutes a gate runs — well inside any reaper's threshold. Losing it to a sweep costs the next runner one `mkdir`.
- **A retention invariant bounds `done/`, keyed on delivery rather than age.** Nothing previously said what limited the ledger, so a project built from the reference grew one forever. The predicate is the whole rule: a ticket flagged for a human — retries exhausted, or the head moved off the gated SHA — still holds a verdict nobody has seen, and `reported: false` is what an abandoned ticket keeps, so a prune trusting age plus "not reported" deletes exactly the escalated records and spares the delivered ones already posted to the PR. The prune rides the reconcile walk, which already reads every ticket, and the clock is injectable so the rule is testable without waiting.
- **The failing tail is scoped to a red verdict.** A green comment is one line that quotes nothing, so a tail captured on green is the ledger's largest field in its most common case and no reader ever opens it. Called out with the trap it hides: when the routine computing the tail also cleans up the task-runner's run-summary scratch files, that cleanup has to be lifted out to survive being skipped on green.
- **The queue layout diagram shows `done/` as `<name>.json.<runnerPid>`.** Settling is a rename out of `processing/` and the claim suffix rides along; the diagram claimed a bare `<name>.json`, which had a reader coding a normalization step with no job.

## 1.6.0

- **`draft → ready` now means "an orchestrator read this diff and is merging it".** It used to encode a gate verdict, which is a claim a machine cannot make: a green suite says the tests passed, never that the agent solved the right problem, stayed in scope, or didn't reach green by weakening an assertion. Nothing mechanically stopped a PR nobody had read from being merge-eligible, and the playbook patched that with prose. The flag that authorizes a merge is now set by the reader.
- **The gate reports green as a PR comment, and leaves the PR draft.** It already commented on red; it now comments in both directions and never touches the draft flag. That collapses the reading to one sentence that holds in every mode — **a PR is gated iff it carries a gate comment** — with three states: `draft + ✓ comment`, `draft + ✗ comment`, and `draft + no comment` (the gate never reported: reconcile and look again).
- **`bin/merge-pr.sh` performs the flip, one line above `gh pr merge`.** Approval and merge are one invocation, so `ready` can never be a durable badge that goes stale when a fix agent pushes after the review. It sits inside the not-yet-merged branch, so a re-run of a half-done close-out stays idempotent.
- **Override gate mode keeps one axis and loses the other.** It still decides *who runs the gate and when* — the implementer runs the full `gate` itself, in the foreground, instead of enqueuing — and both reasons to grant it stand, as does the permanent override for repos with no queue. It no longer decides *who says the diff was read*: an override-mode implementer opens a draft PR and posts its own gate result as a comment, exactly like the queued path. The `--draft`-less PR disappears from every path, and an implementer that opens one is a bug with a mechanical consequence rather than a cosmetic slip.
- **This revises the 1.5.0 delivery rule** — *"a green counts as delivered only when the ready-flip **and** the comment both succeed"* — to: a verdict is delivered exactly when its comment lands. That is not a reversal. 1.5.0 made a verdict survive a failed post, by recording it on the ticket before reporting it; 1.6.0 makes a verdict stop meaning "approved". Both hold, and the reconcile-before-claim pass and the stale-SHA refusal that 1.5.0 introduced are untouched — a stale green comment on a moved head is still a false report.

## 1.5.1

- **This repo is no longer the marketplace.** It was both a plugin and the storefront selling the other two, so installing `frameworks` or `market` meant adding *this* repo as a marketplace first — routing unrelated traffic through `pipeline`'s repo and making the three plugin repos non-comparable. The catalogue moved to [`trinity-ai-labs/claude-plugins`](https://github.com/trinity-ai-labs/claude-plugins), which ships no plugin of its own. The marketplace *name* is unchanged, so `pipeline@trinity-ai-labs` still resolves — only the `marketplace add` line moves.

## 1.5.0

- **A gate verdict is recorded before it is reported.** The queue's invariants covered process death and filesystem races but not the one step that depends on a machine you do not control — the report. A failed post destroyed the verdict outright, since the ticket moved to `done/` unchanged and the result existed nowhere but GitHub. The ticket now carries the outcome, exit code, failing tail, the SHA that was gated, and whether delivery succeeded, so `done/` is a ledger rather than a record that something happened.
- **Drain reconciles before it claims.** Each pass re-posts verdicts that were decided but never delivered, without re-gating. A re-post is refused when the PR's head has moved off the gated SHA — that verdict describes code the PR no longer carries, which is the same false-report as gating a mutated worktree — and retries stop at a cap so a deleted PR cannot make every future pass re-attempt a doomed post.
- A green counts as delivered only when the ready-flip **and** the comment both succeed; a PR left in draft reads as ungated whatever the comment says.
- `orchestrate` gains the missing reading for **a draft PR with no gate comment** — a third state that is neither green nor red, produced by a report that never landed as much as by a gate that never ran. It was previously indistinguishable from red, which invites dispatching a fix agent at a slice with nothing wrong with it.

## 1.4.0

- `setup` onboards a polyrepo workspace, and treats the workspace manifest as a **derived artifact** rather than something to track. The containing folder is a local convention — its name and which members are cloned differ per machine — so the manifest is regenerated by running setup at the root, not synced through a meta-repo.
- Each field is pinned to a tracked source: members from the child dirs with a `.git`, the integration branch from what they are actually on, contracts and conventions from the workspace's own docs. Inferring a contract from resemblance is called out as forbidden — a wrong one makes contract closure look present while protecting nothing, which is worse than declaring none.

## 1.3.0

- **Contract closure.** A cross-repo contract now declares its `owner` and `consumers`, and a task that includes the owner automatically includes them. A repo whose generated types live in three other repos cannot be changed alone — excluding a consumer while the owner is in the task is refused outright rather than producing a task that can neither update nor verify the other side.
- `--dry-run` prints the resolved member set without cutting anything, so an orchestrator can see a task's real surface — including repos pulled in by closure — before paying for the installs.

## 1.2.0

- **Polyrepo workspaces.** A containing folder of sibling repos can declare `.agents/workspace.json`, and `setup-workspace.sh <branch> [repo…]` cuts one worktree per member into `~/.worktrees/<workspace>/<leaf>/<repo>` — the workspace's own layout, so cross-repo paths still resolve and both stacks run side by side. Members are selectable by name, by `--exclude`, or by a manifest `"default": false` for the repo a workspace rarely touches with the others.
- Worktrees for a repo inside a workspace are namespaced under it. Bare polyrepo names — `api`, `client`, `admin` — collide across unrelated projects in a flat `~/.worktrees`.
- Fixed: `setup-worktree.sh` created its directory from the old flat path, leaving stray empty dirs when the real destination was elsewhere.

## 1.1.0

- The marketplace now carries a second plugin, [`frameworks`](https://github.com/trinity-ai-labs/framework-skills) — the Effect v3 and SolidJS reference skills, previously two repos behind shell installers.
- `orchestrate` and `decompose` name those skills by their namespaced ids (`frameworks:effect-v3`, `frameworks:solid`), since plugin skills are always namespaced and the bare names no longer resolve.

## 1.0.1

- Marketplace install documented as the primary path; the skills-directory clone demoted to a development convenience.
- `CHANGELOG.md`, plus a CI job that fails when plugin content changes without a version bump.

## 1.0.0

First release as a Claude Code plugin.

- **Four skills** — `setup`, `write-issue`, `decompose`, `orchestrate`, namespaced `/pipeline:*`.
- **`bin/` on PATH** — `setup-worktree.sh`, `merge-pr.sh`, `remove-worktree.sh` are bare commands while the plugin is enabled, replacing the old `install.sh` symlink dance.
- **Per-project config moved into each repo** at `.agents/worktree.json`, declarative rather than sourced bash. The previous location keyed config to a checkout's *directory name*, which had already forced a duplicate config file for one repo cloned under two names.
- **`setup` skill** — onboards a repo by grounding its real lockfile, scripts, and CI, then scaffolds a durable gate queue into that repo if it wants one. The plugin carries the knowledge; the project owns the code.
- **Tiering** — a repo may want no flow at all, worktrees plus a single check, or the full gate-and-queue. `gate` == `scopedCheck` is a valid answer, and absent `enqueue`/`drain` means the implementer gates in-line.
