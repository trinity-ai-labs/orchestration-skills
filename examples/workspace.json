{
  "branchingModel": "release",
  "integrationBranch": "release/0.1.0",
  "branchPrefixes": [
    "feature",
    "hotfix"
  ],
  "members": [
    "api",
    "client",
    "admin",
    {
      "path": "website",
      "default": false
    }
  ],
  "crossRepoContracts": [
    {
      "what": "Generated TypeScript types",
      "owner": "api",
      "consumers": [
        "admin",
        "client"
      ],
      "note": "api generates them; consumers hold copies and regenerate. Never hand-edited — so a task touching api must include its consumers."
    }
  ],
  "briefConventions": "Branch only as feature/* or hotfix/*, and target the integration branch in every repo. A change crossing repos uses the SAME branch name in each, and the contract side lands before its consumers."
}
